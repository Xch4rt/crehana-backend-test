---
phase: 08-web-ui
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - README.md
  - tests/architecture/test_documentation_claims.py
  - AI_WORKFLOW.md
  - CLAUDE.md
  - DECISION_LOG.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [UI-08]

must_haves:
  truths:
    - "README.md says what the UI is, on which URL it runs, and that it is beyond the brief — without adding a single row to the requirement-to-evidence map, which stays keyed on the PDF"
    - "make rehearse proves, in a fresh clone built from nothing, that the UI answers and that a bearer-authenticated call through its /api/ proxy returns the whole-list completion percentage"
    - "The documentation gate covers Phase 8: every ADR the README and AI_WORKFLOW cite resolves, the quoted counts are the files' counts, and the human/AI account covers eight phases"
    - "AI_WORKFLOW.md's sentence rejecting a Node toolchain in a Python deliverable is amended rather than left standing false, and the reversal is attributed honestly: the AI recommended against the UI, the human decided for it"
    - "CLAUDE.md's Project Rules gain the frontend rules, each one backed by a gate that already exists"
    - "UI-01..UI-08 are ticked in REQUIREMENTS.md, the ROADMAP shows Phase 8 complete, and STATE.md says the tree is clean and make rehearse is green on the final commit"
    - "07-05 can resume at its task 4 push checkpoint with nothing of Phase 8 left to do"
  artifacts:
    - path: "README.md"
      provides: "The UI section, the URL, the beyond-the-brief statement, the rehearsal's UI check and the extended Pending list"
      contains: "localhost:8080"
    - path: "AI_WORKFLOW.md"
      provides: "The amended Node-toolchain sentence, the Phase 8 human/AI block, and one dated Phase 8 incident entry"
      contains: "Phase 8"
    - path: "CLAUDE.md"
      provides: "The frontend Project Rules, each naming the gate that enforces it"
      contains: "frontend/"
    - path: ".planning/STATE.md"
      provides: "The hand-edited position: Phase 8 complete, rehearsal green on the final commit, 07-05 resumable at task 4"
  key_links:
    - from: "README.md"
      to: "scripts/clean-clone-rehearsal.sh"
      via: "the UI verification block inside the rehearsal markers, executed in the clone"
      pattern: "rehearsal:end"
    - from: "tests/architecture/test_documentation_claims.py"
      to: "AI_WORKFLOW.md"
      via: "PHASES extended to eight, so an absent Phase 8 block is a red test"
      pattern: "PHASES"
    - from: ".planning/REQUIREMENTS.md"
      to: "README.md"
      via: "UI-01..UI-08 ticked, and deliberately absent from the [PDF] evidence map"
      pattern: "UI-0"
---

<objective>
Make every document true again, and prove it by running the README rather than reading it. Phase 8
put a Node toolchain and a browser client into a Python deliverable that spent seven phases saying it
had neither. This plan is where that is paid for: the README gains a UI section that states plainly
that the brief asks for no UI; the clean-clone rehearsal gains a UI check it actually executes;
`AI_WORKFLOW.md`'s sentence about a Node toolchain is amended instead of being quietly left wrong;
and the two reversed positions are named by id.

Purpose: UI-08, roadmap Phase 8 SC-5 and SC-6.

Output: an honest README, a rehearsal that is green on the final commit of the phase, a documentation
gate that covers eight phases, the Phase 8 human/AI block and incident entry, the frontend Project
Rules, the requirement ticks, and a STATE.md that leaves 07-05 resumable at its task 4 checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-web-ui/08-CONTEXT.md
@.planning/phases/08-web-ui/08-01-SUMMARY.md
@.planning/phases/08-web-ui/08-02-SUMMARY.md
@CLAUDE.md

<interfaces>
**What plans 08-01 and 08-02 already did, so this plan does not redo it.** 08-01 appended ADR-105
through ADR-108 and moved the README's quoted count to `108 ADRs`, and added five `make ui-*` rows to
the README's "Local development" table. 08-02 changed no document at all. So this plan starts from a
README whose ADR count is already correct and whose target table is already complete — **do not
re-add either**. Read `08-01-SUMMARY.md` first and confirm that for yourself, because appending an
ADR in this plan means moving the count again in the same commit.

**The gates this plan has to satisfy** (`tests/architecture/test_documentation_claims.py`, inside
every `make test`):

- `test_the_adr_count_the_readme_states_is_the_count_in_the_log` — `\b(\d+) ADRs\b` in README.md must
  equal the number of `^## ADR-NNN:` headings.
- `test_the_contract_count_the_readme_states_is_the_count_in_importlinter` — `\b(\d+) contracts\b`
  must equal the `[importlinter:contract` count. **This plan adds no contract**; `.importlinter`
  governs `taskmanager` only and has nothing to say about TypeScript.
- `test_every_make_target_the_readme_names_exists` — every `make <target>` in a code span or fenced
  block must be on `.PHONY`.
- `test_the_endpoint_table_*` — set equality against `app.openapi()`, in both directions. **No route
  changed in this phase**, so the table must not change either.
- `test_the_rehearsal_markers_are_one_well_formed_pair` — exactly one `begin`, one `end`, in order,
  with at least 3 command lines between them.
- `test_the_rehearsal_region_needs_no_virtualenv` — inside the markers, the only `make` targets
  permitted are `env`, `up`, `down`, `docker-test`. **No `ui-*` target may appear there**; the UI
  check inside the region uses `curl` and POSIX tools only.
- `test_the_human_ai_account_covers_every_phase` — `PHASES: Final[tuple[int, ...]] = (1, 2, 3, 4, 5,
  6, 7)`, scoped to the section under `## Human-Decided vs AI-Delegated`. Task 2 extends it to
  include 8.
- `test_the_scan_is_not_vacuous` — floors: `MINIMUM_ENDPOINT_ROWS = 19`, `MINIMUM_ADR_CITATIONS = 5`,
  `MINIMUM_PHONY_TARGETS = 8`, `MINIMUM_MERMAID_BLOCKS = 3`, `MINIMUM_REHEARSAL_COMMANDS = 3`. All
  stay satisfied.

**What `make rehearse` does to the README** (`scripts/clean-clone-rehearsal.sh`, ADR-103, extended by
plan 08-01 task 3):

1. Stops the developer's stack with a plain `docker compose down` and leaves it down.
2. Refuses to start on a dirty working tree — it clones the **committed** tree.
3. Step 5, before the build: every backticked token in `README.md` that looks like a path
   (matching `^[A-Za-z0-9_.][A-Za-z0-9_./-]*$` and containing a `/` or a known extension or a leading
   dot) **must exist in the clone**, with `.env` as the single stated exemption. So a README that
   writes `` `frontend/node_modules` `` in backticks would fail the rehearsal — that directory is
   git-ignored. Write it as prose, never in a code span. `` `frontend/package.json` ``,
   `` `frontend/nginx.conf` `` and `` `frontend/src/api/client.ts` `` are all fine: they are tracked.
4. Step 5 also requires every `[PDF x.y]` key cited in `.planning/REQUIREMENTS.md` to have a
   `^| <key> |` row in the README's evidence map. **The UI requirements are `[U]`, not `[PDF]`**, so
   they add no rows and the map must not gain any — that is D-09 and the orchestrator's instruction
   in one mechanism.
5. `docker compose build --no-cache`, which now builds the `ui` image too.
6. It extracts the command lines from the fenced ` ```bash ` blocks between the markers,
   concatenates them into one script and runs it under `set -e -x`. A bare `make up` line becomes
   five generated lines: the backgrounded `make up`, the api health poll, the api identity
   assertion, the ui health poll and the ui identity assertion (08-01 task 3).
7. Shell state is shared across all the region's blocks, so `$API`, `$AUTH`, `$LIST` and `$TASK` from
   the quickstart are still set in any block written after it.

**The state of the world when the quickstart finishes**, which the UI block can rely on: two tasks in
`$LIST`, one of them (`$TASK`) `completed` and `priority: high`, the other `medium` and `pending`.
So `GET /api/v1/task-lists/$LIST/tasks?priority=high` returns **one** item while reporting
`"total_tasks":2`, `"completed_tasks":1`, `"completion_percentage":50.0`.

**The sentence in `AI_WORKFLOW.md` that must be amended** sits just above the
`## Human-Decided vs AI-Delegated` heading and reads:

> None of the three diagrams is rendered anywhere in this repository's toolchain. There is no
> local Mermaid renderer and adding a Node toolchain to a Python deliverable was judged the wrong
> trade, so the syntax is deliberately restricted to what GitHub renders reliably...

Locate it with `grep -n "wrong" AI_WORKFLOW.md`. It is outside the Incident Log, so it is editable in
place — that is this project's rule, and the Incident Log's append-only rule is what it is contrasted
with.

**The Out-of-Scope rows already amended by the user before planning** (do not re-amend, just check
they are still coherent): `.planning/PROJECT.md` line ~130 and `.planning/REQUIREMENTS.md` line ~148
both carry a struck-through "Frontend / UI" row with a reversal note dated 2026-09-19.

**Where 07-05 is parked.** `.planning/phases/07-documentation-delivery/07-05-PLAN.md` is at its
**task 4**, a blocking human checkpoint approving the push. Tasks 4-7 (approve, push, CI run, badge
and diagram check, delivery email) run after this phase. **Do not open, edit or execute that file**,
and do not push anything. The one thing this plan owes it is a repository in the state task 4
expects: clean tree, every gate green, `make rehearse` green on the final commit, and a STATE.md that
says so.

**Hand-edit the planning files.** `gsd-sdk`'s state handlers have regressed `.planning/STATE.md` in
every plan since Phase 7 opened — nine consecutive plans have recorded it. Edit STATE.md and
ROADMAP.md by hand and diff the result.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — the UI section, the rehearsal's UI check, and the Pending list (UI-08)</name>
  <files>README.md, tests/architecture/test_documentation_claims.py</files>
  <read_first>
    - README.md in full — all ten sections, and specifically the rehearsal region's current extent
      (from `<!-- rehearsal:begin -->` above "## Run it" to `<!-- rehearsal:end -->` after the
      invitation block), the "Local development" table as 08-01 left it, and the "Pending, and what I
      would do next" section
    - tests/architecture/test_documentation_claims.py in full — every constant and every test this
      edit has to keep green
    - scripts/clean-clone-rehearsal.sh, step 5 and step 7, as plan 08-01 left them
    - .planning/phases/08-web-ui/08-01-SUMMARY.md and 08-02-SUMMARY.md — the UI's real shape, the
      real host port, and the real test count, so nothing here is a prediction
  </read_first>
  <action>
    Four edits to `README.md` and one to the gate.

    **1. One sentence in "Run it", inside the rehearsal region.** After the existing line that sends
    the reader to `<http://localhost:8000/docs>`, add a sentence naming the UI: the same
    `docker compose up` also serves a small web UI at `<http://localhost:8080>`, and **the brief asks
    for no UI — it is beyond the challenge**, described below. Mention that the first `make up` also
    builds the UI image, so it takes longer than an API-only start. Use angle-bracket autolinks, not
    backticks, so the rehearsal's path scan does not try to resolve a URL as a file.

    **2. A new section, "## The web UI (beyond the brief)", placed inside the rehearsal region after
    the simulated-invitation block and before `<!-- rehearsal:end -->`.** In prose, three short
    paragraphs and no more:

    - What it is: a React + Vite + TypeScript single-page app in `frontend/`, built to static files
      and served by nginx in its own container. It lets you do from a browser what the quickstart
      above does with `curl` — register, log in, create and rename and delete task lists, create and
      edit and delete tasks, move a task's status through the dedicated endpoint, filter by status
      and by priority, assign a task to a user, and see what is assigned to you.
    - **The honesty paragraph, and it goes first among the caveats**: the challenge brief asks for a
      backend only. This UI is outside it, added by decision after the API was finished; it is not
      offered as evidence for any brief requirement, which is why it has no row in the
      requirement-to-evidence map below. It reaches the API **same-origin** through the UI
      container's own `/api/` reverse proxy (`frontend/nginx.conf`), so the backend gained no CORS
      surface and `src/taskmanager` was not modified by this phase at all — `git log` will show that.
      Cite ADR-105, ADR-106 and ADR-107.
    - How it is held to the same standard: TypeScript `strict`, eslint, and vitest component tests
      with the API mocked at the `fetch` boundary, behind `make ui-lint`, `make ui-typecheck` and
      `make ui-test`, run as their own job in CI; exact-pinned dependencies with a committed lockfile
      installed by `npm ci`, gated by `tests/architecture/test_frontend_gates.py` (ADR-108). Give the
      real test count from `08-02-SUMMARY.md`; never a predicted one.

    Then the fenced block the rehearsal executes. Only `curl`, `grep` and shell — **no `make`
    target**, because the gate restricts the region to the four Docker-path targets and every `ui-*`
    one needs Node:

    ```bash
    UI=http://localhost:8080
    curl -sf $UI | grep -q 'id="root"'
    curl -sf "$UI/api/v1/task-lists/$LIST/tasks?priority=high" -H "$AUTH" | grep -q '"completion_percentage":50.0'
    ```

    Comment lines above each are dropped by the extractor, so write the explanation as Markdown prose
    around the block rather than as shell comments: the first line proves the SPA is being served,
    and the second proves the proxy carried a bearer header and a query string to the API and brought
    back the whole-list percentage. `-f` is what makes an HTTP error a non-zero exit, and `grep -q`
    is what makes a 200 with the wrong body a failure — a bare `curl` would pass against anything.
    `$LIST` and `$AUTH` come from the quickstart above, which shares one shell with this block.

    **3. "Pending, and what I would do next".** Add a short paragraph on the UI's own deliberate
    gaps, in the section's existing voice: no browser end-to-end tests (Playwright was considered and
    left out — the component tests mock at the `fetch` boundary, so nothing proves the real proxy
    except the rehearsal); no routing, so no deep links and no browser Back between screens
    (ADR-106); no i18n; no pagination, because the API has none (API-01); and no generated TypeScript
    client from the OpenAPI document — the types are hand-written and would be the first thing to
    replace. Keep the existing paragraphs untouched.

    **4. Do NOT extend `PHASES` here.** The `PHASES` bump in
    `tests/architecture/test_documentation_claims.py` belongs to task 2, in the same commit as the
    `AI_WORKFLOW.md` Phase 8 block that satisfies it: CLAUDE.md requires `make test` green before
    ANY commit, and bumping it here would make this task's commit red on purpose. This task touches
    that module only if a README check genuinely needs it.

    Do **not** touch the endpoint table, the evidence map, the ADR count or the contract count.
  </action>
  <verify>
    <automated>make docker-test</automated>
  </verify>
  <acceptance_criteria>
    - `make lint`, `make typecheck`, `make arch` and `make test` all exit 0 before this task's
      commit — no expected failure, no exception
    - `sh scripts/clean-clone-rehearsal.sh --extract-only` exits 0 and its output ends with the two
      new `curl` lines, in order, after the invitation block's commands
    - `grep -c 'rehearsal:begin' README.md` and `grep -c 'rehearsal:end' README.md` both print 1
    - No backticked token in the new prose names an untracked path:
      `git ls-files --error-unmatch $(grep -o '`[^`]*`' README.md | sed 's/`//g' | grep -E '^[A-Za-z0-9_.][A-Za-z0-9_./-]*$' | grep '/' | sort -u)` succeeds for every token except `.env`
    - The evidence map has exactly the same number of rows as before this task
    - `make docker-test` exits 0
  </acceptance_criteria>
  <done>The README describes the UI, says plainly that the brief did not ask for one, and hands the rehearsal two commands that can fail.</done>
</task>

<task type="auto">
  <name>Task 2: AI_WORKFLOW, CLAUDE.md, and the requirement ticks (UI-08)</name>
  <files>AI_WORKFLOW.md, CLAUDE.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - AI_WORKFLOW.md: the paragraph above `## Human-Decided vs AI-Delegated` containing the
      Node-toolchain sentence (`grep -n "wrong trade" AI_WORKFLOW.md`); the whole "What is already
      true of Phase 7" block, which is the template for the Phase 8 one; the last entry of the
      Incident Log; and the `## What I Did Not Do` section
    - CLAUDE.md's Project Rules in full — every rule there names the gate that enforces it, and the
      new ones must too
    - **Moved here from task 1 (plan-check blocker):** in the SAME commit that adds the
      "What is already true of Phase 8" block to `AI_WORKFLOW.md`, extend `PHASES` in
      `tests/architecture/test_documentation_claims.py` to `(1, 2, 3, 4, 5, 6, 7, 8)` and adjust the
      constant's comment ("the seven phases that were actually run"). Drive it red once first
      (bump `PHASES` with the block absent, observe `test_the_human_ai_account_covers_every_phase`
      fail, then add the block) WITHOUT committing the red state. Change nothing else in that module
    - DECISION_LOG.md ADR-105 through ADR-108 as plan 08-01 wrote them — this task cites them and
      must not contradict them
    - .planning/REQUIREMENTS.md: the `### Web UI (UI)` block and the traceability table near line 237
    - .planning/ROADMAP.md: the `### Phase 8: Web UI` section and the progress table near line 389
  </read_first>
  <action>
    **1. `AI_WORKFLOW.md` — amend the Node-toolchain sentence in place.** It currently says a Node
    toolchain in a Python deliverable "was judged the wrong trade". That judgement was reversed in
    Phase 8, so rewrite the paragraph to say what is now true: the diagrams are still not rendered by
    anything in this repository, and the reason is now narrower — the Node toolchain that exists here
    since Phase 8 is the UI's, and it carries no Mermaid renderer; adding one would be a build
    dependency for a picture. Name ADR-105 and say in one clause that the earlier judgement was
    reversed by the human, so a reader who remembers the old sentence learns that rather than
    wondering. Edit in place: this paragraph is outside the Incident Log, and the append-only rule
    covers the log, not the narrative.

    **2. `AI_WORKFLOW.md` — "What is already true of Phase 8".** Append a block in the exact shape of
    the Phase 7 one, inside `## Human-Decided vs AI-Delegated` and after it, with the two
    sub-headings **Decided by the human** and **Delegated to AI**. Every claim points at a commit, a
    file or a test, and **every commit hash is resolved with `git cat-file -e <sha>` before it is
    written** — that is the Phase 7 rule and it is why this file has never been a reconstruction.

    Decided by the human, at minimum:
    - That there would be a UI **inside the deliverable** at all, and that the AI's recommendation
      was the opposite. Say it in those words: the AI recommended keeping the UI out of the delivered
      repository, and the human decided to include it, reversing the "Frontend / UI" Out of Scope row
      and this document's own sentence about a Node toolchain (ADR-105). This is the most important
      line in the block — a document that logs only the decisions the AI agreed with is not the
      document this project claims to be.
    - That the UI reaches the API through the container's own reverse proxy rather than teaching the
      backend CORS, so `src/taskmanager` is untouched (D-03, ADR-107).
    - That the UI is plain: no component library, no state library, no router (D-05, ADR-106), with
      the cost — no deep links — accepted and published in the README.
    - That the token lives in memory with a `sessionStorage` mirror and never in `localStorage`
      (D-06), which is now an eslint rule rather than a habit.
    - That the frontend gets the backend's kind of gates, and that `make docker-test` keeps its
      meaning — the Python suite on 3.13 — rather than growing a Node half (D-04, ADR-108).

    Delegated to AI, at minimum: the whole frontend; the nginx configuration and the two-stage image;
    the client, the session and the screens with their vitest suite (give the real number from
    `08-02-SUMMARY.md`); the pins gate; and the falsifications — name each gate that was driven red
    once before it was trusted, in one line each, from the two SUMMARYs.

    **3. `AI_WORKFLOW.md` — one dated Phase 8 Incident Log entry**, appended at the end of the log,
    in the established shape (a `### YYYY-MM-DD — <what happened>` heading, then what happened, how
    it was caught, and what changed). Write about something that **actually happened during
    08-01 and 08-02** — read both SUMMARYs and pick the real one. Candidates, if they occurred: the
    TypeScript pin, where the registry's `latest` (7.0.2) would have silently disabled every
    type-aware lint rule because `typescript-eslint` caps at `<6.1.0` and the planner caught it by
    reading `peerDependencies` rather than by trusting `npm view typescript version`; or a
    falsification that proved the wrong thing first. **Do not invent an incident**, and do not
    reconstruct one that did not happen — if the two plans genuinely produced nothing worth an entry,
    write that the phase's honest entry is the reversal itself: a project that spent seven phases
    arguing against a Node toolchain acquired one, and the documents that argued against it had to be
    edited rather than quietly left standing.

    **4. `AI_WORKFLOW.md` — "What I Did Not Do".** Add the UI's deliberate omissions under a short
    new bolded lead-in, matching the README's Pending paragraph so the two files agree: no browser
    end-to-end tests, no routing and therefore no deep links, no i18n, no pagination (the API has
    none), no generated TypeScript client, and no UI surface for the simulated invitation because the
    API returns nothing about it (ADR-070).

    **5. `CLAUDE.md` — Project Rules.** Add a `### Frontend` subsection in the file's voice, where
    every rule names the gate that fails when it is broken. Nothing advisory:
    - The UI lives in `frontend/` and is a separate build. No Python tool reads it — black, isort,
      flake8 and mypy are invoked with the explicit paths `src tests`, and `[tool.coverage.run]
      source = ["taskmanager"]`; widening any of them is a change to a gate, not a convenience.
    - `src/taskmanager` is not modified for the UI's benefit. The UI reaches the API **same-origin**
      through `frontend/nginx.conf`'s `/api/` proxy, so there is no CORS middleware and no
      `Access-Control-Allow-*` header anywhere (ADR-107). An absolute API URL in `frontend/src` is a
      defect: the client's base is the relative `/api/v1`.
    - Every frontend dependency is an exact pin with a committed lockfile, installed with `npm ci`,
      and TypeScript runs `strict`. Enforced by `tests/architecture/test_frontend_gates.py`, which
      runs inside `pytest` and therefore inside `make docker-test` and CI — which is why the
      `Dockerfile` `test` stage copies `frontend/package.json`, `frontend/package-lock.json` and
      `frontend/tsconfig.json` (ADR-102's rule, ADR-108).
    - The access token lives in memory, mirrored to `sessionStorage` at most. `localStorage` and
      `document.cookie` are eslint errors under `frontend/src/**` (D-06), exempted only in the test
      that asserts their absence.
    - The UI invents no error text for an API refusal: it renders the RFC 9457 body's `detail`,
      falling back to `title`, and keys special handling on `code`. It may word only a failure the
      API never answered.
    - The completion percentage is rendered from the response's own counters and never recomputed
      from a filtered `items` array (ADR-009). `CompletionBar` takes the number as a prop and derives
      nothing; the test that passes inconsistent props on purpose is what keeps it that way.
    - The two-places note: `make ui-lint`, `make ui-typecheck` and `make ui-test` are new commands, so
      they exist in **both** `.pre-commit-config.yaml` and `.github/workflows/ci.yml`. The
      pre-commit side is scoped to `^frontend/` and exits 0 with an explanation when
      `frontend/node_modules` is absent — the one hook in this repository that may decline to run,
      and the reason the CI job is the hard gate (ADR-108).

    **6. `.planning/REQUIREMENTS.md`** — tick UI-01 through UI-08 (`- [x]`) and set their rows in the
    traceability table to Done. Leave the struck-through Out-of-Scope row as the user wrote it.

    **7. `.planning/PROJECT.md`** — leave the amended Out of Scope row alone; only check that it is
    still coherent with what shipped, and fix it if it is not.

    **8. `.planning/ROADMAP.md`** — mark Phase 8's three plans `[x]`, set its progress-table row to
    3/3 Complete, and tick the six Phase 8 success criteria against what actually shipped. If any
    criterion did not fully land, say so in one clause rather than ticking it.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` and `make docker-test` both exit 0 — in particular
      `test_the_human_ai_account_covers_every_phase` is now green with `PHASES` including 8, and
      `test_every_cited_adr_resolves_to_an_entry` is green for every new ADR citation
    - Every commit hash written into `AI_WORKFLOW.md` resolves:
      `for s in $(grep -oE '\bcommit [0-9a-f]{7,40}' AI_WORKFLOW.md | awk '{print $2}'); do git cat-file -e "$s" || echo "MISSING $s"; done` prints nothing
    - `grep -c '"wrong trade"' AI_WORKFLOW.md` — the old sentence is gone; `grep -n 'ADR-105'
      AI_WORKFLOW.md` finds the amended paragraph
    - The Phase 8 human/AI block contains the sentence stating that the AI recommended against the UI
      and the human decided otherwise
    - `git diff AI_WORKFLOW.md | grep -c '^-'` is greater than 1 (the narrative paragraph was edited
      in place) **and** the Incident Log gained lines only — verify by inspecting the diff hunks, not
      by a count
    - `grep -c '\- \[ \] \*\*UI-0' .planning/REQUIREMENTS.md` prints 0
    - No file under `src/taskmanager` appears in this task's diff
  </acceptance_criteria>
  <done>Every document says what is now true, including the two it had to contradict, and the reversal is attributed to the person who made it.</done>
</task>

<task type="auto">
  <name>Task 3: Rehearse the committed tree, then leave it exactly as 07-05 task 4 expects</name>
  <files>.planning/STATE.md, README.md, scripts/clean-clone-rehearsal.sh</files>
  <read_first>
    - scripts/clean-clone-rehearsal.sh in full, as plan 08-01 left it — what it stops, what it clones,
      what it asserts, and that it refuses a dirty tree
    - .planning/STATE.md — the frontmatter, `## Current Position`, the `## Accumulated Context`
      convention (one `- [Phase 08-NN]:` bullet per finding) and `## Session Continuity`
    - .planning/phases/07-documentation-delivery/07-05-SUMMARY.md if it exists, and otherwise the
      07-05 entries already in STATE.md — the exact wording of the task 4 checkpoint this plan must
      leave resumable. **Do not open 07-05-PLAN.md to edit it**; read it only if you need the
      checkpoint's wording, and change nothing in it
  </read_first>
  <action>
    **1. Commit everything, then rehearse.** `make rehearse` refuses to start on a dirty tree, so
    every edit from tasks 1 and 2 must be committed first. Then run `make rehearse`. It will stop the
    developer's compose stack and leave it down; that is expected and it prints `make up` as the
    remedy.

    Budget for it: this run now does a `--no-cache` build of three images instead of two, and the new
    one runs `npm ci` and a Vite build, so it is minutes longer than the 115-161 s that 07-05
    measured. Report the real elapsed time from the script's own step table.

    **2. Fix whatever it finds — in the document or the script, never in the assertion.** The failure
    modes this change makes likely, so they are recognised rather than debugged from scratch:
    - a path cited in backticks in the README's new UI section that is not tracked (step 5);
    - the `ui` container never reporting healthy inside the poll bound — read the tail of the
      `make up` log the script prints, and check `depends_on` and the healthcheck rather than raising
      the bound;
    - the identity assertion failing because something else on the machine publishes `:8080` — that
      is the assertion doing its job; stop the other thing;
    - the proxied `curl` returning the wrong body, which would mean the `/api/` location or the
      `proxy_pass` upstream is wrong, not that the README is wrong.

    If a fix is needed, make it, commit it, and **re-run `make rehearse` from the top**. A rehearsal
    is only a claim about the commit it cloned.

    **3. Update `.planning/STATE.md` by hand**, then commit, then **run `make rehearse` one final
    time** so the last commit of the phase is the one that was rehearsed. That second run is the
    point: 07-05's task 4 asks a human to approve publishing a tree, and "green two commits ago" is
    not the same claim. Record both runs' timings in the SUMMARY.

    What STATE.md must say after the final edit:
    - frontmatter: `status: executing`; `stopped_at` unchanged in substance — still 07-05 task 4's
      blocking push checkpoint — but with Phase 8 named as complete ahead of it; `last_activity`
      rewritten for Phase 8; `progress.total_phases: 8`, `completed_phases: 7` — and because that number reads as
      "phases 1-7 are done", `## Current Position` must say in one explicit sentence that Phase 8
      finished OUT OF ORDER while Phase 7 is still open (07-05 tasks 4-7: push approval, push + CI,
      badge/diagram check, email), i.e. the seven complete phases are 1-6 and 8;
      `total_plans: 68`, `completed_plans: 67`, and the percent recomputed from those two numbers
      rather than restated.
    - `## Current Position`: Phase 8 complete, three of three plans executed; the UI is served at
      `http://localhost:8080` by `docker compose up`; `make rehearse` green on commit `<sha>`, naming
      the sha; and one sentence saying the next action is **07-05 task 4**, the blocking human
      checkpoint approving the push, with tasks 5-7 after it.
    - `## Accumulated Context`: one `- [Phase 08-01]:` / `- [Phase 08-02]:` / `- [Phase 08-03]:`
      bullet per real finding, in the file's established voice. At least these, if they held: that
      TypeScript's registry `latest` was the wrong pin and why; that the SPA fallback cannot swallow
      `/api/` and that this was asserted with a `curl` requiring 401 rather than reasoned about; that
      the completion bar takes the number as a prop and the test that passes inconsistent props on
      purpose is what keeps it that way; that the pre-commit hook may decline to run and where the
      hard gate therefore is; and that the planning files were hand-edited again — the tenth
      consecutive plan to record the same handler regression.
    - `## Session Continuity`: stopped at "Completed 08-03-PLAN.md (Phase 8 documents and the
      rehearsal)"; resume file `.planning/phases/07-documentation-delivery/07-05-PLAN.md` at task 4.

    **4. Leave nothing half-done.** At the end: `git status --porcelain` is empty, the developer stack
    is down (the rehearsal left it that way — do not bring it back up just to look tidy; the script
    prints the one command that restores it), nothing has been pushed, and `07-05-PLAN.md` is
    byte-identical to how this phase found it (`git diff HEAD~N --
    .planning/phases/07-documentation-delivery/07-05-PLAN.md` is empty).

    **One number 07-05 will be wrong about, and this plan must flag rather than fix.** 07-05's task 4
    says "289+ commits". Phase 8 adds more. Do not edit that file; instead write one line in the
    SUMMARY and one in STATE.md saying that the executor resuming 07-05 task 4 must state the
    **current** `git rev-list --count origin/main..HEAD` (or the full count, if there is no upstream
    yet) rather than the number written in the plan.
  </action>
  <verify>
    <automated>make rehearse</automated>
  </verify>
  <acceptance_criteria>
    - `make rehearse` exits 0 on the **final** commit of the phase, and its trace shows the two new
      UI `curl` lines executing against the clone's own `ui` container
    - The rehearsal's step table and total are recorded in the SUMMARY as measured, not estimated
    - `make lint`, `make typecheck`, `make arch`, `make test` and `make docker-test` are all green on
      that same final commit, and so are `make ui-lint`, `make ui-typecheck` and `make ui-test`
    - `git status --porcelain` is empty
    - `git diff <phase-base-sha>..HEAD -- src/taskmanager` is empty: the backend was not modified by
      any of Phase 8's three plans
    - `git diff <phase-base-sha>..HEAD -- .planning/phases/07-documentation-delivery/` is empty
    - `git log --oneline origin/main..HEAD | wc -l` (or the equivalent if no upstream is configured)
      is recorded in the SUMMARY, so 07-05's stale "289+" is replaceable with a real number without
      anybody editing that plan
    - Nothing was pushed: `git status -sb` shows no push, and no `git push` appears in this plan's
      shell history
  </acceptance_criteria>
  <done>The README was executed in a fresh clone and the UI answered; the repository is clean, green, unpushed, and 07-05 can resume at its task 4 checkpoint with nothing of Phase 8 outstanding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> evaluator | The README is the only thing most readers will trust; a false sentence in it costs the credibility of every true one |
| developer machine -> rehearsal | The script stops and clones real things and must not destroy the developer's data |
| local history -> public GitHub | Still unpushed; 07-05 owns that boundary and this plan must not cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Repudiation | The README implying the UI satisfies a brief requirement | mitigate | The UI section states the brief asks for no UI, and the requirement-to-evidence map gains no row — the rehearsal's `[PDF]`-key check is over `[PDF]` keys only, and the UI requirements are `[U]` |
| T-08-12 | Tampering | The rehearsal's UI assertions passing against a foreign process on `:8080` | mitigate | `rehearsal_assert_the_ui_is_ours` (08-01 task 3) compares the publishing container id with `docker compose ps -q ui` before any UI assertion runs |
| T-08-13 | Denial of service | The rehearsal destroying the developer's database volume | mitigate | Inherited unchanged: a plain `down` on the dev project, and every `down -v` inside the clone under `COMPOSE_PROJECT_NAME=crehana-rehearsal`. This plan changes none of that machinery |
| T-08-14 | Information disclosure | Pushing before 07-05's task 4 approval | mitigate | No task in this plan pushes; the acceptance criteria assert it, and 07-05's blocking checkpoint is left intact and untouched |
| T-08-15 | Repudiation | A commit hash in `AI_WORKFLOW.md` that does not resolve | mitigate | Every hash is resolved with `git cat-file -e` before it is written, and the acceptance criteria re-check them all in a loop |
</threat_model>

<verification>
- `make rehearse` green on the phase's final commit, with its step table recorded as measured.
- `make lint`, `make typecheck`, `make arch`, `make test`, `make docker-test`, `make ui-lint`,
  `make ui-typecheck`, `make ui-test` all green on that commit.
- Every ADR id cited in README.md and AI_WORKFLOW.md resolves; the quoted ADR and contract counts are
  the files' counts; the endpoint table is unchanged.
- `git status --porcelain` empty; nothing pushed; `07-05-PLAN.md` untouched.
</verification>

<success_criteria>
UI-08 is satisfied: the README, the rehearsal, the documentation gate, `DECISION_LOG.md`,
`AI_WORKFLOW.md` and `CLAUDE.md` all describe a repository that now contains a web UI, say plainly
that the brief asked for none, and name by id the two positions that were reversed — including this
project's own written objection to a Node toolchain, and who overruled it. 07-05 resumes at task 4
against a clean, green, rehearsed, unpushed tree.
</success_criteria>

<output>
Create `.planning/phases/08-web-ui/08-03-SUMMARY.md` when done.
</output>
