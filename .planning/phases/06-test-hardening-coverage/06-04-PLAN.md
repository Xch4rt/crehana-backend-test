---
phase: 06-test-hardening-coverage
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - tests/architecture/test_coverage_configuration.py
  - tests/problem_details.py
  - tests/unit/presentation/test_error_contract.py
  - tests/api/
  - tests/conftest.py
  - tests/integration/conftest.py
  - tests/integration/api/test_task_lists.py
  - tests/integration/api/test_assignment.py
  - tests/integration/api/test_auth.py
  - tests/integration/api/test_permission_matrix.py
  - tests/integration/api/test_tasks.py
  - tests/integration/api/test_users.py
  - Makefile
  - DECISION_LOG.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "Lowering the coverage threshold below 75, adding an omit entry, changing the coverage source away from taskmanager, adding a fifth exclude_also entry or adding a pragma under src/ fails a pytest run (D-11)"
    - "The 75 threshold, the no-pragma rule and tests being outside the denominator are carried forward unchanged, not reopened (D-12)"
    - "Every collected test carries the unit or the integration marker, and a test added without one fails collection (D-13)"
    - "make test-unit runs the no-database slice without a running PostgreSQL and without overwriting coverage.xml (D-13)"
    - "tests/api/ is gone, the error-contract tests live with their presentation siblings, and MEMBERS and PROBLEM_JSON are imported from a module that is never collected (D-13)"
    - "The host and Docker coverage totals are recorded and agree; the CI leg is the one manual verification, taken at phase verification (D-11)"
    - "Every new gate this phase added has an ADR from ADR-085 onward and a CLAUDE.md Project Rules bullet (D-01)"
    - "TEST-01..TEST-05 are ticked once, here, each against a named passing test (D-15)"
  artifacts:
    - path: "tests/architecture/test_coverage_configuration.py"
      provides: "The coverage configuration pin over pytest.ini and pyproject.toml"
      contains: "EXPECTED_EXCLUDE_ALSO"
    - path: "tests/problem_details.py"
      provides: "MEMBERS and PROBLEM_JSON, importable and never collected"
    - path: "tests/unit/presentation/test_error_contract.py"
      provides: "The error-contract tests in their right home"
    - path: "tests/conftest.py"
      provides: "The unmarked-item collection guard"
      contains: "pytest_collection_modifyitems"
    - path: "Makefile"
      provides: "The test-unit target"
      contains: "test-unit"
  key_links:
    - from: "tests/integration/api/test_task_lists.py"
      to: "tests/problem_details.py"
      via: "from tests.problem_details import MEMBERS, PROBLEM_JSON"
      pattern: "tests\\.problem_details"
    - from: "tests/conftest.py"
      to: "pytest.ini markers"
      via: "collection guard rejecting an item carrying neither marker"
      pattern: "pytest_collection_modifyitems"
---

<objective>
Pin the coverage configuration, tidy the suite's shape, and close the phase.

Purpose: TEST-03 and roadmap SC-5, plus the requirement ticks for all five TEST requirements. The
coverage rule already exists in CLAUDE.md as prose; this plan turns it into a gate. The tidy is
deliberately light — a move, a marker line per module and two Makefile targets. No deletions, no
rewrites: nothing in the brief rewards a smaller suite.

Output: the configuration pin, `tests/problem_details.py` and the error-contract move, markers
everywhere plus the guard that keeps them, `make test-unit`, the ADRs and CLAUDE.md bullets for
every gate this phase added, and the five requirement ticks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-test-hardening-coverage/06-CONTEXT.md
@.planning/phases/06-test-hardening-coverage/06-RESEARCH.md
@.planning/phases/06-test-hardening-coverage/06-PATTERNS.md
@.planning/phases/06-test-hardening-coverage/06-VALIDATION.md
@.planning/phases/06-test-hardening-coverage/06-01-SUMMARY.md
@.planning/phases/06-test-hardening-coverage/06-02-SUMMARY.md
@.planning/phases/06-test-hardening-coverage/06-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Measured facts this plan is built on — take them as given, do not re-derive:

- Values to pin, all true today: `pytest.ini` addopts carry `--cov=taskmanager`,
  `--cov-report=term-missing`, `--cov-report=xml`, `--cov-fail-under=75`, `--strict-markers`,
  `--strict-config`; `filterwarnings = error`; `markers = unit, integration`.
  `[tool.coverage.run] source == ["taskmanager"]`, `branch is true`, **no** `omit` key;
  `[tool.coverage.report]` has **no** `fail_under` key (the threshold has exactly one home) and
  `exclude_also` is exactly four entries: `if TYPE_CHECKING:`, `raise NotImplementedError`,
  `@abstractmethod`, `\.\.\.`. `# pragma: no cover` occurs **0** times under `src/`.
- Parse `pytest.ini` with `configparser` and `pyproject.toml` with `tomllib` (both stdlib).
- 58 collected test modules carry no marker today (all of `tests/unit/`, all of
  `tests/architecture/`, `tests/api/test_error_contract.py`), plus every module this phase added.
  315 tests carry `integration` via a module-level `pytestmark`; `pytest -m unit` currently
  deselects all 1019.
- `pytest -m "not integration" --no-cov` runs 704 tests in ~1.4 s with no database and no
  warnings. It reaches 91.43 % coverage — so D-13's premise that a unit-only run cannot clear 75 %
  is false — but `--no-cov` is still the right body for `make test-unit`, because `pytest.ini`
  also writes `coverage.xml` and a partial run must not overwrite the artifact `make test` and CI
  produce.
- `tests/api/test_error_contract.py` holds 17 tests driven through `tests/probe.py`'s throwaway
  router and the root `tests/conftest.py`'s `client` / `tolerant_client` fixtures. It touches no
  database. It also exports `MEMBERS` and `PROBLEM_JSON`, imported by six modules:
  `tests/integration/api/{test_task_lists,test_assignment,test_auth,test_permission_matrix,test_tasks,test_users}.py`.
  Each import site carries a comment naming the old home — the comment text must be updated too,
  not only the import line.
- `tests/integration/conftest.py`'s `alembic_config` docstring names
  `tests/api/test_error_contract.py` by path and goes stale on the move.
- `tests/probe.py` and the new `tests/problem_details.py` are never collected and take no marker.
- Next free ADR number is **ADR-085** (`DECISION_LOG.md` ends at ADR-084).
- The CI leg of the three-way agreement is the phase's one manual verification
  (`06-VALIDATION.md` §"Manual-Only Verifications"); host and Docker are recorded here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The coverage configuration pin (D-11, D-12, TEST-03)</name>
  <files>tests/architecture/test_coverage_configuration.py</files>
  <read_first>
    - tests/architecture/test_layer_boundaries.py (the exact-set pin with its cost stated in the
      docstring — `EXPECTED_CONTRACT_NAMES` — which is the pattern for pinning `exclude_also`)
    - tests/architecture/test_no_commit_in_repositories.py (the root-relative `Final[Path]`
      constant with the `parents[2]` comment, and the text-scan offender list with `file:line`)
    - pytest.ini and pyproject.toml (the values under audit)
    - CLAUDE.md §"Quality gates" (the coverage rule this gate turns from prose into a test)
    - .planning/phases/06-test-hardening-coverage/06-RESEARCH.md §"The configuration pin (D-11)"
  </read_first>
  <action>
    Create `tests/architecture/test_coverage_configuration.py`, turning CLAUDE.md's coverage rule
    into a gate (D-11, carrying D-12 forward unchanged).

    Read `pytest.ini` with `configparser` and `pyproject.toml` with `tomllib`, and assert: the
    addopts carry `--cov=taskmanager`; they carry `--cov-fail-under=N` with `N >= 75` (parse the
    number, do not string-match the literal, so a raise is allowed and a drop is not);
    `[tool.coverage.run] source == ["taskmanager"]`; `branch` is true; `[tool.coverage.run]` has no
    `omit` key; `[tool.coverage.report]` has no `fail_under` key, so the threshold keeps exactly
    one home; and `exclude_also` equals `EXPECTED_EXCLUDE_ALSO`, an exact four-entry list — pinned
    by exact list and not by length, because a fifth entry is how a real exclusion would be
    smuggled in. Add a text scan over `src/taskmanager/**/*.py` reporting `file:line` for every
    `# pragma: no cover` and asserting the offender list is empty (0 today, so this is a hard
    literal and not a ceiling). Assert non-vacuity: both config files parsed and the `src/` glob
    matched files.

    Docstring in the house style, with the cost of the exact-set comparison stated as
    `test_layer_boundaries.py` states its own, a `Deliberately NOT used:` paragraph for asserting
    the coverage *number* (100 % is a norm, not a requirement — D-12 — and a test that pinned it
    would fail on an honest refactor), and the no-extra-gate paragraph.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/architecture/test_coverage_configuration.py --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/architecture/test_coverage_configuration.py --no-cov` exits 0
    - Temporarily setting `--cov-fail-under=70` in `pytest.ini` makes it red; temporarily setting
      `--cov-fail-under=80` leaves it green — revert to 75
    - Temporarily adding an `omit` entry under `[tool.coverage.run]` makes it red — revert
    - Temporarily adding a fifth `exclude_also` entry makes it red — revert
    - Temporarily adding `# pragma: no cover` to a line under `src/` makes it red naming that
      `file:line` — revert and confirm `git status --porcelain -- src/` is empty
    - `.venv/bin/mypy src tests` and `.venv/bin/flake8 src tests` exit 0
  </acceptance_criteria>
  <done>Every configuration fact the coverage number depends on is asserted, and each falsification above was observed.</done>
</task>

<task type="auto">
  <name>Task 2: Fold in the stray tests/api/ (D-13)</name>
  <files>tests/problem_details.py, tests/unit/presentation/test_error_contract.py, tests/api/, tests/integration/api/test_task_lists.py, tests/integration/api/test_assignment.py, tests/integration/api/test_auth.py, tests/integration/api/test_permission_matrix.py, tests/integration/api/test_tasks.py, tests/integration/api/test_users.py, tests/integration/conftest.py</files>
  <read_first>
    - tests/api/test_error_contract.py (in full: the `MEMBERS` and `PROBLEM_JSON` definitions, the
      fixtures it uses and where they come from)
    - tests/probe.py (the "lives under tests/, importable, never collected" precedent and the
      docstring that justifies the location — `tests/problem_details.py` owes the same)
    - tests/conftest.py (the `client` and `tolerant_client` fixtures the moved module uses, and
      that they are visible from `tests/unit/presentation/`)
    - tests/unit/presentation/test_actor.py or test_health.py (the siblings the moved module joins)
    - tests/integration/api/test_task_lists.py lines around the `from tests.api.test_error_contract
      import MEMBERS, PROBLEM_JSON` line (the comment every import site carries)
    - tests/integration/conftest.py (`alembic_config`'s docstring, which names the old path)
  </read_first>
  <action>
    Two steps, in this order.

    First, create `tests/problem_details.py` holding `PROBLEM_JSON` and `MEMBERS`, with a
    `probe.py`-style docstring saying why it lives under `tests/`, that it is never collected, and
    that it exists so the error contract has exactly one statement of itself. Rewrite the six
    import sites to `from tests.problem_details import MEMBERS, PROBLEM_JSON`, updating the comment
    at each site to name the new home — the comment's argument (a second copy is the one that
    quietly disagrees; a rename over there breaks this import loudly) stays, only the path changes.

    Second, move `tests/api/test_error_contract.py` to
    `tests/unit/presentation/test_error_contract.py` with `git mv` so the history follows, update
    its own import of the two constants, and delete `tests/api/` including its `__init__.py`.
    Update the `alembic_config` docstring in `tests/integration/conftest.py`, which names the old
    path, in the same commit or the prose goes stale.

    Nothing is deleted or rewritten beyond the move itself (D-13): all 17 tests survive unchanged.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/presentation/test_error_contract.py tests/integration/api --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/presentation/test_error_contract.py tests/integration/api --no-cov` exits 0
    - `.venv/bin/pytest` exits 0 and the collected total is unchanged from the previous plan's total
    - `test -d tests/api` is false
    - `grep -rn "tests.api.test_error_contract\|tests/api/test_error_contract" tests/ | grep -v '^Binary'` returns nothing
    - `.venv/bin/pytest --collect-only -q | grep -c problem_details` is 0 (the new module is never collected)
  </acceptance_criteria>
  <done>The error contract is stated in one non-collected module, its tests live with their siblings, and tests/api/ is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Markers everywhere, the collection guard, and make test-unit (D-13)</name>
  <files>tests/conftest.py, Makefile, tests/unit/, tests/architecture/</files>
  <read_first>
    - tests/integration/api/test_task_lists.py (the module-level `pytestmark = pytest.mark.integration`
      line and where in the module it sits: after the constants, before the first helper)
    - tests/conftest.py (in full — the guard goes here, and the file's existing imports and
      fixtures set the style)
    - pytest.ini (`markers =` already registers `unit` and `integration`; `--strict-markers` is on)
    - Makefile (the `test` target and the header's three file-level rules; the `.PHONY` line)
    - .planning/phases/06-test-hardening-coverage/06-RESEARCH.md §"Markers (D-13)" and
      §"make test-unit and the coverage gate"
    - .planning/phases/06-test-hardening-coverage/06-PATTERNS.md §"No Analog Found" (there is no
      `pytest_collection_modifyitems` precedent for the guard; report offending node ids, not a count)
  </read_first>
  <action>
    Make the marker partition total, then defend it.

    Add `pytestmark = pytest.mark.unit` as a module-level line to every collected test module that
    carries no marker — all of `tests/unit/`, all of `tests/architecture/`, the module moved in
    task 2, and every module this phase added (`test_use_case_totality.py`,
    `test_error_contract_totality.py`, `test_assertion_quality.py`,
    `test_coverage_configuration.py`, `test_break_check.py`). Place it exactly where the
    integration modules place theirs. `tests/integration/test_endpoint_totality.py` takes
    `integration`, like its neighbours. `tests/probe.py` and `tests/problem_details.py` are never
    collected and take no marker. Do not change any test body.

    Add `pytest_collection_modifyitems(config: pytest.Config, items: list[pytest.Item]) -> None` to
    `tests/conftest.py` that FAILS the run when a collected item carries neither `unit` nor
    `integration`, reporting the offending node ids — not a count — via
    `pytest.UsageError` or an equivalent hard failure at collection time. Annotate it explicitly;
    `mypy --strict` covers `tests/`. Comment it with the rule it defends (D-13) and why it is
    itself the gate, so no CI step is added.

    Add a `test-unit` target to the `Makefile` running `$(VENV)/bin/pytest -m unit --no-cov`, add
    it to `.PHONY`, and give it the comment block the file's convention requires: the fast host
    loop, no database, and `--no-cov` rather than a threshold override because `pytest.ini` also
    writes `coverage.xml` and a partial run must not overwrite the artifact `make test` and CI
    produce. Do not touch the `test` target, and do not add the marker split to CI.
  </action>
  <verify>
    <automated>make test-unit</automated>
  </verify>
  <acceptance_criteria>
    - `make test-unit` exits 0
    - With the database stopped (`docker compose stop db`), `make test-unit` still exits 0; restart
      it afterwards with `docker compose start db`
    - `.venv/bin/pytest -m unit --no-cov -q` and `.venv/bin/pytest -m integration --no-cov -q`
      collected counts sum to the total reported by `.venv/bin/pytest --collect-only -q`
    - Temporarily removing the `pytestmark` line from one module makes any pytest run fail at
      collection naming that module's node ids — revert
    - `git status --porcelain coverage.xml` shows no change after `make test-unit`
    - `make lint && make typecheck && make test` exits 0
  </acceptance_criteria>
  <done>Every collected test carries exactly one of the two markers, a new unmarked test cannot be added silently, and the fast loop has a target.</done>
</task>

<task type="auto">
  <name>Task 4: Document the gates, record the agreement, tick the requirements, close the phase</name>
  <files>DECISION_LOG.md, CLAUDE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/06-test-hardening-coverage/06-04-SUMMARY.md</files>
  <read_first>
    - DECISION_LOG.md (ADR-084 in full: the `## ADR-0NN: <lowercase statement of the decision>`
      heading, then `**Context**`, `**Options**` bulleted with the rejection reason inline,
      `**Decision**`, `**Consequences**` bulleted with a bold opening clause)
    - CLAUDE.md §"Project Rules" (the existing bullets under Quality gates, Error handling and
      Persistence — a new bullet names the gate, the file that enforces it and what fails)
    - .planning/REQUIREMENTS.md (the TEST-01..TEST-05 checkboxes and the traceability table rows)
    - .planning/ROADMAP.md §"Phase 6" (the `**Plans**: TBD` line and the success criteria)
    - .planning/STATE.md
    - .planning/phases/06-test-hardening-coverage/06-VALIDATION.md §"Manual-Only Verifications"
    - The three prior SUMMARYs for this phase (each names its ADR-worthy rules)
  </read_first>
  <action>
    Close the phase. Four pieces, all mechanical.

    (a) Write one ADR per new rule this phase introduced, numbered from ADR-085 in the order the
    gates landed: use-case totality, endpoint totality via the run-wide recorder, error-leaf
    totality, assertion quality with the `no_reread` exemption, the coverage configuration pin, the
    marker partition with its collection guard, and the break-check script. Each in the ADR-084
    format, each stating the weaker alternative and why it was rejected. Add a matching
    `## Project Rules` bullet to `CLAUDE.md` for each — naming the gate, the file that enforces it
    and what makes it red — and state once, in the Quality gates section, that none of these gates
    adds a pre-commit hook or a CI step because they ride inside pytest, and that
    `make break-check` is deliberately in neither.

    (b) Record the three-way agreement (D-11): run `make test` and `make docker-test` and record
    both coverage totals and pass counts in the SUMMARY, against the 1019-passed / 100 % baseline
    measured at `cfa6f8d`. The CI leg is the phase's one manual verification — do not push for it;
    note in the SUMMARY that it is taken at phase verification per `06-VALIDATION.md`.

    (c) Tick TEST-01..TEST-05 in `.planning/REQUIREMENTS.md` — the checkboxes and the traceability
    table rows — each against a named passing test or command: TEST-01
    `tests/architecture/test_use_case_totality.py`; TEST-02
    `tests/integration/test_endpoint_totality.py`; TEST-03
    `tests/architecture/test_coverage_configuration.py` plus the `make test` total; TEST-04 the
    transition complement in `tests/integration/api/test_tasks.py`, the seeded auth modes in
    `tests/integration/api/test_auth.py`, `tests/architecture/test_error_contract_totality.py` and
    `tests/integration/api/test_permission_matrix.py`; TEST-05
    `tests/architecture/test_assertion_quality.py` plus `make break-check`. This is the only place
    in the phase where ticks are taken.

    (d) Update the `**Plans**` line and plan list for Phase 6 in `.planning/ROADMAP.md`. Leave the
    phase checkbox, the progress-table status cell and the completion date alone — they belong to
    the orchestrator after verification. Then update `.planning/STATE.md`, and after any `gsd-sdk`
    state handler runs, `git diff .planning/STATE.md` and repair regressions by hand: the handlers
    are known to reset the percent, inject blank lines and miscount totals.

    Commit with no AI or Claude co-author trailer of any kind (CLAUDE.md §"Language and
    attribution"), message in English.
  </action>
  <verify>
    <automated>make lint && make typecheck && make arch && make test && make test-unit && make break-check</automated>
  </verify>
  <acceptance_criteria>
    - `make lint && make typecheck && make arch && make test && make test-unit && make break-check` exits 0
    - `make docker-test` exits 0 and its coverage total equals the host total from `make test`
    - `grep -c "^## ADR-0" DECISION_LOG.md` increased by the number of ADRs written, and the
      numbering is contiguous from ADR-085 with no gap or duplicate
    - `grep -n "TEST-0" .planning/REQUIREMENTS.md` shows all five as `[x]` and their traceability
      rows no longer say Pending
    - `git status --porcelain -- src/` is empty
    - `git log -1 --format=%B` contains no `Co-Authored-By` or `Generated with` line
    - `git diff .planning/STATE.md` shows only intended edits
  </acceptance_criteria>
  <done>Every gate is documented in both places, host and Docker agree, the five requirements are ticked against named tests, and the phase is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configuration → coverage number | `pytest.ini` and `pyproject.toml` decide what the 75 % is measured over |
| collection → suite | A test file that is collected under no marker is invisible to `-m` selections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-41 | Tampering | The coverage threshold and its denominator | mitigate | Task 1 pins the threshold floor, the `source`, the absence of `omit` and of a second `fail_under` home, the exact four `exclude_also` entries, and zero `# pragma: no cover` under `src/` — each falsified once before being trusted |
| T-06-42 | Repudiation | A partial run overwriting `coverage.xml` | mitigate | `make test-unit` uses `--no-cov`, so the artifact the three-way agreement rests on is produced only by a full run; asserted by the `git status --porcelain coverage.xml` criterion |
| T-06-43 | Repudiation | A test added under no marker, silently outside `-m unit` and `-m integration` | mitigate | The collection guard in `tests/conftest.py` fails the run and reports the offending node ids |
| T-06-SC | Tampering | package-manager installs | accept | No dependency is added by this phase (D-08); no install task exists |
</threat_model>

<verification>
- `make lint && make typecheck && make arch && make test && make test-unit && make break-check` green.
- `make docker-test` green and agreeing with the host coverage total.
- `.venv/bin/pytest -m unit --no-cov` green with the database stopped.
- All five TEST requirements ticked against named passing tests.
</verification>

<success_criteria>
The coverage number is defended by a gate rather than by prose, every collected test is in exactly
one of the two marker buckets, the suite has one home for its error-contract constants, and every
rule this phase introduced is written down in `DECISION_LOG.md` and `CLAUDE.md`.
</success_criteria>

<output>
Create `.planning/phases/06-test-hardening-coverage/06-04-SUMMARY.md` when done
</output>
