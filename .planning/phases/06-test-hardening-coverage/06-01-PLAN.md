---
phase: 06-test-hardening-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/test_use_case_totality.py
  - tests/architecture/test_error_contract_totality.py
  - tests/integration/conftest.py
  - tests/integration/test_endpoint_totality.py
  - tests/integration/api/test_tasks.py
  - tests/integration/api/test_auth.py
  - tests/integration/test_concurrent_writes.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]

must_haves:
  truths:
    - "A use case added under src/taskmanager/application/use_cases/ without a fakes-based unit test fails a pytest run (D-01, D-02)"
    - "An operation published in app.openapi() that no integration test requested fails a full pytest run, and a focused run (-k, a single file, -m) is not falsely red (D-01, D-03)"
    - "Every status pair outside ALLOWED_TRANSITIONS is driven over HTTP, self-pairs asserting the deliberate 200 no-op and cross-pairs the 409 invalid_status_transition problem body, and a fourth status would add its cases without an edit (D-04)"
    - "Every DomainError leaf actually raised under src/ has its RFC 9457 code asserted by some test, and a tenth leaf added later fails the gate (D-04)"
    - "Each of the six non-unknown-subject authentication failure modes is refused by the mechanism it names rather than by the unknown-subject branch, so disabling token-expiry enforcement turns an HTTP test red (D-14)"
    - "The lock DeleteTaskList holds is proven against two real PostgreSQL connections, not only against the fakes (D-14)"
    - "The plan closes with minimal ceremony and no evidence or transcript file (D-15)"
  artifacts:
    - path: "tests/architecture/test_use_case_totality.py"
      provides: "D-02 use-case totality gate with a REQUIRED_USE_CASE_SYMBOLS non-vacuity guard"
      contains: "REQUIRED_USE_CASE_SYMBOLS"
    - path: "tests/architecture/test_error_contract_totality.py"
      provides: "Every raisable DomainError code is asserted somewhere under tests/"
      contains: "REQUIRED_RAISED_CODES"
    - path: "tests/integration/test_endpoint_totality.py"
      provides: "Two-sided endpoint totality check over the run-wide recorder"
    - path: "tests/integration/conftest.py"
      provides: "ASGI recording wrapper on api_client and authenticated_client, plus the ordering hook"
      contains: "pytest_collection_modifyitems"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "tests/integration/test_endpoint_totality.py"
      via: "module-level REQUESTED set populated by the ASGI wrapper"
      pattern: "REQUESTED"
    - from: "tests/integration/api/test_tasks.py"
      to: "src/taskmanager/domain/value_objects/task_status.py"
      via: "complement derived from ALLOWED_TRANSITIONS"
      pattern: "ALLOWED_TRANSITIONS"
    - from: "tests/integration/api/test_auth.py"
      to: "tests/integration/conftest.py"
      via: "seed(session_factory, users=[...]) before the unauthenticated cases"
      pattern: "seed\\("
---

<objective>
Plant the four totality gates the phase promises, and fix the two tests the research proved
pass for the wrong reason.

Purpose: "every use case / every endpoint / every invalid transition / every auth failure mode"
becomes a build failure rather than a claim (D-01). Three of the four gates go green on arrival
because the suite is already total; the fourth exposes the phase's headline defect — with
`verify_exp: False` planted in `tokens.py`, the seven HTTP auth-failure cases stay green because
none of them seeds the caller's `users` row.

Output: two new architecture gates, one new integration gate plus its recorder, a derived
transition complement over HTTP, and two D-14 fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-hardening-coverage/06-CONTEXT.md
@.planning/phases/06-test-hardening-coverage/06-RESEARCH.md
@.planning/phases/06-test-hardening-coverage/06-PATTERNS.md
@CLAUDE.md

<interfaces>
Measured facts this plan is built on — take them as given, do not re-derive:

- 20 use-case symbols under `src/taskmanager/application/use_cases/`: `visible_task_list`,
  `visible_task`, `owned_task` (functions in `access.py`); `AuthenticateActor`, `Login`,
  `GetProfile`, `RegisterUser`; `CreateTaskList`, `DeleteTaskList`, `GetTaskList`,
  `ListTaskLists`, `UpdateTaskList`; `AssignTask`, `UnassignTask`, `ChangeTaskStatus`,
  `CreateTask`, `DeleteTask`, `GetTask`, `ListTasks`, `ListAssignedTasks`, `UpdateTask`;
  `ListUsers`. All 20 are already imported, constructed and exercised by modules under
  `tests/unit/application/` that also import `fakes`.
- 19 operations in `app.openapi()`, all 19 already driven by
  `tests/integration/api/test_permission_matrix.py`'s `MATRIX`.
- On FastAPI 0.141.1 / Starlette 1.6.0, `scope["route"].path_format` is router-local
  (`/task-lists/{list_id}`) and `scope["root_path"]` is `""` — the `/api/v1` prefix is in
  neither. `app.routes` holds `_IncludedRouter` objects (ADR-057).
- The complement of `ALLOWED_TRANSITIONS` is 4 pairs today: `(pending, pending)`,
  `(in_progress, in_progress)`, `(completed, completed)`, `(completed, pending)`.
- 9 `DomainError` leaves are actually raised under `src/`: `validation_error`,
  `invalid_status_transition`, `task_not_found`, `task_list_not_found`, `user_not_found`,
  `duplicate_task_list_name`, `email_already_registered`, `authentication_failed`,
  `authorization_failed`. `code` is a `ClassVar[str]` on every class in
  `src/taskmanager/domain/exceptions.py`.
- `make typecheck` runs `mypy src tests` under `strict = true`, and `pytest.ini` sets
  `filterwarnings = error`. Every new module, hook and helper is inside both scopes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use-case totality gate (D-02, TEST-01)</name>
  <files>tests/architecture/test_use_case_totality.py</files>
  <read_first>
    - tests/architecture/test_routers_raise_no_http_exception.py (the house AST-gate style: module
      docstring sections, `Final` path constants with the `parents[2]` comment, `_parsed`,
      `_location`, `REQUIRED_SCANNED_MODULES` non-vacuity guard, `assert offenders == []` with a
      prose message, and the planted-snippet self-tests)
    - tests/architecture/test_no_commit_in_repositories.py (the simpler shape and its companion
      "the package is actually scanned" test)
    - src/taskmanager/application/use_cases/access.py and one class-based use case such as
      src/taskmanager/application/use_cases/task_lists/create.py (the two symbol shapes the scan
      must find: module-level `async def` and module-level `class`)
    - tests/unit/application/test_access.py and tests/unit/application/test_create_task_list.py
      (how a test module imports a use case and the fakes, and the three call shapes in use)
    - .planning/phases/06-test-hardening-coverage/06-RESEARCH.md §"D-02" (the four-step gate shape
      and the limit that must be written into the docstring)
  </read_first>
  <action>
    Create `tests/architecture/test_use_case_totality.py`, an AST gate proving every use-case
    symbol is reachable from the fakes-based unit suite (D-01, D-02, TEST-01).

    Discovery: walk `src/taskmanager/application/use_cases/**/*.py` from a `Final[Path]` constant
    resolved with the same `parents[2]` hop and comment the sibling gates use; collect every
    module-level `class` or `async def` whose name does not start with `_` as the expected symbol
    set. Do not import the use-case packages — parse them, as the sibling gates do.

    Coverage rule: walk `tests/unit/application/test_*.py`; a symbol is covered iff some single
    module (a) imports it via an `ImportFrom` rooted at `taskmanager.application.use_cases`,
    (b) also imports `fakes`, and (c) constructs or calls it — an `ast.Call` whose func resolves
    to the bound local name, so an aliased import counts under the name it binds. Report
    `symbol -> file:line` for every uncovered symbol via the `_location` helper, and assert
    `offenders == []` with a message naming the rule, D-02 and the offender list.

    Non-vacuity: a `REQUIRED_USE_CASE_SYMBOLS: Final[frozenset[str]]` naming all 20 symbols listed
    in `<interfaces>`, plus a companion test asserting the discovered set is non-empty and that
    `REQUIRED_USE_CASE_SYMBOLS <= discovered` — the `REQUIRED_SCANNED_MODULES` idiom, applied to
    symbols, so a renamed or emptied package fails instead of passing on an empty scan. Add a
    `REQUIRED_SCANNED_TEST_MODULES`-style assertion that the test-side glob matched too.

    Self-tests: copy the planted-snippet pattern — a helper that parses a source string and
    returns the offenders — and prove at least that an import without a construction is an
    offender, that an import plus a construction without the fakes import is an offender, and
    that an aliased import plus a construction is covered.

    Docstring: the three fixed sections the house style uses. State the known limit honestly —
    an import plus a construction proves *reachability from the fakes-based suite*, not an
    exercise; `test_write_paths_hold_what_they_change.py` alone imports and constructs 13 of the
    20, and requiring `await instance.execute(...)` would be brittle across the three call shapes,
    so the coverage gate is what proves execution. Include a `Deliberately NOT used:` paragraph
    for the hand-written `{module: test_module}` map (it is the registry D-02 exists to avoid:
    `tasks/assign.py` is covered by two test modules and `auth/register.py` by
    `test_register_user.py`). Close with the no-extra-gate paragraph, worded "the Docker test
    stage and CI all already run" — the existing wording about pre-commit running pytest is
    inaccurate (RESEARCH §"Project Constraints").
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/architecture/test_use_case_totality.py -x --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/architecture/test_use_case_totality.py -x --no-cov` exits 0
    - Temporarily removing one name from `REQUIRED_USE_CASE_SYMBOLS` and re-running does NOT make
      the gate red (the guard is a subset check), while temporarily adding a fictitious name DOES
      make it red naming that name — revert both
    - Temporarily renaming `tests/unit/application/test_list_users.py` to a non-`test_` name makes
      the gate red naming `ListUsers` — revert
    - `.venv/bin/mypy src tests` exits 0 and `.venv/bin/flake8 src tests` exits 0
  </acceptance_criteria>
  <done>The gate passes against today's tree, and both falsifications above behaved as stated.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint totality — ASGI recorder, ordering hook and two-sided check (D-03, TEST-02)</name>
  <files>tests/integration/conftest.py, tests/integration/test_endpoint_totality.py</files>
  <read_first>
    - tests/integration/conftest.py (in full: `api_client` and `authenticated_client` and the
      documented one-line difference between them, the `statements` recorder fixture as the
      existing "listen, append, clean up" precedent, the module-level constants convention, and
      the `alembic_config` docstring — do not fix its stale path reference here, plan 04 owns it)
    - tests/unit/presentation/test_security_scheme.py (`NON_OPERATION_KEYS` and `_operations()`,
      the only OpenAPI enumeration in the repo — copy it rather than reading `app.routes`)
    - .planning/phases/06-test-hardening-coverage/06-RESEARCH.md §"D-03" (the recorder sketch, the
      unique-suffix resolver, the partial-run detection list and the ordering argument)
    - .planning/phases/06-test-hardening-coverage/06-PATTERNS.md §"tests/integration/conftest.py"
  </read_first>
  <action>
    Record every operation the integration run actually requests, and compare it against
    `app.openapi()` (D-01, D-03, TEST-02).

    In `tests/integration/conftest.py`: add a module-level `REQUESTED: Final[set[tuple[str, str]]]`
    and a pure-ASGI wrapper function that awaits the wrapped app and then, for `scope["type"] ==
    "http"`, reads `scope.get("route")` (absent on 404/405 — nothing to record) and adds
    `(scope["method"].lower(), route.path_format)`. Wrap the app at the transport in BOTH
    `api_client` and `authenticated_client` — `ASGITransport(app=<wrapper>)` — so the application
    under test stays byte-for-byte the production one; do not call `app.add_middleware`. Annotate
    the wrapper with `Scope`, `Receive`, `Send` from `starlette.types` so `mypy --strict` passes.
    Keep yielding the same `(client, app)` two-tuple; no test signature changes.

    Add `pytest_collection_modifyitems(config: pytest.Config, items: list[pytest.Item]) -> None`
    to the same conftest, moving the totality item to the end of `items` rather than relying on
    filename sort.

    Create `tests/integration/test_endpoint_totality.py` with the two-sided check:
    - Always: every recorded operation resolves to a published one. Resolve a router-local
      template to a published path by unique-suffix match against `app.openapi()["paths"]`,
      asserting inside the resolver that exactly one published path ends with it and failing
      loudly on a collision or on no match. `/api/v1` is in neither `path_format` nor
      `root_path` on this stack — that is why the resolver exists.
    - Only on a full run: every published operation was recorded. Detect a partial selection from
      `config.option` — `keyword`, `markexpr`, `deselect`, `last_failed`, `failed_first`,
      `stepwise`, and `config.args` differing from the `testpaths` default — and
      `pytest.skip("partial selection — totality is asserted on a full run")` that half only.
    Assert non-vacuity: the operation mapping is non-empty and, on a full run, `REQUESTED` is
    non-empty. Take the app from the harness fixtures' two-tuple, not from a fresh `create_app`,
    so the document compared is the one the tests drove.

    Docstring: why a wrapper and not `add_middleware`; why a module-level session-lived set is
    sound (single process, no xdist); why the two-sided split exists; and the honest property —
    a route requested by a test that only proved a 401 still counts as requested, which is why
    plan 02's assertion-quality gate is what makes each request mean something. Add the
    no-extra-gate paragraph.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration --no-cov` exits 0 and the totality item runs last
    - `.venv/bin/pytest tests/integration/api/test_users.py --no-cov` exits 0 (a focused run is
      not falsely red — the totality half skips, the recorded-subset half still runs)
    - `.venv/bin/pytest -k task_lists --no-cov` exits 0
    - Temporarily adding a `GET /api/v1/ping` route to the app factory makes
      `.venv/bin/pytest tests/integration --no-cov` red naming that operation — revert
    - `.venv/bin/mypy src tests` exits 0
  </acceptance_criteria>
  <done>All 19 published operations are recorded on a full integration run; focused runs stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Negative-path totality — transition complement over HTTP and the error-leaf gate (D-04, TEST-04)</name>
  <files>tests/integration/api/test_tasks.py, tests/architecture/test_error_contract_totality.py</files>
  <read_first>
    - tests/integration/api/test_tasks.py (the existing `test_an_invalid_transition_is_409_naming_the_transition`,
      the lifecycle test that asserts a move is in `ALLOWED_TRANSITIONS` before driving it, the
      `given_a_task` / builder helpers, `assert_task_not_found`, and the existing
      `ALLOWED_TRANSITIONS` import — keep every existing test, nothing is deleted this phase)
    - tests/unit/domain/test_task_status.py (the table is read from the source of truth, and the
      self-transition leg is already its own test)
    - tests/unit/domain/test_task.py (the three self-transitions asserted as idempotent no-ops)
    - src/taskmanager/domain/value_objects/task_status.py (`ALLOWED_TRANSITIONS`)
    - src/taskmanager/domain/exceptions.py (the `code` ClassVar on every leaf, and which classes
      are abstract parents)
    - tests/architecture/test_routers_raise_no_http_exception.py (the gate style task 1 also follows)
  </read_first>
  <action>
    Two pieces, both derived from the source of truth rather than from a checklist.

    (a) In `tests/integration/api/test_tasks.py`, add one parametrized HTTP test over the
    complement of `ALLOWED_TRANSITIONS`, built as every `(a, b)` in `TaskStatus × TaskStatus`
    where `b not in ALLOWED_TRANSITIONS[a]`, with readable ids per pair. Branch on `a is b`:
    a self-pair asserts the deliberate 200 no-op (the response body still carries status `a`)
    and a cross-pair asserts 409 with the `invalid_status_transition` problem body — full
    RFC 9457 member list, `code`, and the errors naming the current and requested statuses —
    using the module's existing problem-body conventions and `PROBLEM_JSON` / `MEMBERS` imports.
    Every leg re-reads the task with a GET afterwards and asserts the persisted status is what
    the leg promised (unchanged on the 409 leg), so the test satisfies D-06 on arrival and plan
    02's gate does not flag it. Seed the task directly at starting status `a` rather than walking
    it there. State the `a is b` split in the docstring as a decision (the self-pairs are outside
    the table but are deliberate no-ops), and state that a fourth status adds its cases with no
    edit. Keep the existing hard-coded 409 test in place.

    (b) Create `tests/architecture/test_error_contract_totality.py`: import
    `taskmanager.domain.exceptions`, walk `DomainError.__subclasses__()` recursively to collect
    every class and its `code` ClassVar; AST-scan `src/taskmanager/**/*.py` for `raise <Name>`
    statements to learn which leaves are actually raised (abstract parents such as `DomainError`,
    `BusinessRuleViolationError`, `ConflictError` and `NotFoundError` are never raised and are
    outside the rule); then assert each raised leaf's `code` appears as a string literal in at
    least one module under `tests/`, reporting `code -> class` for every uncovered one. Guard
    non-vacuity with `REQUIRED_RAISED_CODES: Final[frozenset[str]]` naming the nine codes listed
    in `<interfaces>`, asserted a subset of the discovered raised set, plus an assertion that the
    `src/` scan and the `tests/` scan both matched files. Docstring: why the rule is "raised under
    `src/`" rather than "declared" (an abstract parent has no body to assert), and the
    no-extra-gate paragraph.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/architecture/test_error_contract_totality.py tests/integration/api/test_tasks.py --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/architecture/test_error_contract_totality.py tests/integration/api/test_tasks.py --no-cov` exits 0
    - The complement test reports 4 parametrized cases today (`pending→pending`,
      `in_progress→in_progress`, `completed→completed`, `completed→pending`), visible in
      `.venv/bin/pytest tests/integration/api/test_tasks.py -k complement --no-cov -v`
    - Temporarily adding `TaskStatus.PENDING` to `ALLOWED_TRANSITIONS[TaskStatus.COMPLETED]`
      changes the parametrization and makes the suite red — revert
    - Temporarily renaming the `task_not_found` code literal in `src/taskmanager/domain/exceptions.py`
      makes `test_error_contract_totality.py` red naming the new code — revert
    - `.venv/bin/mypy src tests` and `.venv/bin/flake8 src tests` exit 0
  </acceptance_criteria>
  <done>Both negative-path totality properties are asserted from the source of truth and pass.</done>
</task>

<task type="auto">
  <name>Task 4: The two D-14 findings — seed the auth caller, and lock the list-deletion path against real connections</name>
  <files>tests/integration/api/test_auth.py, tests/integration/test_concurrent_writes.py</files>
  <read_first>
    - tests/integration/api/test_auth.py (`UNAUTHENTICATED_CASES`, its seven header builders,
      `test_unauthenticated_requests_are_refused_with_the_one_shared_body`, the
      `a_token_for_an_unknown_subject` docstring which deliberately relies on an unseeded
      subject, and `test_every_unauthenticated_refusal_carries_the_same_body_as_the_others`)
    - tests/integration/conftest.py (`seed`, `authenticated_client`, `session_factory`, `OWNER_ID`,
      and the builders the API modules use)
    - tests/integration/api/test_task_lists.py (the `seed(session_factory, users=[a_user()], ...)`
      call shape and the `a_user()` builder)
    - tests/integration/test_concurrent_writes.py (in full: the two-connection pattern, the
      `_until_it_waits_or_finishes` / `_settle` helpers, the three bounds, and the load-bearing
      `assert waited, "<message>"` line)
    - src/taskmanager/application/use_cases/task_lists/delete.py (the `for_update=True` call the
      new case must prove)
    - src/taskmanager/infrastructure/security/tokens.py (what `verify_exp` and `_REQUIRED_CLAIMS`
      actually enforce, so the expired-token case is asserted against the right mechanism)
    - .planning/phases/06-test-hardening-coverage/06-RESEARCH.md §"D-04 Authentication failures"
      and §"break 5"
  </read_first>
  <action>
    Fix the two tests the research proved pass for the wrong reason (D-14). Both are test-side
    fixes; no `src/` change is expected, and if one turns out to be needed it stays minimal.

    (a) `AuthenticateActor` confirms the subject on every request, and
    `test_unauthenticated_requests_are_refused_with_the_one_shared_body` never seeds a `users` row
    for `OWNER_ID`, so six of its seven cases are refused for the unknown-subject reason whatever
    credential they carry — measured: with `verify_exp: False` planted in `tokens.py` the whole
    selection stays green. Take `session_factory` into the test and seed the caller with
    `a_user()` before driving the cases, so `an_expired_token`, `a_foreign_secret_token`,
    `garbage`, `an_empty_bearer_parameter`, `a_basic_scheme` and `no_header` each reach their own
    branch. `a_token_for_an_unknown_subject` must keep signing for a fresh `uuid.uuid4()` subject
    so it still tests the confirmation read; say so in its docstring. Keep the existing assertions
    — the shared problem body, the `WWW-Authenticate: Bearer` challenge, and that the offered
    credential is not echoed — and keep
    `test_every_unauthenticated_refusal_carries_the_same_body_as_the_others` passing: the bodies
    stay byte-identical, which is the point of that test.

    (b) Add a fifth case to `tests/integration/test_concurrent_writes.py` proving a list deletion
    holds the lock it takes: following
    `test_two_list_patches_are_serialised_and_neither_edit_is_lost`, open two units of work on two
    real connections, have the first take the list `for_update=True`, start the second as a
    deletion (or the deletion first and a patch second — pick the direction that pins
    `DeleteTaskList`'s own locking read), assert with the existing helpers and the three bounds
    that the second waited, and assert the end state after both settle. The load-bearing
    `assert waited, "<message>"` line is mandatory; without it the outcome could be luck. This
    closes the blind spot behind break 5, whose only red today is a fakes-based road pin.

    Do not write anything to `AI_WORKFLOW.md` here — plan 03 records both findings, before and
    after, in one dated incident entry. State both fixes plainly in this plan's SUMMARY so plan 03
    can cite them.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/api/test_auth.py tests/integration/test_concurrent_writes.py --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration/api/test_auth.py tests/integration/test_concurrent_writes.py --no-cov` exits 0
    - With `"verify_exp": False` temporarily added to the `options` dict in
      `src/taskmanager/infrastructure/security/tokens.py`,
      `.venv/bin/pytest tests/integration/api/test_auth.py -k unauthenticated --no-cov` exits
      non-zero and names the `an_expired_token` case — revert the edit and confirm
      `git status --porcelain -- src/` is empty
    - With `, for_update=True` temporarily dropped from the locking read in
      `src/taskmanager/application/use_cases/task_lists/delete.py`, the new concurrency case is
      among the failures of `.venv/bin/pytest tests/integration/test_concurrent_writes.py --no-cov`
      — revert and confirm `git status --porcelain -- src/` is empty
    - `.venv/bin/pytest` exits 0 afterwards (full suite, coverage gate included)
  </acceptance_criteria>
  <done>Both vacuous tests now fail for the reason they name, proven by the two temporary breaks above.</done>
</task>

<task type="auto">
  <name>Task 5: Close the plan</name>
  <files>.planning/phases/06-test-hardening-coverage/06-01-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current position, progress counters and the notes under Current Position)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Run the full gate set and close out. Keep the ceremony minimal (D-15): one SUMMARY, no
    evidence file, no transcript file, no RED artifact.

    In the SUMMARY, record: the four gates planted and that each was driven red once before being
    trusted (one sentence each, naming the temporary edit that did it — the transcripts are not
    kept); the two D-14 findings with their before and after in one line each, flagged for plan 03
    to fold into the `AI_WORKFLOW.md` incident entry; the full-suite pass count and coverage total
    after this plan, for the three-way agreement plan 04 records; and the ADR-worthy rules this
    plan introduced (use-case totality, endpoint totality, error-leaf totality) so plan 04 can
    write them up from ADR-085 onward with their CLAUDE.md bullets.

    After any `gsd-sdk` state handler runs, `git diff .planning/STATE.md` and repair regressions by
    hand — the handlers are known to reset the percent, inject blank lines and miscount totals.

    Commit with no AI or Claude co-author trailer of any kind (CLAUDE.md §"Language and
    attribution"), message in English.
  </action>
  <verify>
    <automated>make lint && make typecheck && make arch && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make lint && make typecheck && make arch && make test` exits 0
    - `.venv/bin/pytest` reports no fewer tests than the 1019 baseline and coverage >= 75%
    - `git log -1 --format=%B` contains no `Co-Authored-By` or `Generated with` line
    - `git diff .planning/STATE.md` shows only intended edits (percent, counters and prose intact)
  </acceptance_criteria>
  <done>Every gate is green, the SUMMARY exists, STATE.md is correct, and the work is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (bearer token) | Untrusted credentials cross here; `get_current_actor` and `AuthenticateActor` decide |
| test suite → build verdict | A gate's verdict is what the project trusts instead of a review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | `infrastructure/security/tokens.py` expiry enforcement, exercised through `tests/integration/api/test_auth.py` | mitigate | Task 4(a): seed the caller so the expired / foreign-signed / malformed cases are refused by the mechanism they name; proven by planting `verify_exp: False` and observing an HTTP test go red (ASVS V3) |
| T-06-02 | Tampering | `application/use_cases/task_lists/delete.py` locking read | mitigate | Task 4(b): a two-connection integration case proves the lock against real PostgreSQL, not only against fakes (lost update on concurrent writes) |
| T-06-03 | Repudiation | The four new gates | mitigate | Every gate carries a non-vacuity guard (`REQUIRED_USE_CASE_SYMBOLS`, `REQUIRED_RAISED_CODES`, non-empty operation map and `REQUESTED` set) and is driven red once before being trusted; a gate that is green forever while checking nothing is worse than no gate |
| T-06-04 | Elevation of privilege | 404/403 visibility in `application/use_cases/access.py` | accept | Already defended: the 76-cell permission matrix turns 33 tests red when the decision is flipped (measured). This plan's endpoint recorder additionally makes a new unmatrixed route fail |
| T-06-SC | Tampering | package-manager installs | accept | No dependency is added by this phase (D-08 declines mutmut); no install task exists, so the package legitimacy gate does not apply |
</threat_model>

<verification>
- `make lint && make typecheck && make arch && make test` green.
- `.venv/bin/pytest tests/integration --no-cov` green with the totality item last.
- A focused run (`.venv/bin/pytest tests/integration/api/test_users.py --no-cov`) is green.
- Each of the four falsifications named in the task acceptance criteria was observed, and
  `git status --porcelain -- src/` is empty afterwards.
</verification>

<success_criteria>
Adding a use case, a route, a status or a `DomainError` leaf without its test fails a build; the
six named authentication failure modes each reach their own branch; and the list-deletion lock is
proven against two real connections.
</success_criteria>

<output>
Create `.planning/phases/06-test-hardening-coverage/06-01-SUMMARY.md` when done
</output>
