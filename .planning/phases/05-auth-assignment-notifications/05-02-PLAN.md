---
phase: 05-auth-assignment-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/taskmanager/application/ports/security.py
  - src/taskmanager/application/ports/repositories.py
  - src/taskmanager/infrastructure/db/repositories/tasks.py
  - tests/unit/application/fakes.py
  - tests/unit/application/test_ports.py
  - tests/unit/application/test_fakes.py
  - tests/integration/test_repositories_tasks.py
  - .planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt
autonomous: true
requirements: [AUTH-04, ASGN-02, ASGN-03]

must_haves:
  truths:
    - "PasswordHasher gains exactly one method, dummy_verify(password) -> None (D-21), and the port docstring argues why: D-12's timing equalisation needs an Argon2 verify against a throwaway hash, application may not import pwdlib, and a hard-coded Argon2 string in source reads as a credential"
    - "TaskRepository gains list_for_assignee(assignee_id) -> Sequence[Task] (D-02), implemented by SqlAlchemyTaskRepository with ORDER BY created_at, id and by FakeTaskRepository with the same sort"
    - "FakeUserRepository.list_all is ordered by (created_at, id) like the adapter it stands in for (D-25, RESEARCH Pitfall 9), and the divergence was observed red before the fix"
    - "Every fake still satisfies its port under mypy strict: the port-conformance assignments in tests/unit/application/test_ports.py compile after both port changes"
  artifacts:
    - path: "src/taskmanager/application/ports/security.py"
      provides: "the D-21 port extension"
      contains: "dummy_verify"
    - path: "src/taskmanager/application/ports/repositories.py"
      provides: "the assigned-tasks query contract"
      contains: "list_for_assignee"
    - path: ".planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt"
      provides: "the red run against the unsorted user fake"
  key_links:
    - from: "src/taskmanager/infrastructure/db/repositories/tasks.py"
      to: "src/taskmanager/application/ports/repositories.py"
      via: "list_for_assignee implements the port method, returning domain entities only"
      pattern: "list_for_assignee"
    - from: "tests/unit/application/fakes.py"
      to: "src/taskmanager/infrastructure/db/repositories/users.py"
      via: "the fake's list_all sort mirrors the adapter's ORDER BY created_at, id"
      pattern: "sorted"
---

<objective>
Make the two port changes this phase genuinely needs, in one deliberate commit each, and bring the
in-memory doubles back into line with the adapters they stand in for.

Purpose: CONTEXT's carried-forward section says the ports "keep their shape unless research proves a
need". Research proved it twice — once for D-12's timing equalisation (D-21) and once for D-02's
discovery route — and both are port changes that break every implementation at once, so they belong
in a plan that owns the whole blast radius rather than in the plan that first wants the method. The
`FakeUserRepository.list_all` divergence (RESEARCH Pitfall 9) is the same defect 04-06 fixed for the
task fake and missed here; it would make an ASGN-03 ordering assertion pass in unit tests and fail
over HTTP.
Output: `PasswordHasher.dummy_verify`, `TaskRepository.list_for_assignee` with its adapter and fake,
and a sorted `FakeUserRepository.list_all` with the red capture that proves it was unsorted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-assignment-notifications/05-CONTEXT.md
@.planning/phases/05-auth-assignment-notifications/05-RESEARCH.md
@.planning/phases/05-auth-assignment-notifications/05-PATTERNS.md
@.planning/phases/05-auth-assignment-notifications/05-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- The two ports as they stand today. -->

`application/ports/security.py`:

    class PasswordHasher(Protocol):
        async def hash(self, password: str) -> str: ...
        async def verify(self, password: str, hashed: str) -> bool: ...

`application/ports/repositories.py`, `TaskRepository` already declares `get`, `get_for_update`,
`add`, `update`, `delete`, `list_for_task_list(task_list_id, *, status=None, priority=None)` and
`completion_stats(task_list_id)`.

The adapter's ordering idiom, from `infrastructure/db/repositories/tasks.py::list_for_task_list`:
`statement.order_by(TaskRow.created_at, TaskRow.id)` — `created_at` alone is not a total order,
because two tasks created in one request share an instant.

`FakeUserRepository.list_all` today is `return list(self.stored.values())` — insertion order.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The D-21 PasswordHasher port extension</name>
  <files>src/taskmanager/application/ports/security.py, tests/unit/application/fakes.py, tests/unit/application/test_ports.py</files>
  <read_first>
    - `src/taskmanager/application/ports/security.py` — the whole file: the module docstring is
      where the *reason* for a port method is argued, and the new method must join it in the same
      voice
    - `tests/unit/application/fakes.py` — `FakePasswordHasher` (the reversible `fake-hash:` prefix
      and its docstring), and the surrounding fakes for style
    - `tests/unit/application/test_ports.py` — the conformance tests
      (`hasher: PasswordHasher = FakePasswordHasher()`): the typed assignment **is** the test and
      `mypy --strict` is the gate; there is no ABC and no `isinstance`
    - `.planning/phases/05-auth-assignment-notifications/05-RESEARCH.md` § Pattern 4 and
      § Open Question 3 — the measured timings (23.6 ms against a real hash, 23.4 ms against the
      dummy) and the three rejected alternatives
    - `.planning/phases/05-auth-assignment-notifications/05-CONTEXT.md` D-12 and D-21
    - `.importlinter` — the `application-framework-free` contract, which lists `pwdlib`, so the
      application layer cannot produce a dummy hash itself
  </read_first>
  <behavior>
    - `FakePasswordHasher().dummy_verify("anything")` awaits and returns `None`
    - `hasher: PasswordHasher = FakePasswordHasher()` type-checks under `mypy --strict`
    - The fake records that `dummy_verify` was called, so a `Login` unit test can assert the unknown-email leg burned the work
  </behavior>
  <action>
    Add one method to the `PasswordHasher` Protocol:
    `async def dummy_verify(self, password: str) -> None: ...`

    It returns `None` on purpose — there is no answer to report, only work to perform — and a
    comment on the method must say so, otherwise a reader will expect a `bool` and a caller will be
    tempted to branch on it.

    Extend the module docstring with the argument for the method's existence, in the register the
    file already uses: D-12 requires an unknown email and a wrong password to be indistinguishable;
    equal bodies are not enough if the unknown-email path returns in under a millisecond while the
    wrong-password path pays Argon2's 23 ms; the equalising work is a verify against a throwaway
    hash; producing that hash needs `pwdlib`, which `.importlinter`'s `application-framework-free`
    contract forbids in this layer; and hard-coding an encoded Argon2 string in source reads as a
    credential to anyone grepping the repository. Name this as the one place research overrode
    CONTEXT's "the ports keep their shape" note, and name D-21 as the decision that authorised it.

    Extend `FakePasswordHasher` with a conforming `dummy_verify` that does nothing expensive but
    **records the call** — add a public list attribute (for example `dummy_verifications:
    list[str]`) appended to on each call, in the style of `FakeEmailNotifier.sent` and
    `FakeTaskRepository.held_for_update`. Plan 05-07's `Login` tests assert on it. Extend the fake's
    docstring with one sentence saying the recorder exists so a timing-equalisation test can assert
    the work was requested without measuring a clock.

    Add a test in `tests/unit/application/test_ports.py` in the module's existing shape: the typed
    assignment plus one assertion that `dummy_verify` is awaited and records the password it was
    given. Do not add an `isinstance` check and do not introduce an ABC.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/application/test_ports.py -q --no-cov && .venv/bin/mypy src tests && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/application/test_ports.py -q --no-cov` exits 0
    - `grep -c "dummy_verify" src/taskmanager/application/ports/security.py` prints at least `1`
    - `.venv/bin/python -c "
import asyncio, inspect
from taskmanager.application.ports.security import PasswordHasher
from tests.unit.application.fakes import FakePasswordHasher
h: PasswordHasher = FakePasswordHasher()
assert inspect.iscoroutinefunction(h.dummy_verify)
assert asyncio.run(h.dummy_verify('pw')) is None
"` exits 0
    - `grep -c "pwdlib" src/taskmanager/application/ports/security.py` prints at least `1` — the docstring names the forbidden library by name as the reason
    - `.venv/bin/mypy src tests` exits 0
    - `make arch` exits 0 and its output contains `0 broken`
  </acceptance_criteria>
  <done>The port has the one method D-12 cannot be implemented without, and the reason is on the record as a deliberate extension rather than a quiet addition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TaskRepository.list_for_assignee, its adapter and its fake</name>
  <files>src/taskmanager/application/ports/repositories.py, src/taskmanager/infrastructure/db/repositories/tasks.py, tests/unit/application/fakes.py, tests/unit/application/test_fakes.py, tests/integration/test_repositories_tasks.py</files>
  <read_first>
    - `src/taskmanager/application/ports/repositories.py` — `TaskRepository` in full, and
      `list_for_task_list`'s comment ("belongs in the query the adapter issues rather than in a
      comprehension the use case applies to a full table read")
    - `src/taskmanager/infrastructure/db/repositories/tasks.py` — `list_for_task_list` in full: the
      `select(TaskRow).where(...)`, the `order_by(TaskRow.created_at, TaskRow.id)` and the ordering
      rationale in its docstring, plus `task_to_entity` usage so only domain entities are returned
    - `tests/unit/application/fakes.py` — `FakeTaskRepository.list_for_task_list`, including the
      long comment about the fake/adapter ordering divergence
    - `tests/integration/test_repositories_tasks.py` — the module's fixtures, its fixed identifiers
      and how an existing listing test seeds rows whose insertion order is **not** the expected
      answer
    - `.planning/phases/05-auth-assignment-notifications/05-CONTEXT.md` D-02 and the Deferred Ideas
      section (filtering `assigned-to-me` by status or priority is explicitly out of scope)
  </read_first>
  <behavior>
    - The adapter returns only tasks whose `assignee_id` equals the argument, across more than one parent list
    - The adapter returns `[]` for a user with no assignments
    - The adapter's result is ordered by `created_at` then `id`, proven with two rows sharing one instant and seeded in the reverse of the expected order
    - The fake's result is ordered identically
    - `repository: TaskRepository = SqlAlchemyTaskRepository(session)` still type-checks
  </behavior>
  <action>
    Add to the `TaskRepository` Protocol:
    `async def list_for_assignee(self, assignee_id: UUID) -> Sequence[Task]: ...`
    with a comment naming D-02 and re-making `list_for_task_list`'s argument: the filter belongs in
    the SQL, not in a comprehension over a full table read. State that it takes no `status` or
    `priority` keyword — that filtering is a Deferred Idea in CONTEXT, not an oversight.

    Implement it on `SqlAlchemyTaskRepository` as
    `select(TaskRow).where(TaskRow.assignee_id == assignee_id).order_by(TaskRow.created_at,
    TaskRow.id)`, executed with `self._session.scalars(...)` and mapped through `task_to_entity`,
    exactly as `list_for_task_list` does. No transaction-ending call: the
    `test_no_commit_in_repositories.py` gate scans this package.

    Implement it on `FakeTaskRepository` beside `list_for_task_list`, selecting on `assignee_id`
    and sorting with the same `key=lambda entry: (entry.created_at, entry.id)`. Point at the
    existing ordering comment rather than copying it a third time.

    Add unit cases to `tests/unit/application/test_fakes.py` for the fake (filter, empty, order) and
    integration cases to `tests/integration/test_repositories_tasks.py` for the adapter. In the
    ordering case, seed two tasks that share one `created_at` instant, in the reverse of the id
    order, so an implementation that dropped the `id` tie-break fails rather than flakes — the
    argument `repositories/tasks.py` already writes out for `list_for_task_list`.

    Cross-list coverage is the point of the assignee query: seed at least two lists owned by
    different users with tasks assigned to the same user, and assert the result spans both.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/application/test_fakes.py -q --no-cov && .venv/bin/pytest tests/integration/test_repositories_tasks.py -q --no-cov && .venv/bin/mypy src tests && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration/test_repositories_tasks.py -k assignee -q --no-cov` exits 0 with at least 3 tests collected
    - `.venv/bin/pytest tests/unit/application/test_fakes.py -q --no-cov` exits 0
    - `grep -c "list_for_assignee" src/taskmanager/application/ports/repositories.py` prints at least `1`
    - `grep -c "list_for_assignee" src/taskmanager/infrastructure/db/repositories/tasks.py` prints at least `1`
    - `.venv/bin/python -c "
import inspect
from taskmanager.infrastructure.db.repositories.tasks import SqlAlchemyTaskRepository
from tests.unit.application.fakes import FakeTaskRepository
for cls in (SqlAlchemyTaskRepository, FakeTaskRepository):
    s = inspect.signature(cls.list_for_assignee)
    assert list(s.parameters) == ['self', 'assignee_id'], (cls, s)
"` exits 0
    - `.venv/bin/pytest tests/architecture/test_no_commit_in_repositories.py -q --no-cov` exits 0
    - `.venv/bin/mypy src tests` exits 0 — the `TaskRepository` port binding in `tests/unit/infrastructure/test_adapter_ports.py` still compiles with no change to that file
  </acceptance_criteria>
  <done>D-02's discovery route has its query, in SQL, ordered totally, with the fake and the adapter agreeing on the answer.</done>
</task>

<task type="auto">
  <name>Task 3: Sort FakeUserRepository.list_all, after observing it unsorted</name>
  <files>tests/unit/application/fakes.py, tests/unit/application/test_fakes.py, .planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt</files>
  <read_first>
    - `tests/unit/application/fakes.py` — `FakeUserRepository.list_all` (`return
      list(self.stored.values())`) and the long comment above
      `FakeTaskRepository.list_for_task_list`, which describes exactly this divergence and is the
      comment to mirror
    - `src/taskmanager/infrastructure/db/repositories/users.py` — `list_all`'s
      `order_by(UserRow.created_at, UserRow.id)` and the docstring recording the `id` tie-break as
      review fix WR-03
    - `.planning/phases/04-task-lists-tasks/evidence/04-06-fake-ordering.txt` — the precedent
      capture: the fake's divergence was **observed red** before it was fixed, not asserted
    - `.planning/phases/05-auth-assignment-notifications/05-CONTEXT.md` D-25 — "falsified the way
      04-06 did"
    - `.planning/phases/05-auth-assignment-notifications/05-RESEARCH.md` § Pitfall 9
  </read_first>
  <action>
    Write the ordering test **first** and run it against the unsorted fake. Seed three users whose
    insertion order is deliberately not the `created_at, id` order — including two sharing one
    instant, so the `id` tie-break is exercised — and assert `list_all()` returns them in
    `(created_at, id)` order. Capture the failing run verbatim to
    `.planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt`,
    including the command, the assertion diff and the summary line. Create the `evidence/`
    directory if it does not exist.

    Then change `FakeUserRepository.list_all` to
    `sorted(self.stored.values(), key=lambda entry: (entry.created_at, entry.id))` and re-run,
    appending the green run to the same capture file.

    Add the comment the task fake carries, adapted: the fake used to return insertion order, which
    is a divergence with teeth — an ASGN-03 ordering assertion would pass here and fail over HTTP,
    where PostgreSQL may answer in whatever order the plan produced; `created_at` alone is not a
    total order either. Name D-25 and record that this fake was missed when the two sibling fakes
    were fixed in 04-02 and 04-06.

    Do not touch the adapter: `SqlAlchemyUserRepository.list_all` already orders correctly and
    `ListUsers` (plan 05-08) reuses it unchanged.

    The red step is **not** a separate commit. CLAUDE.md forbids committing a red tree and
    `--no-verify` is forbidden; the capture and the fix are committed together, the compromise 02-01
    and 04-02 recorded.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/application/test_fakes.py -q --no-cov && .venv/bin/pytest tests/unit tests/architecture -q --no-cov && test -s .planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/application/test_fakes.py -k list_all -q --no-cov` exits 0 with at least 1 test collected
    - `.venv/bin/python -c "
import asyncio, datetime as dt, uuid
from tests.unit.application.fakes import FakeUserRepository
" ` exits 0
    - `grep -c "sorted(" tests/unit/application/fakes.py` prints at least `4` — the two sibling fakes, `list_all`, and `list_for_assignee` from Task 2
    - `.planning/phases/05-auth-assignment-notifications/evidence/05-02-fake-user-ordering.txt` contains both a failing run (a line matching `1 failed`) and a passing run (a line matching `passed`)
    - `grep -c "D-25" tests/unit/application/fakes.py` prints at least `1`
    - `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0
  </acceptance_criteria>
  <done>The user fake answers in the order the database does, and the file proves the divergence existed rather than claiming it was anticipated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application port → infrastructure adapter | The contract is stated in domain terms; a fake that diverges from the adapter makes the unit suite lie |
| caller's token subject → `list_for_assignee` argument | The only input to D-02's query, and it must come from the token rather than from a client-supplied id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-05 | Information disclosure | login timing distinguishing an unknown email from a wrong password | mitigate | `PasswordHasher.dummy_verify` puts the equalising Argon2 work behind the port so the application layer can request it without importing `pwdlib`; the fake records the call so plan 05-07 can assert the unknown-email leg performed it |
| T-5-11 | Information disclosure | IDOR through the assigned-tasks query | mitigate | `list_for_assignee` takes exactly one argument and filters in SQL; plan 05-08's `ListAssignedTasks` passes the **token's** subject, never a body or query value, and no `owner_id` widening exists on the port |
| T-5-16 | Information disclosure | a credential comparison written by this project | mitigate | Neither the port nor the fake compares a real hash; the constant-time comparison stays inside `argon2-cffi` behind the adapter (plan 05-05) |
| T-4-xx | Repudiation | a unit suite that passes against an ordering the database does not produce | mitigate | The `list_all` divergence is observed red and the capture is committed beside the fix; both new list methods sort on `(created_at, id)` in the fake and in the adapter, and the adapter test seeds two rows sharing an instant |
| T-5-SC | Tampering | pip installs | accept | This plan installs no package; `requirements*.txt` unchanged |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0.
- `.venv/bin/pytest tests/integration/test_repositories_tasks.py -q --no-cov` exits 0 with the `db`
  container running.
- `make lint && make typecheck && make arch && make test` all exit 0; `make arch` reports `0 broken`.
- `grep -rn "pragma: no cover" src/taskmanager/` prints nothing.
</verification>

<success_criteria>
- `PasswordHasher` has exactly one new method, and the port docstring argues for it by name.
- `TaskRepository.list_for_assignee` exists on the port, the adapter and the fake, all three ordered
  by `(created_at, id)`.
- `FakeUserRepository.list_all` is sorted, and the evidence file shows the unsorted version failing.
</success_criteria>

<output>
Create `.planning/phases/05-auth-assignment-notifications/05-02-SUMMARY.md` when done
</output>
