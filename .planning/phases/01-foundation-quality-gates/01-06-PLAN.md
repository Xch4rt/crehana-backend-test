---
phase: 01-foundation-quality-gates
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - CLAUDE.md
  - DECISION_LOG.md
autonomous: true
requirements: [AIW-04, FND-09]
must_haves:
  truths:
    - "D-17: CLAUDE.md carries a project-rules section stating the layer order, that HTTPException may never be raised outside presentation, that every quality gate must be green before a commit, English only, and no AI attribution in commit messages"
    - "D-20: the no-AI-attribution and English-only rules are written down in CLAUDE.md as enforceable project rules, not left implicit"
    - "D-19: DECISION_LOG.md exists in ADR style with a context / options / decision / consequences block for every decision locked so far"
    - "D-07: DECISION_LOG.md records that ruff is deliberately not used because the brief mandates flake8, and names ruff as what would be chosen absent the constraint"
    - "D-11 and D-13: DECISION_LOG.md records the venv-qualified repo-local hook entries and the consequence that CI and Docker run the gates directly instead of invoking pre-commit"
    - "AIW-04: the .planning artifacts and the shipped repository describe the same rules, so an evaluator reading CLAUDE.md sees what the gates actually enforce"
  artifacts:
    - path: "CLAUDE.md"
      provides: "the architecture and quality rules imposed on the AI"
      contains: "HTTPException"
    - path: "DECISION_LOG.md"
      provides: "ADR-style record of every locked technical decision and its consequences"
      contains: "## ADR"
  key_links:
    - from: "CLAUDE.md project rules"
      to: ".importlinter contracts"
      via: "the stated layer order matching the layers contract exactly"
      pattern: "domain"
    - from: "DECISION_LOG.md"
      to: ".flake8 and pyproject.toml"
      via: "the flake8-over-ruff ADR naming the configuration files that implement it"
      pattern: "flake8"
---

<objective>
Write down the rules the automated gates enforce, so a human reader (and every future
Claude session) sees the same contract the tooling enforces, and open the ADR-style
decision log that Phase 7 finishes.

Purpose: AIW-04 is graded on `CLAUDE.md` holding the architecture and quality rules and on
`.planning/` being consistent with what shipped. `DECISION_LOG.md` is a literal
brief-mandated file; starting it now with the decisions already locked means Phase 7
documents rather than reconstructs.
Output: a project-rules section appended to `CLAUDE.md`, and `DECISION_LOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-quality-gates/01-CONTEXT.md
@.planning/phases/01-foundation-quality-gates/01-RESEARCH.md
@.planning/phases/01-foundation-quality-gates/01-04-SUMMARY.md
@.planning/phases/01-foundation-quality-gates/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project rules section in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` (the existing file — it is generated in GSD-delimited blocks; the new section must go OUTSIDE every `<!-- GSD:*-start -->` / `<!-- GSD:*-end -->` pair so a later regeneration cannot overwrite it)
    - `.planning/phases/01-foundation-quality-gates/01-CONTEXT.md` decisions D-17, D-20, D-10, D-02
    - `.importlinter` (the layer order and forbidden-module lists the rules must mirror exactly)
    - `Makefile` (the target names the rules must reference)
    - `.planning/ROADMAP.md` "Standing Rules"
  </read_first>
  <action>
Append a new top-level section titled `## Project Rules` to `CLAUDE.md`, placed outside
every GSD-managed delimiter block so that regenerating `CLAUDE.md` cannot silently delete
it. Add a one-line note stating it is hand-maintained.

The section must state, in English, each of the following as an explicit rule:

Layer rules — the import direction is `main` > `presentation` > `infrastructure` >
`application` > `domain`, imports only ever point downward, `taskmanager.domain` imports
no third-party library at all (stdlib dataclasses and enums only), and
`taskmanager.application` imports no web framework and no ORM (Pydantic is allowed there
for DTOs). State that this is enforced automatically by the import-linter contracts in
`.importlinter`, executed by `tests/architecture/test_layer_boundaries.py`, by `make arch`
and by CI — so a violating import fails a test, not a review.

Error-handling rule — `fastapi.HTTPException` may never be raised outside the
`presentation` layer. Business failures raise a `DomainError` subclass (Phase 2) and are
translated once, at a single exception-handling point, into an RFC 9457
`application/problem+json` response.

Quality-gate rule — `make lint`, `make typecheck`, `make arch` and `make test` must all be
green before any commit; pre-commit enforces the same set locally. Coverage is gated at
75% over `src/taskmanager` and the threshold must never be lowered, nor met with
`# pragma: no cover` or coverage `omit` entries. Add one sentence recording where each gate
runs: pre-commit is the developer-host gate and depends on `.venv` existing, while CI and
the Docker `test` stage run the same tools directly because they have no `.venv` — so a new
gate must be added in both places, never only in `.pre-commit-config.yaml`.

Configuration rule — no secret ever gets a default value in code; every setting is read
through `taskmanager.infrastructure.config.settings`, documented in `.env.example`, and
`.env` is never committed.

Language and attribution rules — everything is written in English (code, comments, docs,
commits and planning artifacts), and git commit messages carry NO Claude/AI co-author or
attribution trailer of any kind (D-20). State that AI involvement is documented
transparently in `AI_WORKFLOW.md` instead, which is the project's chosen form of honesty
about it.

Keep it tight and directive — bullet rules, not prose essays. Do not restate the whole
technology stack; the generated Stack section above already carries it.
  </action>
  <verify>
    <automated>grep -q '^## Project Rules' CLAUDE.md && grep -q 'HTTPException' CLAUDE.md && grep -q 'domain' CLAUDE.md && grep -q 'problem+json' CLAUDE.md && grep -q 'make arch' CLAUDE.md && grep -qi 'no.*attribution\|no Claude' CLAUDE.md && grep -qi 'English' CLAUDE.md && grep -q '75' CLAUDE.md && .venv/bin/python -c "
import re, pathlib
t = pathlib.Path('CLAUDE.md').read_text()
i = t.index('## Project Rules')
spans = [(m.start(), t.index('-end -->', m.start())) for m in re.finditer(r'<!-- GSD:[a-z-]+-start', t)]
assert not any(a < i < b for a, b in spans), 'Project Rules must sit outside GSD blocks'
print('placement OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains a `## Project Rules` heading located outside every `<!-- GSD:*-start -->` / `-end -->` block
    - The section names the layer order main, presentation, infrastructure, application, domain in that direction
    - It states that `taskmanager.domain` imports no third-party library and `taskmanager.application` no web framework or ORM
    - It contains the literal token `HTTPException` with a prohibition outside `presentation`
    - It names `problem+json` and the single exception-handling point
    - It names `make lint`, `make typecheck`, `make arch` and `make test` as the pre-commit gate set and the 75% coverage threshold
    - It records that pre-commit is the host-only gate (it needs `.venv`) while CI and the Docker test stage run the same tools directly, so a new gate must be wired in both places
    - It forbids lowering the threshold and forbids `pragma: no cover` / coverage `omit` as a way to reach it
    - It states the no-secret-default and `.env`-never-committed rules
    - It states English-only and that commits carry no Claude/AI attribution trailer
    - The stated layer order matches the `layers` contract in `.importlinter` exactly
  </acceptance_criteria>
  <done>`CLAUDE.md` carries a hand-maintained `## Project Rules` section that mirrors what the gates enforce, placed where regeneration cannot destroy it.</done>
</task>

<task type="auto">
  <name>Task 2: DECISION_LOG.md in ADR style</name>
  <files>DECISION_LOG.md</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-CONTEXT.md` decision D-19 (the exact list of decisions to record) plus D-07
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` sections "Open Questions (RESOLVED)" (items 1, 2, 3 and 4 — the `.importlinter` vs `pyproject.toml` call, the `up`/`down` placeholders, the repository timing, the `.env.example` variable set), "Pitfall 9: host Python 3.13 does not exist on this machine" (the 3.14-host / 3.13-authoritative skew to record in one line), "Alternatives Considered", "Pitfall 6" and "Assumptions Log" A1 (record the mirror-vs-local mypy hook choice and the venv-qualified entry that plan 04 had to add on top of it)
    - `.planning/phases/01-foundation-quality-gates/01-04-SUMMARY.md` (the recorded deviation from the RESEARCH pre-commit example)
    - `CLAUDE.md` Technology Stack section (the `Alternatives Considered` and `What NOT to Use` tables are the raw material for the options and consequences fields)
    - `.planning/ROADMAP.md` (the phase in which each decision takes effect)
  </read_first>
  <action>
Create `DECISION_LOG.md` at the repository root — a literal brief-mandated filename.

Open with a short English preamble stating the format (one ADR per decision: Context,
Options considered, Decision, Consequences), that the log is appended to as the project
progresses, and the date it was opened.

Then write one ADR block per decision, each with a stable id and a title, e.g.
`## ADR-001: PostgreSQL as the database`. Every block has exactly four labelled
subsections: **Context** (what forced the choice), **Options** (at least two real
alternatives, with the rejected one named), **Decision**, **Consequences** (including the
cost or risk accepted, not only the benefit). Record at minimum the decisions named in
D-19, one ADR each:

PostgreSQL over SQLite; Python 3.13 over 3.12 and over 3.14 as the target (and the
separate one-line note that the development host runs CPython 3.14 while Docker and CI run
3.13, with CI treated as authoritative — RESEARCH Pitfall 9); the `src/taskmanager/` src
layout with four layer packages; stdlib dataclass domain entities with Pydantic confined to
schema/DTO/settings boundaries; RFC 9457 `application/problem+json` as the error contract;
psycopg 3 with async SQLAlchemy 2.0 over asyncpg (the single-driver argument, including that
Alembic then stays synchronous); Alembic migrations over `Base.metadata.create_all()`; the
404-for-invisible / 403-for-visible-but-forbidden visibility rule; completion percentage
computed over the whole list via a single SQL aggregate and unaffected by filters; flake8
rather than ruff because the brief mandates it — naming ruff explicitly as what would be
chosen absent the constraint (D-07); plain pip with exact `==` pins over poetry/uv;
pytest-asyncio with `asyncio_default_fixture_loop_scope = function`; `postgres:18-alpine`
as the pinned database image.

Add four more ADRs for the choices this phase itself settled: `.importlinter` as a
root-level INI file rather than `[tool.importlinter]` in `pyproject.toml` (noting the two
forms are functionally equivalent and that discoverability decided it); mypy and
`lint-imports` as `repo: local` pre-commit hooks with `language: system` rather than
mirror repos (noting the `additional_dependencies` drift the mirror would create); the
architecture contract executed through import-linter's Python API because
`python -m importlinter.cli` always exits 0; and `make up` / `make down` shipping as honest
placeholders until compose arrives in Phase 3.

The `repo: local` hooks ADR carries one extra consequence that must be written down
explicitly, because it shapes two other files. The hook entries are `.venv/bin/mypy` and
`.venv/bin/lint-imports`, not bare tool names: pre-commit never activates a virtualenv and
this host has no project tools on `PATH` outside `.venv`, so a bare entry dies with
"command not found". The accepted cost is that `.pre-commit-config.yaml` now only works
where `.venv` exists — the developer host. Therefore CI and the Docker `test` stage never
invoke `pre-commit`: `.github/workflows/ci.yml` runs black, isort, flake8, mypy,
`lint-imports` and pytest as its own steps, and the container's gate is `pytest`. Record
the maintenance consequence in the same Consequences subsection: a new gate must be added
in both `.pre-commit-config.yaml` and `ci.yml`, because neither derives from the other.

Two hard rules. First, every ADR must be a decision that was actually made — do not invent
alternatives that were never on the table, and do not soften a rejected option into a
strawman. Second, record consequences honestly, including the ones that cost something
(for example: pinning nine dependencies that Phase 1 never imports; a 3.14 host that could
diverge from 3.13 CI under `filterwarnings = error`; the GitHub Actions workflow having
never executed on a real runner before the first push; the duplicated gate list between
pre-commit and CI).

All English. Keep each ADR to roughly one screen.

After writing the file, run `.venv/bin/pre-commit run --all-files` twice. Adding a new
Markdown file frequently trips `end-of-file-fixer` or `trailing-whitespace`, so the first
run is allowed to modify the file and exit non-zero; re-stage what it fixed and run again.
The second run must exit 0 with every hook `Passed`, and `git diff --quiet` must exit 0
afterwards — the same run-twice discipline plans 04 and 08 use.
  </action>
  <verify>
    <automated>test -f DECISION_LOG.md && test "$(grep -c '^## ADR-' DECISION_LOG.md)" -ge 17 && test "$(grep -ci '^\*\*Context\*\*\|^### Context' DECISION_LOG.md)" -ge 17 && test "$(grep -ci '^\*\*Consequences\*\*\|^### Consequences' DECISION_LOG.md)" -ge 17 && grep -qi 'ruff' DECISION_LOG.md && grep -q 'RFC 9457' DECISION_LOG.md && grep -q 'psycopg' DECISION_LOG.md && grep -q 'postgres:18-alpine' DECISION_LOG.md && grep -q 'asyncio_default_fixture_loop_scope' DECISION_LOG.md && grep -q 'importlinter' DECISION_LOG.md && grep -q '3.14' DECISION_LOG.md && grep -q '\.venv/bin/lint-imports' DECISION_LOG.md && grep -q 'pre-commit' DECISION_LOG.md && { .venv/bin/pre-commit run --all-files || true; } && .venv/bin/pre-commit run --all-files && git diff --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `DECISION_LOG.md` exists at the repository root with a preamble naming the ADR format and the opening date
    - It contains at least 17 `## ADR-` headings with sequential ids
    - Every ADR has all four labelled subsections Context, Options, Decision, Consequences
    - Every ADR's Options subsection names at least one concretely rejected alternative
    - The log contains ADRs covering: PostgreSQL, Python 3.13, src layout, dataclass domain plus Pydantic boundaries, RFC 9457, psycopg 3 with async SQLAlchemy, Alembic, the 404/403 visibility rule, whole-list completion percentage, flake8 over ruff, pip exact pins, pytest-asyncio loop scope, `postgres:18-alpine`
    - It additionally contains ADRs for `.importlinter` as an INI file, the local-vs-mirror pre-commit hooks for mypy and lint-imports, the import-linter Python API choice, and the `up`/`down` placeholders
    - The `repo: local` hooks ADR names the literal entries `.venv/bin/mypy` and `.venv/bin/lint-imports`, states that pre-commit activates no virtualenv, and records as an accepted consequence that CI and the Docker test stage run the gates directly and never invoke `pre-commit`
    - That same ADR records the maintenance cost: a new gate must be wired into both `.pre-commit-config.yaml` and `.github/workflows/ci.yml`
    - `ruff` is named explicitly as the tool that would be chosen absent the brief's flake8 mandate
    - The Python 3.14 host versus 3.13 CI skew is recorded, with CI named as authoritative
    - At least three Consequences subsections record an accepted cost or risk, not only benefits
    - `.venv/bin/pre-commit run --all-files` is run twice: the first run may fix formatting in the new file, the second exits 0 and `git diff --quiet` exits 0 afterwards
  </acceptance_criteria>
  <done>`DECISION_LOG.md` exists in ADR style covering every decision locked so far, including the four settled by this phase and the venv-qualified hook consequence, with honest consequences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented rule -> enforced gate | A rule written in `CLAUDE.md` that no gate enforces is a false assurance to a reviewer |
| Repository -> public audience | Both files ship publicly at delivery and must contain no credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-32 | Repudiation | `CLAUDE.md` layer rules drifting from `.importlinter` | mitigate | Task 1 acceptance criteria require the documented layer order to match the `layers` contract exactly, and `read_first` makes `.importlinter` the source |
| T-01-33 | Information Disclosure | a real secret pasted into a documentation example | mitigate | Both files reference `.env.example` placeholders only; pre-commit `detect-private-key` runs over them |
| T-01-34 | Repudiation | a decision log that rationalises rather than records | mitigate | Every ADR must name a concretely rejected alternative and at least three must record an accepted cost; invented alternatives are explicitly forbidden |
| T-01-35 | Tampering | `## Project Rules` deleted by a `CLAUDE.md` regeneration | mitigate | The section is placed outside every GSD delimiter block and a script asserts the placement |
| T-01-47 | Repudiation | the duplicated gate list between pre-commit and CI silently diverging, so one environment stops enforcing a gate | mitigate | The consequence is documented in both `CLAUDE.md` (Task 1) and the `repo: local` hooks ADR (Task 2) as a standing maintenance rule: add every new gate in both files |
</threat_model>

<verification>
1. `grep -q '^## Project Rules' CLAUDE.md && grep -q 'HTTPException' CLAUDE.md` — exit 0 (AIW-04)
2. The placement script in Task 1 prints `placement OK`
3. `test "$(grep -c '^## ADR-' DECISION_LOG.md)" -ge 17` — exit 0 (D-19)
4. `grep -qi ruff DECISION_LOG.md` — exit 0 (D-07)
5. `grep -q '\.venv/bin/lint-imports' DECISION_LOG.md` — exit 0 (the plan-04 deviation is recorded)
6. `make lint && make typecheck && make arch && make test` — all exit 0 (FND-09, unchanged by documentation)
7. `.venv/bin/pre-commit run --all-files` twice, then `git diff --quiet` — exit 0
</verification>

<success_criteria>
- `CLAUDE.md` holds the architecture and quality rules the gates enforce, in a section regeneration cannot destroy (AIW-04, D-17)
- The English-only and no-AI-attribution rules are written down as project rules (D-20)
- `DECISION_LOG.md` exists in ADR style covering every decision locked so far (D-19)
- The flake8-over-ruff decision is recorded with ruff named as the unconstrained choice (D-07)
- The venv-qualified local hook entries and the resulting "CI runs the gates directly" split are recorded as an ADR with its maintenance cost (D-11, D-13)
- The host/CI Python skew and other accepted costs are recorded honestly
</success_criteria>

<output>
Create `.planning/phases/01-foundation-quality-gates/01-06-SUMMARY.md` when done.
</output>
