---
phase: 01-foundation-quality-gates
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/taskmanager/infrastructure/config/__init__.py
  - src/taskmanager/infrastructure/config/settings.py
  - src/taskmanager/main.py
  - .env.example
  - tests/unit/test_settings.py
  - tests/unit/test_app_factory.py
  - .planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt
autonomous: true
requirements: [FND-11, FND-06, FND-01, FND-07]
must_haves:
  truths:
    - "D-16: Settings are loaded from the environment by pydantic-settings, and neither database_url nor jwt_secret has any default value — a process missing either one refuses to start with a ValidationError"
    - "D-16: a jwt_secret shorter than 16 characters is rejected at boot, and .env.example documents every declared field with non-secret placeholders"
    - "D-02: main.py is the composition root exposing create_app(settings=None) -> FastAPI, importing downward into infrastructure only, with no module-level app instance and no __main__ block"
    - "D-09: the coverage gate passes honestly at 100% with no pragma: no cover and no omit entries, and the gate is demonstrated firing below 75%"
    - "D-06: mypy strict accepts the settings module because plugins = [pydantic.mypy] is loaded, with no type: ignore anywhere"
    - "D-01: the whole suite runs warning-free under filterwarnings = error on the host CPython 3.14 venv"
  artifacts:
    - path: "src/taskmanager/infrastructure/config/settings.py"
      provides: "pydantic-settings Settings class and cached get_settings()"
      exports: ["Settings", "get_settings"]
    - path: "src/taskmanager/main.py"
      provides: "create_app() composition root"
      exports: ["create_app"]
    - path: ".env.example"
      provides: "documentation of every settings field with placeholder values"
    - path: "tests/unit/test_settings.py"
      provides: "settings happy path, missing secret, short secret, cache, .env.example parity"
    - path: "tests/unit/test_app_factory.py"
      provides: "create_app title/version assertions"
    - path: ".planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt"
      provides: "captured terminal output of the coverage gate firing below 75%"
      contains: "Required test coverage of 75% not reached"
  key_links:
    - from: "src/taskmanager/main.py"
      to: "src/taskmanager/infrastructure/config/settings.py"
      via: "from taskmanager.infrastructure.config.settings import Settings, get_settings"
      pattern: "from taskmanager\\.infrastructure\\.config\\.settings import"
    - from: "tests/unit/test_settings.py"
      to: ".env.example"
      via: "parity test comparing Settings.model_fields against parsed keys"
      pattern: "model_fields"
---

<objective>
Create the only two real code modules Phase 1 ships — the pydantic-settings
configuration module and the FastAPI composition root — plus the six tests that make the
75% coverage gate pass honestly, and then prove the gate actually fires below threshold.

Purpose: a coverage gate on an empty codebase is theatre. Two real modules at 100% with
value-asserting tests make the number mean something, and the recorded red demonstration
is the evidence that the gate is not a no-op.
Output: `settings.py`, `main.py`, `.env.example`, two unit test modules, and a captured
evidence file of the gate failing below 75%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-quality-gates/01-CONTEXT.md
@.planning/phases/01-foundation-quality-gates/01-RESEARCH.md
@.planning/phases/01-foundation-quality-gates/01-VALIDATION.md
@.planning/phases/01-foundation-quality-gates/01-01-SUMMARY.md

<interfaces>
Contracts this plan creates, consumed by Phase 3+ and by the tests in this plan.
Implement exactly these signatures — see RESEARCH "Code Examples -> settings.py" and
"Pattern 4" for the verified bodies.

taskmanager.infrastructure.config.settings:
  class Settings(BaseSettings)
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8",
                                      extra="forbid", frozen=True)
    app_name: str            # default "Task Manager API"
    environment: str         # default "local"
    database_url: str        # NO default
    jwt_secret: str          # NO default, Field(min_length=16)
    jwt_algorithm: str       # default "HS256"
    jwt_expire_minutes: int  # Field(default=30, gt=0)
  get_settings() -> Settings          # decorated @lru_cache(maxsize=1)

taskmanager.main:
  create_app(settings: Settings | None = None) -> FastAPI
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings module, composition root and .env.example</name>
  <files>src/taskmanager/infrastructure/config/__init__.py, src/taskmanager/infrastructure/config/settings.py, src/taskmanager/main.py, .env.example</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` sections "Code Examples -> src/taskmanager/infrastructure/config/settings.py", "Pattern 4: the composition root that does not need a database", "Pitfall 4: mypy --strict over pydantic-settings without the plugin", "Open Questions -> 4. Which .env.example variables exist in Phase 1?", "Security Domain -> Known Threat Patterns"
    - `.planning/phases/01-foundation-quality-gates/01-CONTEXT.md` decisions D-16 and the Phase Boundary block (no routers in this phase)
    - `pyproject.toml` (the `[tool.mypy]` table — the pydantic plugin is already configured)
  </read_first>
  <behavior>
    - `Settings(_env_file=None)` with `DATABASE_URL` and a 32-character `JWT_SECRET` in the environment returns those values and `jwt_expire_minutes == 30`
    - `Settings(_env_file=None)` with `JWT_SECRET` absent raises `pydantic.ValidationError`
    - `Settings(_env_file=None)` with `JWT_SECRET="short"` raises `pydantic.ValidationError`
    - `get_settings()` returns the identical object on two consecutive calls
    - `create_app(Settings(_env_file=None))` returns a `FastAPI` whose `title` is `"Task Manager API"` and whose `openapi()["info"]["version"]` is `"0.1.0"`
    - every key in `.env.example` corresponds to an upper-cased `Settings.model_fields` name, and every field is documented
  </behavior>
  <action>
Create `src/taskmanager/infrastructure/config/__init__.py` (empty) and
`src/taskmanager/infrastructure/config/settings.py` with an English module docstring, a
`Settings(BaseSettings)` class and an `@lru_cache(maxsize=1)`-decorated
`get_settings() -> Settings`. Field set and defaults exactly as listed in the
`<interfaces>` block above. `database_url` and `jwt_secret` carry NO default — that is the
security point of D-16: a misconfigured process fails at boot, not at the first request.
`jwt_secret` uses `pydantic.Field(min_length=16)`; `jwt_expire_minutes` uses
`Field(default=30, gt=0)`. `model_config` is a `SettingsConfigDict` with
`env_file=".env"`, `env_file_encoding="utf-8"`, `extra="forbid"` and `frozen=True`. Do
NOT add `# type: ignore[call-arg]` to the `Settings()` call inside `get_settings` — with
`plugins = ["pydantic.mypy"]` loaded that comment becomes an `unused-ignore` error under
`strict = true` (RESEARCH Pitfall 4).

Create `src/taskmanager/main.py` with an English module docstring and
`create_app(settings: Settings | None = None) -> FastAPI` which resolves
`settings or get_settings()` and returns a `FastAPI` built with `title` from the resolved
settings' `app_name`, `version="0.1.0"` and `openapi_url="/openapi.json"`. Three hard
constraints: no module-level `app = create_app()` (it would evaluate settings at import
time and crash mypy and import-linter in any environment without `JWT_SECRET`); no
`if __name__ == "__main__":` block (it would be a permanently uncovered statement — the
container runs `uvicorn --factory taskmanager.main:create_app`); and no router, no
`/health` route, no database import (CONTEXT Phase Boundary defers all of that to
Phase 3). The optional `settings` parameter is the seam Phase 3 uses to inject a test
engine — keep it.

Create `.env.example` documenting all six fields as upper-cased keys with non-secret
placeholder values and a short English comment each: `APP_NAME`, `ENVIRONMENT`,
`DATABASE_URL`, `JWT_SECRET`, `JWT_ALGORITHM`, `JWT_EXPIRE_MINUTES`. Use an obviously
fake `JWT_SECRET` placeholder at least 16 characters long, and a `DATABASE_URL` in
`postgresql+psycopg://` form. `DATABASE_URL` is declared now even though nothing consumes
it until Phase 3 — that is what lets the CI job export it and lets Phase 3 add
repositories without touching settings.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib; from taskmanager.infrastructure.config.settings import Settings, get_settings; from taskmanager.main import create_app; f=Settings.model_fields; assert f['database_url'].is_required(); assert f['jwt_secret'].is_required(); keys={l.split('=',1)[0].strip() for l in pathlib.Path('.env.example').read_text().splitlines() if '=' in l and not l.strip().startswith('#')}; assert keys=={k.upper() for k in f}, (keys, set(f))" && ! grep -rq 'type: ignore' src/taskmanager/ && ! grep -q '__main__' src/taskmanager/main.py && .venv/bin/mypy src && .venv/bin/flake8 src</automated>
  </verify>
  <acceptance_criteria>
    - `Settings.model_fields["database_url"].is_required()` is `True` and the same for `jwt_secret`
    - `Settings.model_fields` has exactly the six keys `app_name`, `environment`, `database_url`, `jwt_secret`, `jwt_algorithm`, `jwt_expire_minutes`
    - `settings.py` contains `extra="forbid"` and `frozen=True` inside `SettingsConfigDict`
    - `get_settings` is decorated with `@lru_cache(maxsize=1)` and `get_settings.cache_clear` is callable
    - `src/taskmanager/main.py` defines `create_app` with signature `(settings: Settings | None = None) -> FastAPI`
    - `grep -c '__main__' src/taskmanager/main.py` returns 0
    - `grep -rc 'type: ignore' src/taskmanager/` finds no matches
    - `grep -rc 'pragma: no cover' src/taskmanager/` finds no matches
    - `grep -cE 'APIRouter|include_router|/health' src/taskmanager/main.py` returns 0
    - The set of non-comment `KEY=` names in `.env.example` equals the upper-cased `Settings.model_fields` keys
    - `.venv/bin/mypy src` exits 0 and `.venv/bin/flake8 src` exits 0
  </acceptance_criteria>
  <done>`settings.py` and `main.py` exist and pass mypy strict with no ignores; `.env.example` documents all six fields; no routers, no module-level app, no `__main__` block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The six tests that make 75% honest</name>
  <files>tests/unit/test_settings.py, tests/unit/test_app_factory.py</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` sections "Code Examples -> tests/unit/test_settings.py", "Pattern 3: an honest 75 % on a codebase with no features", "Pitfall 5: filterwarnings = error does not police coverage misconfiguration"
    - `src/taskmanager/infrastructure/config/settings.py` and `src/taskmanager/main.py` (created in Task 1)
    - `pytest.ini` (the addopts that gate this run)
  </read_first>
  <behavior>
    - `test_settings_read_from_environment`: with `DATABASE_URL` and a 32-char `JWT_SECRET` monkeypatched, `Settings(_env_file=None).database_url` equals the patched value and `jwt_expire_minutes == 30`
    - `test_missing_secret_is_rejected`: `JWT_SECRET` deleted -> `pytest.raises(ValidationError)`
    - `test_short_secret_is_rejected`: `JWT_SECRET="short"` -> `pytest.raises(ValidationError)`
    - `test_get_settings_is_cached`: `get_settings() is get_settings()` after `cache_clear()`, and `cache_clear()` again afterwards
    - `test_env_example_documents_every_field`: upper-cased `Settings.model_fields` keys equal the keys parsed from `.env.example`
    - `test_create_app_uses_settings`: `create_app(Settings(_env_file=None)).title == "Task Manager API"` and `openapi()["info"]["version"] == "0.1.0"`
  </behavior>
  <action>
Create `tests/unit/test_settings.py` with the five settings-scoped tests named in
`<behavior>` and `tests/unit/test_app_factory.py` with `test_create_app_uses_settings`.
Use `pytest.MonkeyPatch` as an annotated fixture parameter (`monkeypatch:
pytest.MonkeyPatch`) so mypy strict passes over `tests/` too; annotate every test
function `-> None`.

Two non-negotiable details from RESEARCH. First, every `Settings(...)` construction in
tests passes `_env_file=None`; without it a developer's real `.env` leaks into the test
process and the "missing secret" and "short secret" cases stop failing — the tests would
pass while proving nothing. Second, `test_get_settings_is_cached` calls
`get_settings.cache_clear()` both before and after its assertion so the `lru_cache` does
not leak a Settings object built from one test's environment into a later test.

Every test asserts on a value, never merely that no exception was raised. Do not add
`# pragma: no cover` anywhere, do not add `omit` entries to the coverage config, and do
not lower `--cov-fail-under` — reaching 75% by weakening the gate is the one
unrecoverable mistake of this phase.

Then run the full host gate and read the coverage table manually once: confirm the listed
files match `find src -name '*.py'` and that no line starting with `Coverage.py warning:`
or `CoverageWarning` appears. Coverage's own warnings are emitted at session teardown
outside pytest's capture scope, so `filterwarnings = error` does NOT catch a partially
wrong `source` (RESEARCH Pitfall 5) — this manual read is the only defence.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q > /tmp/p1p2-pytest.txt 2>&1; rc=$?; tail -20 /tmp/p1p2-pytest.txt; test $rc -eq 0 && grep -q 'Required test coverage of 75% reached' /tmp/p1p2-pytest.txt && ! grep -qiE 'coverage(\.py)? ?warning' /tmp/p1p2-pytest.txt && .venv/bin/mypy src tests && .venv/bin/flake8 src tests && .venv/bin/black --check src tests && .venv/bin/isort --check-only src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest` exits 0 and collects exactly 6 tests
    - pytest output contains `Required test coverage of 75% reached` and a total coverage of 100%
    - The coverage table lists every file returned by `find src -name '*.py'` and nothing else
    - pytest output contains no line matching `Coverage.py warning` or `CoverageWarning`
    - pytest output contains no warnings summary (`filterwarnings = error` is active)
    - `grep -rc 'pragma: no cover' src tests` finds no matches
    - `grep -c '_env_file=None' tests/unit/test_settings.py` is at least 4
    - `grep -c 'cache_clear' tests/unit/test_settings.py` is at least 2
    - `.venv/bin/mypy src tests` exits 0
    - `--cov-fail-under=75` in `pytest.ini` is unchanged from plan 01
  </acceptance_criteria>
  <done>Six value-asserting tests pass, total coverage is 100% over the real package, and the gate is satisfied without any pragma, omit or threshold change.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the coverage gate fires below 75% and record the evidence</name>
  <files>.planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` section "Pattern 3: an honest 75 % on a codebase with no features" (the verified 56.25% measurement and the exact failure string)
    - `.planning/phases/01-foundation-quality-gates/01-VALIDATION.md` section "Manual-Only Verifications", the FND-06 row
    - `pytest.ini`
  </read_first>
  <action>
Demonstrate the coverage gate is real, capture the output, and remove the violation.

Create a temporary module `src/taskmanager/infrastructure/_coverage_probe.py` containing
roughly a dozen trivially uncovered statements (one or two small pure-Python functions
with several branches) that NO test imports. It must be valid, black/isort/flake8-clean
Python so that the coverage gate is the only thing that fails.

Run `.venv/bin/pytest` and capture the complete stdout+stderr into
`.planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt`. The run
must exit non-zero and the captured text must contain
`Required test coverage of 75% not reached`, with a total well below 75% (RESEARCH
measured 56.25% with a 12-statement probe). The probe appearing in the denominator at all
is the direct payoff of `source = ["taskmanager"]` being the package name rather than a
path — state that in one English sentence at the top of the evidence file, together with
the date and the exact command used.

Then delete `src/taskmanager/infrastructure/_coverage_probe.py` and re-run
`.venv/bin/pytest`: it must exit 0 with `Required test coverage of 75% reached` again.
Append that green line to the same evidence file so red and green sit side by side.

The probe file must NOT survive this task. Verify with a filesystem check and with
`git status --porcelain`, which must show no `_coverage_probe` entry. Plan 07 embeds this
evidence file into `AI_WORKFLOW.md`; do not touch `AI_WORKFLOW.md` here.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt && grep -q 'Required test coverage of 75% not reached' .planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt && grep -q 'Required test coverage of 75% reached' .planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt && ! test -e src/taskmanager/infrastructure/_coverage_probe.py && ! git status --porcelain | grep -q 'coverage_probe' && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt` exists
    - It contains the literal string `Required test coverage of 75% not reached`
    - It contains a reported total coverage below 75%
    - It also contains a `Required test coverage of 75% reached` line from the post-removal run
    - It names the date and the exact command used
    - `src/taskmanager/infrastructure/_coverage_probe.py` does not exist
    - `git status --porcelain` contains no `coverage_probe` entry
    - `.venv/bin/pytest` exits 0 after the probe removal
    - `AI_WORKFLOW.md` was not created or modified by this task
  </acceptance_criteria>
  <done>The coverage gate has been observed failing below 75% and passing again, with both outputs captured as committed evidence and the probe module gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process environment -> application | `DATABASE_URL` and `JWT_SECRET` cross here and are the only untrusted input in Phase 1 |
| Developer `.env` -> test process | A real `.env` leaking into tests would invalidate the negative-path assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Spoofing / Elevation of Privilege | `Settings.jwt_secret` | mitigate | No default value at all plus `Field(min_length=16)`; two tests assert `ValidationError` on missing and on short secrets (Task 1, Task 2). ASVS V13 Configuration |
| T-01-07 | Information Disclosure | `.env.example` | mitigate | Only obviously fake placeholder values; the real `.env` is git-ignored by plan 01 and is never read by tests because every construction passes `_env_file=None` |
| T-01-08 | Tampering | unexpected environment variables silently accepted | mitigate | `extra="forbid"` in `SettingsConfigDict` makes an unknown variable a boot-time `ValidationError` rather than a silent no-op |
| T-01-09 | Tampering | settings mutated at runtime | mitigate | `frozen=True` on the model config |
| T-01-10 | Repudiation | a coverage gate that reports green while measuring nothing | mitigate | Task 3 observes the gate firing below threshold and commits the captured output as evidence; the coverage table is read manually once (RESEARCH Pitfall 5 — `filterwarnings = error` does not catch a wrong `source`) |
| T-01-11 | Information Disclosure | settings evaluated at import time crashing with the secret in a traceback | mitigate | `create_app` is a factory; no module-level `app = create_app()` exists |
</threat_model>

<verification>
1. `.venv/bin/pytest` — exit 0, 6 tests, 100% total, `Required test coverage of 75% reached`
2. `.venv/bin/mypy src tests` — exit 0 (proves the pydantic plugin is doing its job)
3. `.venv/bin/flake8 src tests && .venv/bin/black --check src tests && .venv/bin/isort --check-only src tests` — exit 0
4. `.venv/bin/python -c "import taskmanager.main"` — exit 0 even with no `JWT_SECRET` in the environment (proves no import-time settings evaluation)
5. `grep -rc 'pragma: no cover\|type: ignore' src tests` — no matches
6. `grep -q 'Required test coverage of 75% not reached' .planning/phases/01-foundation-quality-gates/evidence/coverage-gate-red.txt` — exit 0
</verification>

<success_criteria>
- Settings load from the environment with no default for either secret-bearing field (FND-11)
- `.env.example` documents every declared field and a test enforces that parity (FND-11)
- Coverage is 100% over the real package and the 75% gate is satisfied honestly (FND-06)
- The gate has been observed failing below 75% and the output is committed as evidence (FND-06)
- mypy strict passes over `src` and `tests` with no ignores (FND-07)
- `taskmanager.main` is importable without any environment configuration (FND-01)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-quality-gates/01-02-SUMMARY.md` when done.
</output>
