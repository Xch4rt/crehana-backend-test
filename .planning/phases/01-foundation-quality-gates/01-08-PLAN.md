---
phase: 01-foundation-quality-gates
plan: 08
type: execute
wave: 6
depends_on: ["01-06", "01-07"]
files_modified:
  - .planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt
autonomous: false
requirements: [FND-09, FND-10, DOCK-04]
user_setup:
  - service: github
    why: "FND-10 requires a push to GitHub to trigger a CI run; the repository is outward-facing and its name and visibility are the user's call"
    env_vars: []
    dashboard_config:
      - task: "Confirm the repository name and that it should be public, or decline and defer to Phase 7"
        location: "GitHub (the gh CLI performs the creation once the user confirms)"
must_haves:
  truths:
    - "D-12: make format, make lint, make typecheck, make arch and make test all run green on the finished phase in one sitting, with make format a proven no-op on the clean tree"
    - "D-15: make docker-test runs the whole suite on Python 3.13 inside the image, with the host venv irrelevant to the result"
    - "D-11: pre-commit run --all-files passes twice with no file modified on the second run, including from a shell with no activated virtualenv"
    - "D-16 and D-20: no .env, no credential and no AI attribution trailer exists anywhere in the repository history"
    - "D-13: either the public GitHub repository exists with a green CI run, or the user declined and ci.yml ships committed with every local gate green, with the green-CI evidence explicitly carried to Phase 7"
  artifacts:
    - path: ".planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt"
      provides: "captured output of the complete phase gate run"
      contains: "Required test coverage of 75% reached"
  key_links:
    - from: "Makefile targets"
      to: "every gate in the phase"
      via: "a single uninterrupted run of format, lint, typecheck, arch, test and docker-test"
      pattern: "make (format|lint|typecheck|arch|test|docker-test)"
---

<objective>
Run the complete Phase 1 gate end to end on the finished repository, capture the output as
evidence, and then — only with the user's explicit confirmation — create the public GitHub
repository and push so CI runs for real.

Purpose: every prior plan verified its own slice. This plan verifies the assembled whole,
and handles the one outward-facing, irreversible action of the phase behind a human
checkpoint.
Output: a captured phase-gate evidence file, and either a green CI run on a real
repository or a documented deferral to Phase 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-quality-gates/01-CONTEXT.md
@.planning/phases/01-foundation-quality-gates/01-RESEARCH.md
@.planning/phases/01-foundation-quality-gates/01-VALIDATION.md
@.planning/phases/01-foundation-quality-gates/01-06-SUMMARY.md
@.planning/phases/01-foundation-quality-gates/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full phase gate run and secret audit</name>
  <files>.planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-VALIDATION.md` sections "Sampling Rate" (the before-verify-work requirement) and the full "Per-Task Verification Map" table — every row's command must be represented in this run
    - `.planning/ROADMAP.md` Phase 1 success criteria (criterion 1 names `make format` explicitly, so this run must execute it)
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` section "Security Domain -> Known Threat Patterns" (the `.env` and image-layer rows)
    - `Makefile`, `.pre-commit-config.yaml`, `Dockerfile`, `.github/workflows/ci.yml`
  </read_first>
  <action>
Run the complete phase gate in one sitting and capture all output into
`.planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt`, headed with the
date and the host details (interpreter version, Docker version, Make version).

Host gate: `make format`, `make lint`, `make typecheck`, `make arch`, `make test` — each
must exit 0, and the `make test` output must show `Required test coverage of 75% reached`
with the full coverage table. Run `make format` FIRST and assert `git diff --quiet` exits 0
immediately afterwards: the tree is already formatted, so the ROADMAP's named `format`
target must be a proven no-op rather than a target nobody ever runs. If `make format`
rewrites anything at this point, an earlier plan's gate did not actually hold — stop and fix
that, do not commit the reformat as the evidence.

Dockerized gate: `make docker-test` — must exit 0 and report the same coverage gate
reached, this time on Python 3.13 inside the image. This is the DOCK-04 acceptance in its
documented one-command form; a host with no Python at all would produce the same result.

Stability gate: `.venv/bin/pre-commit run --all-files` twice; the second run must report
every hook passed and `git diff --quiet` must exit 0 afterwards. Then repeat the run once
under a stripped environment —
`env -i HOME="$HOME" PATH="/usr/bin:/bin:/usr/local/bin" .venv/bin/pre-commit run --all-files`
— to re-confirm plan 04's `.venv/bin/`-qualified local hook entries still resolve with no
activated virtualenv, which is the condition a real `git commit` runs under.

Requirement spot checks from the VALIDATION.md map that no earlier plan owns end to end:
`.venv/bin/pip install --dry-run -r requirements-dev.txt` (FND-02);
a grep proving `.flake8` has no bare `ignore =` or `exclude =` line (FND-03);
`.venv/bin/pytest --collect-only` output containing `configfile: pytest.ini` (FND-05);
`.venv/bin/lint-imports` exit 0 (ARC-03);
`AI_WORKFLOW.md` containing an `## Incident Log` heading and a `### 2026-` dated entry (AIW-03);
`CLAUDE.md` containing both `HTTPException` and `domain` (AIW-04);
a grep proving `pre-commit` appears in neither `.github/workflows/ci.yml` nor the
`Dockerfile`, since neither environment has a `.venv` for the local hooks to resolve against.

Secret and hygiene audit, all of which must come back clean: no `.env` path appears in
`git log --all --name-only`; no `.env` appears in `git ls-files`; a case-insensitive grep
of every commit body for `co-authored-by`, `claude`, `generated with` and `anthropic`
returns zero matches (D-20); `docker history taskmanager` has no case-insensitive `secret`
match; `git status --porcelain` is empty except for the evidence file being written.

If any check fails, fix the cause rather than the check, then re-run the whole sequence
from the top so the captured evidence reflects a single clean pass. Do not create the
GitHub repository in this task.
  </action>
  <verify>
    <automated>EV=.planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt; make format && git diff --quiet && make lint && make typecheck && make arch && make test && make docker-test && .venv/bin/pre-commit run --all-files && .venv/bin/pre-commit run --all-files && git diff --quiet && env -i HOME="$HOME" PATH="/usr/bin:/bin:/usr/local/bin" .venv/bin/pre-commit run --all-files && git diff --quiet && ! grep -qi 'pre-commit' .github/workflows/ci.yml && ! grep -qi 'pre-commit' Dockerfile && test -f "$EV" && grep -q 'Required test coverage of 75% reached' "$EV" && grep -q 'make format' "$EV" && test "$(git log --all --name-only 2>/dev/null | grep -c '^\.env$')" -eq 0 && test "$(git ls-files | grep -c '^\.env$')" -eq 0 && test "$(git log --all --format='%B' 2>/dev/null | grep -ciE 'co-authored-by|claude|generated with|anthropic')" -eq 0 && test "$(docker history taskmanager 2>/dev/null | grep -ci secret)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `make format` exits 0 and `git diff --quiet` exits 0 immediately afterwards, proving the ROADMAP-named target is exercised and is a no-op on the finished tree
    - `make lint`, `make typecheck`, `make arch` and `make test` each exit 0 in the same single uninterrupted run
    - `make docker-test` exits 0 and its output shows `Required test coverage of 75% reached`
    - `.venv/bin/pre-commit run --all-files` exits 0 twice in a row and `git diff --quiet` exits 0 afterwards
    - `env -i HOME="$HOME" PATH="/usr/bin:/bin:/usr/local/bin" .venv/bin/pre-commit run --all-files` exits 0, re-confirming the local hooks resolve with no activated virtualenv
    - `grep -ci 'pre-commit' .github/workflows/ci.yml` and `grep -ci 'pre-commit' Dockerfile` both return 0
    - `.venv/bin/pip install --dry-run -r requirements-dev.txt` exits 0
    - `.venv/bin/pytest --collect-only` output contains `configfile: pytest.ini`
    - `.venv/bin/lint-imports` exits 0
    - `.flake8` matches neither `^\s*ignore\s*=` nor `^\s*exclude\s*=`
    - `AI_WORKFLOW.md` contains both an `## Incident Log` heading and a `### 2026-` dated entry
    - `CLAUDE.md` contains both `HTTPException` and `domain`
    - `git log --all --name-only` contains no `.env` entry and `git ls-files` contains no `.env`
    - `git log --all --format='%B'` matches none of `co-authored-by`, `claude`, `generated with`, `anthropic` case-insensitively
    - `docker history taskmanager` contains no case-insensitive `secret` match
    - The evidence file is headed with the date and the host interpreter, Docker and Make versions, records the `make format` invocation, and contains the full coverage table
  </acceptance_criteria>
  <done>The entire Phase 1 gate — including `make format` and a venv-less pre-commit run — has been observed green in one sitting on both the host and inside the image, with a clean secret and attribution audit, and the output is committed as evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Create the public GitHub repository and push (requires user confirmation)</name>
  <files>(no repository file changes — remote creation and push only)</files>
  <read_first>
    - `.planning/phases/01-foundation-quality-gates/01-RESEARCH.md` section "Open Questions (RESOLVED) -> 3. Should Phase 1 create the public GitHub repository, or only the workflow file?" and "Assumptions Log" entry A2 (the workflow has never run on a real runner; budget one fix-up commit)
    - `.planning/phases/01-foundation-quality-gates/01-VALIDATION.md` section "Manual-Only Verifications", the FND-10 row (including the documented fallback)
    - `.planning/phases/01-foundation-quality-gates/01-CONTEXT.md` "Claude's Discretion" — repository creation is outward-facing and requires explicit user confirmation
    - `.github/workflows/ci.yml`
  </read_first>
  <what-built>
    Everything Phase 1 delivers is complete and verified locally: the packaging skeleton
    and four layer packages, exact-pinned dependencies, `.flake8` and `pytest.ini`, the
    settings module and app factory with six passing tests at 100% coverage, the
    import-linter contracts running as tests (proven red on a violation and green when
    removed), pre-commit, the Makefile, the three-stage non-root Dockerfile, the CI
    workflow, `CLAUDE.md` project rules, `DECISION_LOG.md` and `AI_WORKFLOW.md`.

    The one thing that cannot be verified locally is FND-10's "a push to GitHub triggers a
    CI run that reports green" — it needs a real remote. Creating a public repository is
    outward-facing and irreversible enough that its name and visibility are the user's
    call, not an assumption.
  </what-built>
  <action>
STOP and ask the user before touching anything outward-facing. Do not create a repository,
do not add a remote and do not push until the user has answered.

Ask two questions explicitly: (1) create the public GitHub repository now, or defer it to
Phase 7? (2) if now, exactly what should the repository be named, and confirm it should be
public. Never infer the name from the directory name and never assume visibility.

On a create-now answer: run `gh auth status` first and stop with a request for
authentication if it fails — do not attempt an unauthenticated workaround. Then create the
repository with the `gh` CLI under the confirmed name and visibility, add it as the
`origin` remote, and push the `main` branch. Then watch the Actions run with
`gh run list --limit 1` and `gh run watch` and report the conclusion. The workflow has
never executed on a real runner (RESEARCH assumption A2), so one fix-up commit is expected
and budgeted: read the failing job log, make the minimal fix, commit it with a message
carrying no `Co-Authored-By`, `Claude`, `Generated with` or `Anthropic` line (D-20), push,
and re-check until the run concludes `success`. If a fix-up commit changes any tracked file,
re-run `make format && git diff --quiet` and the two `pre-commit run --all-files` passes
locally before pushing it, so the local gate never goes stale behind CI.

On a defer answer: change nothing in the repository, create no remote, and record the
deferral in `.planning/STATE.md` under Deferred Items as a row naming the item (green CI
evidence on a real push, FND-10) and Phase 7 as the owner, since Phase 7 already owns
AIW-05. Report the phase complete under the documented fallback: `ci.yml` is committed and
every local gate is green in
`.planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt`.
  </action>
  <how-to-verify>
    1. Decide whether to create the repository now, or defer it to Phase 7.
    2. If creating now, state the repository name to use and confirm it should be public.
    3. On confirmation, the executor runs `gh auth status`, creates the repository under
       the confirmed name and visibility with the `gh` CLI, adds it as the `origin` remote
       and pushes `main`. If `gh auth status` fails, the executor stops and asks for
       authentication rather than guessing.
    4. The executor then polls the Actions run with `gh run list` / `gh run watch` and
       reports the conclusion. The workflow has never executed on a real runner; if it
       fails, the executor reads the job log, makes the minimal fix, re-runs the local gate
       (`make format`, then the two pre-commit passes), commits with a message carrying no
       AI attribution trailer, pushes, and re-checks until green.
    5. Confirm the run is green, or accept the fallback.

    FALLBACK if you decline or defer: the phase still completes. `.github/workflows/ci.yml`
    is committed, every local gate is green and captured in
    `.planning/phases/01-foundation-quality-gates/evidence/phase-gate.txt`, and the
    "green CI on a real push" evidence is carried explicitly to Phase 7, which already owns
    AIW-05 (public repo, green badge in the README). The executor records the deferral in
    `.planning/STATE.md` under Deferred Items so it cannot be lost.
  </how-to-verify>
  <verify>
    <human-check>The user has answered create-now (with a name and confirmed public visibility) or defer, and the executor reports either a `gh run list` conclusion of `success` or a deferral recorded in `.planning/STATE.md`.</human-check>
  </verify>
  <acceptance_criteria>
    - The user has explicitly answered create-now or defer
    - If create-now: the repository name and its public visibility were supplied by the user, not assumed
    - If create-now: `gh repo view --json name,visibility` reports the confirmed name and `PUBLIC`
    - If create-now: `git remote -v` shows `origin` pointing at that repository and `git push` succeeded
    - If create-now: `gh run list --limit 1` reports a `completed` run with conclusion `success`
    - If create-now: any fix-up commit needed to get CI green carries no `Co-Authored-By`, `Claude`, `Generated with` or `Anthropic` line, and the local gate was re-run green before it was pushed
    - If defer: no remote was created, and the deferral is recorded in `.planning/STATE.md` under Deferred Items naming Phase 7 as the owner
    - In both branches, `git log --all --format='%B'` still matches none of the AI attribution patterns
  </acceptance_criteria>
  <done>The user has explicitly decided; either a real push produced a green CI run, or the deferral is recorded with Phase 7 named as owner.</done>
  <resume-signal>Reply with the repository name and "public, create it now", or reply "defer to Phase 7".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private working tree -> public internet | This checkpoint is the moment everything becomes permanently public; a committed secret is unrecoverable |
| Local git history -> published history | Attribution trailers and stray files in history cannot be quietly removed after a public push |
| `gh` CLI credentials -> GitHub account | The executor acts with the user's account privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-40 | Information Disclosure | publishing a repository containing a secret | mitigate | Task 1's audit asserts `.env` appears in neither `git ls-files` nor `git log --all --name-only` and that `docker history` has no secret match, and it runs BEFORE the push checkpoint |
| T-01-41 | Repudiation | AI attribution trailers in published commit history | mitigate | Task 1 greps all commit bodies for `co-authored-by`, `claude`, `generated with` and `anthropic` and requires zero matches; the checkpoint repeats the check for any CI fix-up commit (D-20) |
| T-01-42 | Elevation of Privilege | creating a repository under the wrong account or wrong visibility | mitigate | Blocking human checkpoint: name and visibility come from the user, `gh auth status` is checked first, and the executor stops rather than guessing on an auth failure |
| T-01-43 | Tampering | CI reporting green because a gate step silently no-opped on the runner | mitigate | The first real run is inspected via `gh run list` / `gh run watch`; plan 05's YAML assertion already requires all six gate commands to be present and forbids a `pre-commit` step standing in for them |
| T-01-44 | Repudiation | a phase declared complete on gates that were never all green at once | mitigate | Task 1 captures a single uninterrupted run of the whole gate — `format`, `lint`, `typecheck`, `arch`, `test`, `docker-test` and two pre-commit passes — into a committed evidence file |
</threat_model>

<verification>
1. `make format && git diff --quiet` — exit 0 (ROADMAP criterion 1)
2. `make lint && make typecheck && make arch && make test` — exit 0 (FND-09)
3. `make docker-test` — exit 0, coverage gate reached on Python 3.13 (DOCK-04)
4. `.venv/bin/pre-commit run --all-files` twice, then `git diff --quiet` — exit 0 (FND-08)
5. `env -i HOME="$HOME" PATH="/usr/bin:/bin:/usr/local/bin" .venv/bin/pre-commit run --all-files` — exit 0
6. `! grep -qi 'pre-commit' .github/workflows/ci.yml Dockerfile` — exit 0
7. `git ls-files` and `git log --all --name-only` contain no `.env` entry
8. `git log --all --format='%B'` matches none of `co-authored-by`, `claude`, `generated with`, `anthropic`
9. If the repository was created: `gh run list --limit 1` reports conclusion `success` (FND-10)
10. If deferred: `.planning/STATE.md` Deferred Items names the green-CI evidence as Phase 7's
</verification>

<success_criteria>
- Every Phase 1 gate has been observed green in one uninterrupted run, on the host and inside the image, including the ROADMAP-named `make format` (FND-09, DOCK-04)
- The local hooks were proven to resolve from a shell with no activated virtualenv, and neither CI nor the image invokes `pre-commit`
- The repository contains no `.env`, no credential and no AI attribution trailer in any commit (D-16, D-20)
- The user has explicitly decided on repository creation; nothing outward-facing happened without confirmation
- Either a real push produced a green CI run (FND-10), or the deferral is recorded with Phase 7 named as owner
</success_criteria>

<output>
Create `.planning/phases/01-foundation-quality-gates/01-08-SUMMARY.md` when done.
</output>
