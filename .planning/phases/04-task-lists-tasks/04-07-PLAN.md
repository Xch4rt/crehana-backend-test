---
phase: 04-task-lists-tasks
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-03", "04-04"]
files_modified:
  - src/taskmanager/presentation/api/actor.py
  - src/taskmanager/presentation/api/dependencies.py
  - src/taskmanager/presentation/api/schemas/__init__.py
  - src/taskmanager/presentation/api/schemas/task_lists.py
  - src/taskmanager/presentation/api/schemas/tasks.py
  - tests/unit/presentation/test_actor.py
  - tests/unit/presentation/test_schemas.py
autonomous: true
requirements: [ARC-05, TASK-01, TASK-03, TASK-06]

must_haves:
  truths:
    - "A single presentation dependency, get_current_actor() -> UUID, is the only source of caller identity, exposed as the Annotated alias CurrentActor; its docstring states in those words that this is not authentication (D-01)"
    - "DEMO_USER_ID is a Final UUID constant in presentation/api/actor.py, not a setting, and .env.example gains no key (D-03)"
    - "Every HTTP boundary of the phase is typed by a Pydantic v2 model: request schemas, response schemas and the task-collection envelope (ARC-05)"
    - "PATCH schemas set extra=\"forbid\" and require at least one known field, so {} and an unknown key are both 422; an explicit null on a non-nullable field is a field-level 422 (D-05, D-06)"
    - "status is not a field of the task PATCH schema at all, so sending it is extra_forbidden at body.status (D-08, TASK-03)"
    - "No request schema repeats a business limit: no max_length, no past-date validator (Phase 2 D-04)"
  artifacts:
    - path: "src/taskmanager/presentation/api/actor.py"
      provides: "DEMO_USER_ID, get_current_actor, CurrentActor"
      contains: "DEMO_USER_ID"
    - path: "src/taskmanager/presentation/api/schemas/tasks.py"
      provides: "TaskCreateRequest, TaskPatchRequest, TaskStatusChangeRequest, TaskResponse, TaskCollectionResponse"
      contains: "extra=\"forbid\""
    - path: "src/taskmanager/presentation/api/schemas/task_lists.py"
      provides: "TaskListCreateRequest, TaskListPatchRequest, TaskListResponse"
  key_links:
    - from: "src/taskmanager/presentation/api/schemas/tasks.py"
      to: "src/taskmanager/application/dto/commands.py"
      via: "to_command() converts model_fields_set into the UNSET sentinel"
      pattern: "model_fields_set"
    - from: "src/taskmanager/presentation/api/schemas/tasks.py"
      to: "src/taskmanager/domain/entities/task.py"
      via: "the priority field default reads Task.DEFAULT_PRIORITY, the one copy of `medium`"
      pattern: "DEFAULT_PRIORITY"
---

<objective>
The presentation layer's two non-router halves: the caller-identity seam that Phase 5 will replace
with JWT, and the Pydantic models that type every HTTP boundary this phase crosses.

Purpose: ARC-05 is the requirement that Pydantic types the boundary and *only* the boundary. These
modules are where D-05, D-06 and D-08 become mechanical — the difference between "status is not
writable through PATCH" being a promise and being a 422 an evaluator can produce with one curl.
Output: `presentation/api/actor.py`, a `get_clock` provider, the `schemas/` package, and unit tests
covering every PATCH leg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-lists-tasks/04-CONTEXT.md
@.planning/phases/04-task-lists-tasks/04-RESEARCH.md
@.planning/phases/04-task-lists-tasks/04-PATTERNS.md
@.planning/phases/04-task-lists-tasks/04-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- The actor seam, 04-RESEARCH Pattern 5. -->

```python
DEMO_USER_ID: Final[UUID] = UUID("00000000-0000-4000-8000-00000000de00")


def get_current_actor() -> UUID: ...


CurrentActor = Annotated[UUID, Depends(get_current_actor)]
```

<!-- The PATCH schema shape, executed on fastapi 0.141.1 / pydantic 2.13.5
     (04-RESEARCH Pattern 2). The Unset sentinel must NOT appear here. -->

```python
class TaskPatchRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    title: str | None = None
    description: str | None = None
    priority: TaskPriority | None = None
    due_date: datetime | None = None

    @field_validator("title", "priority")
    @classmethod
    def _not_explicitly_null(cls, value: object, info: ValidationInfo) -> object: ...

    @model_validator(mode="after")
    def _at_least_one_field(self) -> Self: ...

    def to_command(self, *, actor_id, task_list_id, task_id) -> UpdateTaskCommand:
        sent = self.model_fields_set      # never `self.model_fields`
        ...
```

Measured behaviour that the unit tests must reproduce:

| Body | Status | `errors` entry |
|------|--------|----------------|
| `{}` | 422 | `body` / `value_error` |
| `{"titel": "x"}` | 422 | `body.titel` / `extra_forbidden` |
| `{"status": "completed"}` | 422 | `body.status` / `extra_forbidden` |
| `{"title": null}` | 422 | `body.title` / `value_error` |
| `{"description": null}` | ok | `model_fields_set == {"description"}`, value `None` |

<!-- Response envelope, D-09 -->

```python
class TaskCollectionResponse(BaseModel):
    items: list[TaskResponse]
    total_tasks: int
    completed_tasks: int
    completion_percentage: float
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The actor seam and the Clock provider</name>
  <files>src/taskmanager/presentation/api/actor.py, src/taskmanager/presentation/api/dependencies.py, tests/unit/presentation/test_actor.py</files>
  <read_first>
    - `src/taskmanager/presentation/api/health.py` — L38 (`APIRouter` construction), L45 (the `Final`
      module constant with its reasoning comment) and L47-L54 (the `Annotated` alias plus the
      B008/ADR-034 comment, which the new modules reference rather than re-litigate)
    - `src/taskmanager/presentation/api/dependencies.py` — the whole file: the module docstring
      L1-L18 (which explicitly anticipates this plan), `_resources` and `get_engine` L39-L41 for the
      simplest provider shape, and `get_uow` L49-L76 for the two rules a provider must not break —
      a plain `def`, never a `yield` dependency, and the **port** as the return annotation
    - `src/taskmanager/infrastructure/clock.py` — `SystemClock`, the runtime `Clock` adapter
    - `src/taskmanager/application/ports/clock.py` — the `Clock` Protocol, which is the return
      annotation `get_clock` must carry
    - `.planning/phases/04-task-lists-tasks/04-CONTEXT.md` D-01, D-03 and the `<specifics>` block —
      the seam must read honestly in the docs
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 5" — the verified body and the
      two reasons this is a separate module rather than an addition to `dependencies.py`
    - `tests/unit/infrastructure/test_clock.py` — the shape of a unit test for a provider-like
      module
  </read_first>
  <action>
    Create `src/taskmanager/presentation/api/actor.py` with `DEMO_USER_ID: Final[UUID]` set to the
    literal `00000000-0000-4000-8000-00000000de00`, a plain `def get_current_actor() -> UUID`
    returning it, and `CurrentActor = Annotated[UUID, Depends(get_current_actor)]`.

    The module docstring is the load-bearing part and must say, in those words, that this is **not
    authentication**: Phase 4 runs as a single demo user, and Phase 5 replaces only the body of this
    function with JWT decoding, so no router, schema or use case moves when authentication arrives.
    State the two reasons it is its own module rather than another provider in `dependencies.py` —
    the container entrypoint's seed imports `DEMO_USER_ID` and must not drag SQLAlchemy in to do it,
    and Phase 5 deletes one file instead of editing one. State why the id is a `Final` constant and
    not a setting (D-03): it is not configuration, nothing deploys differently because of it, and it
    disappears in Phase 5, so `.env.example` gains no key. Name the security consequence too: the
    seeded row's `password_hash` is a deliberately unverifiable placeholder, so this identity cannot
    become a live account.

    Add `get_clock() -> Clock` to `dependencies.py`, returning `SystemClock()`. A plain `def` with
    no `Request` parameter, annotated with the **port**, never `SystemClock` — a test overrides the
    provider rather than a signature. Add the two shared aliases beside it:
    `UnitOfWorkDependency = Annotated[UnitOfWork, Depends(get_uow)]` and
    `ClockDependency = Annotated[Clock, Depends(get_clock)]`, with a one-line comment pointing at
    `health.py` L47-L54 for the B008 argument rather than repeating it. Do not store the clock on
    `app.state`: it is stateless and building one per request costs nothing, so the narrowing
    `_resources` exists for does not apply.

    Create `tests/unit/presentation/test_actor.py`:
      - `get_current_actor()` returns `DEMO_USER_ID` and returns the same value twice.
      - `DEMO_USER_ID` is a `UUID` and its version is 4, so it cannot collide with a
        `uuid4()`-generated user in a way a reader would find surprising.
      - `CurrentActor`'s metadata is a `Depends` whose dependency is `get_current_actor` — inspect
        `typing.get_args(CurrentActor)`, so a later refactor that points the alias at a different
        provider fails here.
      - the module's source contains the words `not authentication`, asserted as a text scan the way
        the project's other documentation-as-contract checks work, so the honesty requirement
        survives an edit.
      - `get_clock()` returns an object satisfying the `Clock` port and a fresh instance per call.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/presentation -q --no-cov && .venv/bin/mypy src tests && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/presentation/test_actor.py -q --no-cov` exits 0 with at least 5 tests passing
    - `.venv/bin/python -c "from taskmanager.presentation.api.actor import DEMO_USER_ID, get_current_actor; assert get_current_actor() == DEMO_USER_ID and DEMO_USER_ID.version == 4"` exits 0
    - `.venv/bin/python -c "from taskmanager.presentation.api.dependencies import get_clock; from taskmanager.application.ports.clock import Clock; c = get_clock(); assert c.now().tzinfo is not None"` exits 0
    - `grep -ci "not authentication" src/taskmanager/presentation/api/actor.py` prints at least `1`
    - `grep -c "sqlalchemy" src/taskmanager/presentation/api/actor.py` prints `0`
    - `grep -c "DEMO_USER_ID" .env.example` prints `0`
    - `.venv/bin/pytest tests/unit/test_settings.py::test_env_example_documents_every_field -q --no-cov` exits 0
    - `.venv/bin/mypy src tests` exits 0
    - `.venv/bin/pytest tests/unit/presentation -q --cov=taskmanager.presentation.api.actor --cov-report=term-missing --no-header` shows no missing lines
  </acceptance_criteria>
  <done>Every router in this phase has one place to ask who is calling, the answer is honest about being a placeholder, and the use cases have a clock they can be handed.</done>
</task>

<task type="auto">
  <name>Task 2: Task-list request and response schemas</name>
  <files>src/taskmanager/presentation/api/schemas/__init__.py, src/taskmanager/presentation/api/schemas/task_lists.py, tests/unit/presentation/test_schemas.py</files>
  <read_first>
    - `src/taskmanager/presentation/api/health.py` L57-L68 — `HealthResponse`, the only Pydantic
      model in `src/` today, and its argument that **field order is contract, not decoration**
    - `src/taskmanager/presentation/api/errors/__init__.py` — the package-init shape to copy
    - `src/taskmanager/application/dto/commands.py` (plan 04-04) — `CreateTaskListCommand`,
      `UpdateTaskListCommand`
    - `src/taskmanager/application/dto/results.py` (plan 04-04) — `TaskListResult`'s exact field
      list, which `TaskListResponse` mirrors
    - `src/taskmanager/application/dto/unset.py` (plan 04-01) — `UNSET`, used **only** inside
      `to_command`, never as a field type
    - `src/taskmanager/domain/entities/task_list.py` — `NAME_MAX_LENGTH` and
      `DESCRIPTION_MAX_LENGTH`, which are the entity's and must **not** be repeated as
      `Field(max_length=...)`
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 2" — the verified schema shape
      and the measured behaviour table, and §"Anti-Patterns" for `instance.model_fields`
    - `.planning/phases/04-task-lists-tasks/04-PATTERNS.md` §9 — the two constraints taken from the
      existing code rather than from research
    - `pytest.ini` — `filterwarnings = error`, which is why `instance.model_fields` raises rather
      than warns
  </read_first>
  <action>
    Create `src/taskmanager/presentation/api/schemas/__init__.py` mirroring
    `presentation/api/errors/__init__.py`.

    Create `schemas/task_lists.py` with three models:
      - `TaskListCreateRequest` — `name: str`, `description: str | None = None`,
        `model_config = ConfigDict(extra="forbid")`, and
        `to_command(self, *, actor_id: UUID) -> CreateTaskListCommand`.
      - `TaskListPatchRequest` — `name: str | None = None`, `description: str | None = None`,
        `extra="forbid"`, a `@field_validator("name")` rejecting an explicit null with the message
        `this field may not be null` (it runs only for a value the client actually sent, because
        Pydantic skips field validators for defaults unless `validate_default` is set — that is
        precisely D-05's explicit-null leg and nothing else), a
        `@model_validator(mode="after")` refusing an empty `model_fields_set` with
        `at least one field must be provided` (D-06), and
        `to_command(self, *, actor_id: UUID, task_list_id: UUID) -> UpdateTaskListCommand` reading
        `sent = self.model_fields_set` and passing `self.<field> if "<field>" in sent else UNSET`.
      - `TaskListResponse` — `id`, `owner_id`, `name`, `description`, `created_at`, `updated_at`,
        `total_tasks`, `completed_tasks`, `completion_percentage`, declared in that order because
        Pydantic serialises in declaration order, plus
        `from_result(cls, result: TaskListResult) -> "TaskListResponse"` naming **every** field
        explicitly. Do not use `model_validate`: it works, but it silently accepts a new result
        field and silently ignores a forgotten one, which re-opens the automatic-leak hole the
        result DTO exists to close.

    Three prohibitions, each worth a comment: no `Field(max_length=...)` anywhere — the entity owns
    every limit (Phase 2 D-04); no `Unset` in any field annotation or default — it leaks a `_Unset`
    component into `/openapi.json` and splits the explicit-null error into two unreadable entries
    (measured, 04-RESEARCH Pattern 2); and never `self.model_fields` on an instance — deprecated
    since Pydantic 2.11 and therefore an exception under `filterwarnings = error`.

    Create `tests/unit/presentation/test_schemas.py` (extended again in Task 3) with, for the
    task-list schemas:
      - `set(TaskListPatchRequest.model_fields) == {"name", "description"}`, read off the **class**,
        which is the guard against the stringly typed mapper bug: `"nmae" in sent` would silently
        never update and no type checker would notice (04-RESEARCH Pitfall 7).
      - three legs per patchable field: omitted → `UNSET` on the command; provided → the value;
        explicit `null` → `None` for `description`, and a `ValidationError` for `name`.
      - `{}` raises a `ValidationError` mentioning at least one field must be provided.
      - an unknown key raises a `ValidationError` whose error `type` is `extra_forbidden`.
      - `TaskListCreateRequest` accepts a name with no description and refuses an unknown key.
      - `TaskListResponse.from_result` copies every field of a fully populated `TaskListResult`, and
        a test asserting `set(TaskListResponse.model_fields)` equals the expected nine names in the
        expected order, so the D-10 contract cannot drift.
      - a test asserting no field of either request model declares a `max_length` constraint, by
        inspecting `model_fields[...].metadata`, so Phase 2 D-04 is enforced rather than remembered.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/presentation/test_schemas.py -q --no-cov && .venv/bin/mypy src tests && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/presentation/test_schemas.py -q --no-cov` exits 0 with at least 12 tests passing
    - `.venv/bin/python -c "from taskmanager.presentation.api.schemas.task_lists import TaskListPatchRequest as P; assert set(P.model_fields) == {'name','description'}"` exits 0
    - `.venv/bin/python -c "from taskmanager.presentation.api.schemas.task_lists import TaskListPatchRequest as P; from taskmanager.application.dto.unset import UNSET; import uuid; c=P(description=None).to_command(actor_id=uuid.uuid4(), task_list_id=uuid.uuid4()); assert c.name is UNSET and c.description is None"` exits 0
    - `.venv/bin/python -c "import pydantic; from taskmanager.presentation.api.schemas.task_lists import TaskListPatchRequest as P;\nimport sys\ntry:\n    P()\nexcept pydantic.ValidationError:\n    sys.exit(0)\nsys.exit(1)"` exits 0
    - `grep -c "max_length" src/taskmanager/presentation/api/schemas/task_lists.py` prints `0`
    - `grep -c "Unset" src/taskmanager/presentation/api/schemas/task_lists.py` prints `1` — the `UNSET` import used inside `to_command` only
    - `.venv/bin/mypy src tests` exits 0
    - `.venv/bin/pytest tests/unit/presentation -q --cov=taskmanager.presentation.api.schemas --cov-report=term-missing --no-header` shows `task_lists.py` with no missing lines
  </acceptance_criteria>
  <done>The task-list HTTP boundary is typed by Pydantic, merge-patch semantics are mechanical, and no business limit exists in two layers.</done>
</task>

<task type="auto">
  <name>Task 3: Task request, response and collection schemas</name>
  <files>src/taskmanager/presentation/api/schemas/tasks.py, tests/unit/presentation/test_schemas.py</files>
  <read_first>
    - `src/taskmanager/presentation/api/schemas/task_lists.py` (Task 2) — the shape now established
      for this package
    - `src/taskmanager/application/dto/commands.py` (plan 04-04) — `CreateTaskCommand`,
      `UpdateTaskCommand` (which has no `status`), `ChangeTaskStatusCommand`
    - `src/taskmanager/application/dto/results.py` — `TaskResult`'s eleven fields and
      `TaskCollectionResult`
    - `src/taskmanager/domain/entities/task.py` — `Task.DEFAULT_PRIORITY` (plan 04-01), the single
      copy of `medium`
    - `src/taskmanager/domain/value_objects/task_status.py` and `task_priority.py` — the StrEnums
      that serialise as their values
    - `.planning/phases/04-task-lists-tasks/04-CONTEXT.md` D-05..D-09 and D-11
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 2" (the measured table,
      including the `body.status` / `extra_forbidden` row) and §"Pattern 10"
  </read_first>
  <action>
    Create `schemas/tasks.py` with five models:
      - `TaskCreateRequest` — `title: str`, `description: str | None = None`,
        `priority: TaskPriority = Task.DEFAULT_PRIORITY`, `due_date: datetime | None = None`,
        `extra="forbid"`, and `to_command(self, *, actor_id: UUID, task_list_id: UUID) -> CreateTaskCommand`.
        The priority default reads the domain ClassVar so `medium` exists once and OpenAPI documents
        the real default; importing from `domain` is legal for `presentation` under the layer order.
        No `status` field: a new task starts `pending` and the entity decides that (TASK-01).
      - `TaskPatchRequest` — exactly as in `<interfaces>`: `title`, `description`, `priority`,
        `due_date`, all `| None = None`, `extra="forbid"`, a `@field_validator("title", "priority")`
        for the explicit-null leg, the at-least-one-field `@model_validator`, and `to_command`
        reading `model_fields_set`. **No `status` field.** The class docstring states why in the
        register the project uses: `status` is deliberately absent, so a client that sends it gets a
        422 at `body.status`, which is how "status is not writable here" (TASK-03, D-08) is proven
        rather than promised.
      - `TaskStatusChangeRequest` — one field, `status: TaskStatus`, `extra="forbid"`, and
        `to_command(self, *, actor_id, task_list_id, task_id) -> ChangeTaskStatusCommand` (D-11).
      - `TaskResponse` — the eleven `TaskResult` fields in declaration order, with
        `from_result(cls, result: TaskResult) -> "TaskResponse"` naming every field explicitly, for
        the reason given in Task 2.
      - `TaskCollectionResponse` — `items: list[TaskResponse]`, `total_tasks: int`,
        `completed_tasks: int`, `completion_percentage: float`, in that order, with
        `from_result(cls, result: TaskCollectionResult) -> "TaskCollectionResponse"`. The docstring
        carries D-09: exactly these four members and no others — no `list_id`, no `returned_count`,
        no echoed `filters`, all of which `.planning/research/FEATURES.md` proposes and CONTEXT
        overrules — and the statistics describe the whole list whatever the filter.

    Same three prohibitions as Task 2: no `max_length`, no `Unset` in a field annotation, never
    `self.model_fields`. Additionally: no past-date validator on `due_date` — `Task.create` and
    `Task.reschedule` own that rule and a second copy would drift (Phase 2 D-04, and it would also
    change the error body from the domain's object shape to the request-validation list shape).

    Extend `tests/unit/presentation/test_schemas.py` with, for the task schemas:
      - `set(TaskPatchRequest.model_fields) == {"title", "description", "priority", "due_date"}` —
        and, asserted separately and by name, that `"status"` is **not** in it (D-08).
      - `{"status": "completed"}` raises a `ValidationError` whose single error has
        `type == "extra_forbidden"` and `loc == ("status",)`.
      - three legs per patchable field: omitted → `UNSET`; provided → the value; explicit `null` →
        `None` for `description` and `due_date`, `ValidationError` for `title` and `priority`.
      - `{}` and an unknown key are both refused.
      - `TaskCreateRequest` with only a title produces a command whose `priority is
        Task.DEFAULT_PRIORITY`, binding the OpenAPI default to the entity default.
      - an invalid priority or status string raises a `ValidationError` — the free 422 the enum
        gives, so no hand-rolled membership check is needed anywhere (TASK-06 at the body level).
      - `TaskStatusChangeRequest` refuses an empty body, an unknown key and an invalid status value.
      - `TaskResponse.from_result` copies all eleven fields; `TaskCollectionResponse.from_result`
        preserves item order and copies the three statistics; an empty collection serialises
        `items: []` and `completion_percentage: 0.0`.
      - a declaration-order test for both response models, asserting
        `list(Model.model_fields)` equals the expected sequence, so the D-09 envelope cannot gain a
        member or reorder silently.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/presentation -q --no-cov && .venv/bin/mypy src tests && make lint && make typecheck && make arch && .venv/bin/pytest tests/unit tests/architecture -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/presentation/test_schemas.py -q --no-cov` exits 0 with at least 28 tests passing
    - `.venv/bin/python -c "from taskmanager.presentation.api.schemas.tasks import TaskPatchRequest as P; assert 'status' not in P.model_fields; assert set(P.model_fields) == {'title','description','priority','due_date'}"` exits 0
    - `.venv/bin/python -c "import pydantic, sys; from taskmanager.presentation.api.schemas.tasks import TaskPatchRequest as P\ntry:\n    P(status='completed')\nexcept pydantic.ValidationError as e:\n    err=e.errors()[0]; assert err['type']=='extra_forbidden' and err['loc']==('status',), err; sys.exit(0)\nsys.exit(1)"` exits 0
    - `.venv/bin/python -c "import uuid; from taskmanager.domain.entities.task import Task; from taskmanager.presentation.api.schemas.tasks import TaskCreateRequest as C; c=C(title='x').to_command(actor_id=uuid.uuid4(), task_list_id=uuid.uuid4()); assert c.priority is Task.DEFAULT_PRIORITY"` exits 0
    - `.venv/bin/python -c "from taskmanager.presentation.api.schemas.tasks import TaskCollectionResponse as R; assert list(R.model_fields) == ['items','total_tasks','completed_tasks','completion_percentage']"` exits 0
    - `grep -c "max_length" src/taskmanager/presentation/api/schemas/tasks.py` prints `0`
    - `grep -rc "self.model_fields\b" src/taskmanager/presentation/api/schemas/` prints `0` for every file
    - `make lint && make typecheck && make arch` all exit 0
    - `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0
    - `.venv/bin/pytest tests/unit/presentation -q --cov=taskmanager.presentation.api.schemas --cov-report=term-missing --no-header` shows both modules with no missing lines
  </acceptance_criteria>
  <done>ARC-05 is satisfied for the task half of the slice, and D-08's "status cannot be changed through the generic PATCH" is a 422 with a named `loc` rather than a sentence in a document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JSON body → typed model | The request schema is the first thing a hostile payload meets |
| query/body enum values → application | An enum-typed field means a filter value can never reach SQL as free text |
| caller identity → use case | `get_current_actor` is the single source; nothing else may supply an `actor_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-35 | Elevation of privilege | mass assignment (`status`, `owner_id`, `assignee_id`, `completed_at`) | mitigate | `extra="forbid"` on every request model; `status` is absent from `TaskPatchRequest` and asserted absent by a test; no Phase 4 request schema declares `owner_id` or `assignee_id` at all (D-06, D-08, ASVS V5) |
| T-4-36 | Spoofing | a client supplying its own `actor_id` | mitigate | `actor_id` reaches a command only through `to_command(*, actor_id=...)`, whose value the router takes from `CurrentActor`; no request model has such a field |
| T-4-37 | Spoofing | the demo seam being mistaken for authentication | mitigate | The docstring says "not authentication" in those words and a text-scan test asserts the phrase is still there; plan 04-12 records it as an ADR and plan 04-11's README section repeats it to the evaluator |
| T-4-38 | Elevation of privilege | the demo identity becoming a usable account | mitigate | The seeded row's `password_hash` is a deliberately unverifiable placeholder that Argon2 can never match; the row and the seam are deleted together in Phase 5 |
| T-4-39 | Tampering | a filter or status value reaching SQL as free text | mitigate | Enum-typed fields; an invalid value is a Pydantic 422 before any use case runs, proved by a test per enum field |
| T-4-40 | Information disclosure | the `Unset` sentinel leaking into `/openapi.json` | mitigate | The sentinel appears only inside `to_command`; `grep -c "Unset"` printing `1` per schema module is an acceptance criterion |
| T-4-41 | Information disclosure | a new result field auto-published by `model_validate` | mitigate | Every response model uses an explicit `from_result` naming each field, plus a declaration-order test on `model_fields` |
| T-4-42 | Tampering | a silently ignored typo in the schema-to-command mapper | mitigate | A per-model test asserting `set(Model.model_fields)` exactly, plus omitted/provided/null legs per field — the only way this class of bug is provable, since mypy cannot check a string against `model_fields` |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0.
- `make lint && make typecheck && make arch` all exit 0.
- With `docker compose up -d db`: `make test` exits 0 and reports
  `Required test coverage of 75% reached`.
- `grep -rn "max_length" src/taskmanager/presentation/api/schemas/` prints nothing.
- `.venv/bin/pytest tests/unit/test_settings.py::test_env_example_documents_every_field -q --no-cov`
  exits 0, proving `.env.example` gained no key (D-03).
- Coverage over `taskmanager.presentation.api.schemas` and `...api.actor` shows no missing lines.
</verification>

<success_criteria>
- One dependency supplies caller identity, is honest about being a placeholder, and is the only
  thing Phase 5 has to change (D-01, D-03).
- Every HTTP boundary of the phase is typed by a Pydantic v2 model (ARC-05).
- `{}`, an unknown key and an explicit null on a non-nullable field are each a 422 with the measured
  `loc` and `type` (D-05, D-06).
- `status` cannot be expressed in the generic task PATCH (D-08, TASK-03).
- No business limit and no past-date rule exists in the presentation layer (Phase 2 D-04).
</success_criteria>

<output>
Create `.planning/phases/04-task-lists-tasks/04-07-SUMMARY.md` when done
</output>
