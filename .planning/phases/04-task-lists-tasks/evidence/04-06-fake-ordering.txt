04-06 Task 2 falsification: the fake's list_for_task_list ORDER BY

The SQLAlchemy adapter orders by (created_at, id); FakeTaskRepository
returned insertion order. 04-PATTERNS.md section 11 scheduled the fix for
'both list methods' and plan 04-02 applied it to the task-list side only.
Captured below: the same suite with the sort removed, then restored.

=== RED: return tasks (insertion order, as the fake shipped) ===
E         Use -v to get more diff

tests/unit/application/test_list_tasks.py:174: AssertionError
_____________ test_the_status_filter_narrows_the_items_on_its_own ______________

    async def test_the_status_filter_narrows_the_items_on_its_own() -> None:
        """TASK-06, one filter at a time: only the pending tasks come back."""
        unit_of_work = _uow()

        result = await ListTasks(unit_of_work).execute(_command(status=TaskStatus.PENDING))

>       assert [item.id for item in result.items] == [A_ID, B_ID, E_ID]
E       AssertionError: assert [UUID('eeeeee...aaaaaaaaaaa')] == [UUID('aaaaaa...eeeeeeeeeee')]
E
E         At index 0 diff: UUID('eeeeeeee-eeee-4eee-8eee-eeeeeeeeeeee') != UUID('aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa')
E         Use -v to get more diff

tests/unit/application/test_list_tasks.py:184: AssertionError
=========================== short test summary info ============================
FAILED tests/unit/application/test_list_tasks.py::test_list_tasks_orders_by_created_at_then_id
FAILED tests/unit/application/test_list_tasks.py::test_the_status_filter_narrows_the_items_on_its_own
2 failed, 7 passed in 0.04s

=== GREEN: sorted(tasks, key=lambda entry: (entry.created_at, entry.id)) ===
.........                                                                [100%]
9 passed in 0.02s
