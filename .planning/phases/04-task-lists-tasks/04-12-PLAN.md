---
phase: 04-task-lists-tasks
plan: 12
type: execute
wave: 7
depends_on: ["04-09", "04-10", "04-11"]
files_modified:
  - DECISION_LOG.md
  - AI_WORKFLOW.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt
autonomous: true
requirements: [ARC-05, LIST-01, LIST-02, LIST-03, LIST-04, LIST-05, LIST-06, TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08]

must_haves:
  truths:
    - "DECISION_LOG.md gains an ADR per Phase 4 decision that outlives the phase — the actor seam and its seed (D-01/D-02/D-03), the typed PATCH sentinel (D-05), the updated_at-on-provided choice, the dedicated status endpoint over an action verb, the D-09/D-10 response envelopes, and the D-15 gate pair — in context / options / decision / consequences form, appended only"
    - "AI_WORKFLOW.md gains the phase's dated incident entries, each traceable to a commit, an evidence file or a named test"
    - "CLAUDE.md's Project Rules record the two new failing gates: no router imports or raises HTTPException, and no layer below presentation imports fastapi or starlette"
    - "All fifteen Phase 4 requirement IDs are ticked in REQUIREMENTS.md, each re-verified against a named test rather than inherited from a plan header"
    - "The full phase gate is captured in one uninterrupted run: make lint, make typecheck, make arch, make test, then make docker-test, all green, with the coverage number recorded"
  artifacts:
    - path: "DECISION_LOG.md"
      provides: "the Phase 4 ADRs"
      contains: "ADR-0"
    - path: ".planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt"
      provides: "the one-run four-gate capture plus make docker-test"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the fifteen Phase 4 ticks and the updated traceability rows"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "tests/integration/api/"
      via: "each tick names the test that re-verified it"
      pattern: "LIST-0"
    - from: "CLAUDE.md"
      to: "tests/architecture/test_routers_raise_no_http_exception.py"
      via: "the rule is transcribed from the gate that enforces it, not from a plan"
      pattern: "HTTPException"
---

<objective>
Close the phase: record the decisions that outlive it, record what went wrong honestly, tick the
fifteen requirements against named tests, and capture the whole gate green in one run.

Purpose: this project's thesis is that the workflow is provable, not asserted. A phase that shipped
working code but left its decisions unwritten and its requirements ticked from plan headers would
undermine exactly the thing it is being judged on. `DECISION_LOG.md` is append-only and
`AI_WORKFLOW.md` is written incrementally, so this is the last moment either can be honest.
Output: the Phase 4 ADRs, the incident entries, the CLAUDE.md rules, the requirement ticks and the
gate capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-task-lists-tasks/04-CONTEXT.md
@.planning/phases/04-task-lists-tasks/04-RESEARCH.md
@.planning/phases/04-task-lists-tasks/04-PATTERNS.md
@.planning/phases/04-task-lists-tasks/04-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- The Phase 4 requirement set this plan is the LAST claimant of. Every tick is re-verified
     against a named test before the box is ticked (the 02-07 / 03-11 precedent). -->

| ID | Re-verify against |
|----|-------------------|
| ARC-05 | `tests/unit/presentation/test_schemas.py` + the route-inventory test in `tests/unit/test_app_factory.py` |
| LIST-01 | `tests/integration/api/test_task_lists.py -k create` |
| LIST-02 | `tests/integration/api/test_task_lists.py -k get` |
| LIST-03 | `tests/integration/api/test_statements.py -k lists` + `tests/integration/test_repositories_task_lists.py -k single_statement` |
| LIST-04 | `tests/integration/api/test_task_lists.py -k patch` |
| LIST-05 | `tests/integration/api/test_task_lists.py -k delete` |
| LIST-06 | `tests/integration/api/test_task_lists.py -k duplicate` |
| TASK-01 | `tests/integration/api/test_tasks.py -k create` |
| TASK-02 | `tests/integration/api/test_tasks.py -k wrong_list` |
| TASK-03 | `tests/integration/api/test_tasks.py -k status_is_not_writable` |
| TASK-04 | `tests/integration/api/test_tasks.py -k delete` |
| TASK-05 | `tests/integration/api/test_tasks.py -k status` + `tests/unit/application/test_change_task_status.py` |
| TASK-06 | `tests/integration/api/test_tasks.py -k filter` |
| TASK-07 | `tests/integration/api/test_tasks.py -k statistics` |
| TASK-08 | `tests/integration/api/test_tasks.py -k validation_error` + `tests/unit/domain/test_task.py` |

<!-- Existing ADRs this phase refines or relies on, so a new entry names them by id rather than
     inventing a reference: ADR-008 (404 vs 403), ADR-009 (single SQL aggregate),
     ADR-015 (a gate goes in two places unless it rides inside pytest), ADR-020 (DTOs are frozen
     dataclasses), ADR-021/ADR-028 (ValidationError -> 422 and its scope),
     ADR-034 (Annotated dependency injection), ADR-037 (shell-only code stays out of src/),
     ADR-043 (no pagination). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The Phase 4 ADRs</name>
  <files>DECISION_LOG.md</files>
  <read_first>
    - `DECISION_LOG.md` — the whole file: the ADR numbering reached at the end of Phase 3, the
      context / options / decision / consequences structure, and how ADR-028 and ADR-041 refine
      earlier entries **by id** without editing them
    - `.planning/phases/04-task-lists-tasks/04-CONTEXT.md` — D-01 through D-17, which are the raw
      material
    - All eleven Phase 4 summaries,
      `.planning/phases/04-task-lists-tasks/04-0*-SUMMARY.md` — each one hands forward decisions
      taken during execution that the plans did not anticipate; those are ADRs too, and 03-11's
      lesson was that writing only the enumerated ones pushes the debt into Phase 7
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` "Assumptions Log" and "Open Questions" —
      A1 (the `updated_at` choice) and Open Question 3 (the OpenAPI polish level) are decisions that
      were taken and should be recorded as such
    - `.planning/research/FEATURES.md` — the three places it contradicts CONTEXT (the empty-PATCH
      200, multi-value filters, the richer envelope); recording why CONTEXT won is part of DOC-03's
      "every brief ambiguity resolved"
  </read_first>
  <action>
    Append ADRs to `DECISION_LOG.md`, continuing the existing numbering. The log is append-only:
    `git diff DECISION_LOG.md | grep -c '^-'` must print `1` (the diff header alone) when you are
    done, and any entry that supersedes an earlier one names it by id rather than editing it.

    The decisions this phase owes, at minimum:
      - **The actor seam** (D-01, D-03). A single `get_current_actor` dependency returning a fixed
        demo id, with the options considered (JWT now, a header-supplied id, a get-or-create inside
        the dependency) and the consequence stated plainly: this is **not** authentication, Phase 5
        replaces only the body, and the placeholder `password_hash` means the identity can never be
        authenticated into.
      - **The demo-user seed in the entrypoint** (D-02). Options: an Alembic data migration, a
        get-or-create in the dependency, a module under `src/`, a shell heredoc. Consequences: the
        untargeted `ON CONFLICT` and why it is required under `set -eu`; no coverage exemption; the
        step is deleted in Phase 5. Name ADR-037 as the precedent.
      - **The typed PATCH sentinel** (D-05, Claude's-discretion item 1). Options: `None` as the
        marker, a bare `object()`, `fields_set: frozenset[str]`, per-field wrappers, the enum
        sentinel. Record the measured reason the sentinel stays out of the Pydantic schema — the
        `_Unset` component in `/openapi.json` and the two-entry error `loc`. Refines ADR-020.
      - **`updated_at` moves whenever a field is provided** (research A1). Options: compare values
        first, or stamp unconditionally. Consequence: no genuine no-op request exists because D-06
        makes `{}` a 422, and a second "did this change?" rule beside the mutators is the defect
        Phase 2 D-04 exists to prevent.
      - **A dedicated status endpoint rather than an action verb or a writable PATCH field** (D-08,
        D-11, TASK-03/TASK-05). Options: `PATCH .../status`, a `POST .../complete`-style action
        verb (the Todoist shape `.planning/research/FEATURES.md` names as defensible), and a
        writable `status` in the generic PATCH. Consequence: the state machine has exactly one door,
        and sending `status` to the generic PATCH is a 422 rather than a silent no-op.
      - **The response envelopes** (D-09, D-10). Why the task collection carries exactly four
        members and not `list_id`, `returned_count` or an echoed `filters` block; why every
        task-list answer carries the three counters; and why the statistics describe the list while
        the filter describes the view. Refines ADR-009.
      - **`ChangeTaskStatusCommand` gained `task_list_id`** (D-14). The reference use case could not
        express the wrong-list 404; the alternative was ignoring the parent segment on that one
        route. Consequence: a nested path is honoured on every verb.
      - **The D-15 gate pair.** The import-linter `forbidden` contract works on modules and the AST
        gate works on symbols; the raise-check alone misses an indirect raise, which is why the
        import-check exists and is the load-bearing half. Both add no pre-commit hook and no CI step
        — name ADR-015 and the 02-06 / 03-06 precedent.
      - **The OpenAPI polish level** (Open Question 3). Tags, summaries, response descriptions and a
        status-keyed `responses` map now; a documented `ProblemDetail` model deferred to DOC-04 in
        Phase 7, with the reason (it is a model that exists only for documentation, and Phase 7 owns
        that budget).
      - **Where research and CONTEXT disagreed.** One entry recording the three `FEATURES.md`
        contradictions and that CONTEXT won each time, so a reader of the older research document
        does not "fix" the code back toward it.

    Add any further decision a Phase 4 summary handed forward. Prefer writing one ADR too many over
    leaving one owed: 03-11 wrote twenty-one where the plan enumerated twelve, and that call is the
    precedent.
  </action>
  <verify>
    <automated>git diff DECISION_LOG.md | grep -c '^-' | grep -qx 1 && grep -c "^## ADR-" DECISION_LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff DECISION_LOG.md | grep -c '^-'` prints `1`
    - `grep -c "ADR-" DECISION_LOG.md` increased by at least 9 compared with `git show HEAD:DECISION_LOG.md | grep -c "ADR-"`
    - `grep -ci "not authentication" DECISION_LOG.md` prints at least `1`
    - `grep -c "ADR-037" DECISION_LOG.md` prints at least `2` — the original plus the seed entry referencing it
    - `grep -c "ADR-015" DECISION_LOG.md` prints at least `2`
    - `grep -c "ON CONFLICT" DECISION_LOG.md` prints at least `1`
    - Every new entry contains the four headings the file's existing entries use (context, options, decision, consequences), verified by reading the appended block
  </acceptance_criteria>
  <done>Every Phase 4 decision that a reader in six months would otherwise have to reverse-engineer from the code is written down, with its rejected alternatives, and nothing earlier was edited away.</done>
</task>

<task type="auto">
  <name>Task 2: The incident log, the CLAUDE.md rules, and the fifteen requirement ticks</name>
  <files>AI_WORKFLOW.md, CLAUDE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `AI_WORKFLOW.md` — the whole file: the five-section skeleton, the dated incident-log format,
      and the Phase 1-3 entries whose register the new ones must match (what the human decided, what
      was delegated, what the AI got wrong and how it was caught)
    - `CLAUDE.md` "Project Rules" — the hand-maintained section outside every GSD delimiter block,
      and the convention that every rule there is enforced by a failing gate and is transcribed from
      the enforcement rather than from a plan
    - `.planning/REQUIREMENTS.md` — the fifteen Phase 4 checkboxes and the Traceability table rows
      that must move from `Pending` to `Complete`
    - `.planning/ROADMAP.md` "Phase 4" — the `**Plans:** TBD` placeholder, the plan list, the five
      success criteria to be re-read against what shipped, and the Progress table row
    - All eleven Phase 4 summaries — the source of the incidents; each claim in `AI_WORKFLOW.md`
      must point at a commit, a file or a test
    - `.planning/phases/04-task-lists-tasks/evidence/` — the four captures this phase produced
      (`04-02-importlinter-red.txt`, `04-08-ast-gate-red.txt`, `04-11-seed-idempotence.txt`,
      `04-11-cold-start.txt`), each of which is quotable evidence rather than a paraphrase
    - `tests/architecture/test_routers_raise_no_http_exception.py` and `.importlinter` — the two
      gates the CLAUDE.md rules are transcribed from
  </read_first>
  <action>
    **`AI_WORKFLOW.md`.** Append the Phase 4 incident entries, dated, in the established format.
    Start from what actually happened rather than from a list this plan invents — the summaries and
    the four evidence captures are the source. At minimum the phase owes entries for:
      - the pre-existing red `test_get_settings_is_cached`, found by the researcher's baseline run
        rather than by CI, and fixed by changing the test's isolation rather than its assertion
        (plan 04-01, already appended there — extend it if execution revealed more);
      - `ChangeTaskStatusCommand` shipping in Phase 2 unable to express the URL shape Phase 4 chose,
        caught by research before any router was written;
      - the sentinel-in-the-schema variant that was executed and rejected on measured evidence
        (`/openapi.json` leak, two-entry error `loc`) rather than on taste;
      - whichever of this phase's plans turned out to be wrong in execution, with the cost — 03-11's
        entry naming `03-10-PLAN.md` and `03-RESEARCH.md` Pattern 7 is the precedent, and a phase
        with no such entry should be treated as suspicious rather than clean.
    Quote numbers and output verbatim from the evidence files; do not paraphrase a count.

    **`CLAUDE.md` "Project Rules".** Add the two gates this phase landed, in the section's own
    register (a rule plus the command that fails when it is broken), transcribed from the gate
    rather than from a plan:
      - no module under `src/taskmanager/presentation/api/routers/` raises **or imports**
        `HTTPException`, enforced by `tests/architecture/test_routers_raise_no_http_exception.py`,
        which reports `file:line` and carries a non-vacuity guard;
      - no module under `domain`, `application` or `infrastructure` imports `fastapi` or
        `starlette`, enforced by the `no-http-below-presentation` contract in `.importlinter` and by
        `EXPECTED_CONTRACT_NAMES` in `tests/architecture/test_layer_boundaries.py`.
    Note, as the section does elsewhere, that neither gate adds a pre-commit hook or a CI step
    because both ride inside commands those already run. Keep the addition outside every
    `<!-- GSD:*-start -->` block.

    **`.planning/REQUIREMENTS.md`.** Tick all fifteen Phase 4 boxes — ARC-05, LIST-01..06,
    TASK-01..08 — and move their Traceability rows to `Complete`. Tick each only after running the
    command in this plan's `<interfaces>` table for it and seeing it pass; the ticks are
    re-verification, not inheritance, which is the 02-07 and 03-11 convention. If any command
    collects zero tests, that requirement is not met — stop and report rather than ticking it.

    **`.planning/ROADMAP.md`.** Replace Phase 4's `**Plans:** TBD` with the real count and wave
    structure, list the twelve plans with their one-line objectives and tick them, mark the phase
    complete with its date in both the phase heading and the Progress table, and re-read the five
    success criteria against what shipped. If any criterion's wording is now inaccurate — as
    happened with Phase 2 SC-1 and Phase 3 SC-4 — amend the wording and record the amendment rather
    than quietly reinterpreting it.
  </action>
  <verify>
    <automated>grep -c "LIST-01" .planning/REQUIREMENTS.md && .venv/bin/pytest tests/integration/api tests/unit/presentation -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*\(ARC-05\|LIST-0[1-6]\|TASK-0[1-8]\)\*\*" .planning/REQUIREMENTS.md` prints `0` — no Phase 4 requirement is still unticked
    - `grep -c "| Phase 4 | Pending |" .planning/REQUIREMENTS.md` prints `0`
    - `grep -c "TBD" .planning/ROADMAP.md` is lower than in `git show HEAD:.planning/ROADMAP.md`, and the Phase 4 section contains no `TBD`
    - `grep -c "04-12-PLAN.md" .planning/ROADMAP.md` prints at least `1`
    - `grep -c "test_routers_raise_no_http_exception" CLAUDE.md` prints at least `1`
    - `grep -c "no-http-below-presentation" CLAUDE.md` prints at least `1`
    - `git diff CLAUDE.md | grep -c "GSD:"` prints `0` — the addition is outside every delimiter block
    - `grep -ci "phase 4" AI_WORKFLOW.md` prints at least `1` and the appended entries are dated
    - Each of the fifteen commands in this plan's `<interfaces>` table was run and exited 0, with its output pasted into the Task 3 gate capture
  </acceptance_criteria>
  <done>The requirement ticks are earned rather than inherited, the two new gates are documented where the AI reads its rules, and the phase's mistakes are on the record before anyone can reconstruct them favourably.</done>
</task>

<task type="auto">
  <name>Task 3: The full phase gate in one uninterrupted run</name>
  <files>.planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt</files>
  <read_first>
    - `Makefile` — what `lint`, `typecheck`, `arch`, `test` and `docker-test` each actually run, and
      the note that `docker-test` is the zero-host-setup path free of the developer `.env`
    - `pytest.ini` — the `addopts` carrying `--cov-fail-under=75`, so a full run reports the number
    - `.planning/phases/03-persistence-runnable-stack/evidence/` — the Phase 3 gate capture's
      format, and 03-11's note that the capture must be written to a scratch path and copied in
      afterwards, because writing it into `.planning/` first would make the transcript's own
      `git status --porcelain` line non-empty
    - `.planning/phases/04-task-lists-tasks/04-VALIDATION.md` "Sampling Rate" — the phase-gate
      definition this task discharges
    - `.planning/phases/04-task-lists-tasks/evidence/04-11-cold-start.txt` (plan 04-11) — the
      cold-start half of the gate, already discharged; this capture references it rather than
      repeating it
  </read_first>
  <action>
    Run the whole gate in one uninterrupted sequence and capture it verbatim. Write the transcript
    to a scratch path first and copy it into
    `.planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt` afterwards, so the
    `git status --porcelain` line inside the transcript is genuinely blank.

    The sequence, in this order:
      1. `git status --porcelain` — must print nothing. A gate run against a dirty tree proves
         nothing about what will be committed.
      2. `make format` — run first and shown to be a no-op, so the `make lint` that follows is not
         passing because it was just fixed.
      3. `make lint`
      4. `make typecheck`
      5. `make arch` — must report `Contracts: 4 kept, 0 broken.`
      6. `docker compose up -d db`, then `make test` — record the full summary line and the
         `Required test coverage of 75% reached` line, plus the achieved percentage and the
         statement count. Record the number as observed; do not round it or compare it to a target
         the project does not have.
      7. `make docker-test` — the zero-host-setup path, which is also the run that is free of the
         developer's `.env`. Record its summary line too, and note whether the two runs report the
         same test count; a difference is information, not noise.
      8. The fifteen per-requirement commands from Task 2's re-verification, each with its
         collected-test count, so the ticks in `REQUIREMENTS.md` point at output in this file.
      9. A reference to `evidence/04-11-cold-start.txt` for the compose leg, named by path rather
         than repeated.

    If any step is red, stop, fix it in the file that owns it, and re-run the **whole** sequence from
    step 1 — a gate stitched from separate runs is not the claim the capture makes. Record the
    number of attempts it took at the head of the file.
  </action>
  <verify>
    <automated>git status --porcelain | wc -l | grep -qx 0 && make lint && make typecheck && make arch && docker compose up -d db && sleep 5 && make test && make docker-test</automated>
  </verify>
  <acceptance_criteria>
    - `make lint` exits 0
    - `make typecheck` exits 0
    - `make arch` exits 0 and its output contains `Contracts: 4 kept, 0 broken.`
    - `make test` exits 0 with the `db` container running and its output contains `Required test coverage of 75% reached`
    - `make docker-test` exits 0
    - `.planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt` exists and contains the strings `make lint`, `make typecheck`, `make arch`, `make test`, `make docker-test`, `4 kept, 0 broken` and `Required test coverage of 75% reached`
    - The capture's `git status --porcelain` line is followed by no output
    - `grep -c "04-11-cold-start.txt" .planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt` prints at least `1`
    - `grep -rn "pragma: no cover" src/taskmanager/` prints nothing and `grep -c "omit" pytest.ini` prints `0`
  </acceptance_criteria>
  <done>Every gate the project is judged by is green in one recorded run, on the host and in the container, and the coverage number in the transcript is the one the suite actually produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → the AI's own rules | `CLAUDE.md` is read by every future agent; a rule that drifts from its gate is worse than no rule |
| requirement tick → evidence | A tick asserts a claim to an evaluator; an unearned one is the project's only real integrity risk |
| gate capture → reviewer | A transcript is only worth what the conditions it was produced under are worth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-70 | Repudiation | a requirement ticked from a plan header rather than from a test | mitigate | Each of the fifteen ticks is re-verified by a named command whose output is pasted into the gate capture; a command collecting zero tests blocks the tick |
| T-4-71 | Repudiation | a gate capture stitched from separate runs | mitigate | One uninterrupted sequence starting from a clean `git status --porcelain`, with `make format` shown to be a no-op first and the attempt count recorded |
| T-4-72 | Tampering | `DECISION_LOG.md` edited rather than appended | mitigate | `git diff DECISION_LOG.md | grep -c '^-'` printing `1` is an acceptance criterion; superseding entries name the id they refine |
| T-4-73 | Tampering | a CLAUDE.md rule drifting from the gate it describes | mitigate | Both rules are transcribed from the gate files, which the task reads first, and the acceptance criteria grep for the enforcing file and contract by name |
| T-4-74 | Information disclosure | an `AI_WORKFLOW.md` entry that presents the phase more favourably than it went | mitigate | Every claim points at a commit, a file or a test; numbers are quoted verbatim from the evidence captures; the task states explicitly that a phase with no wrong-plan entry should be treated as suspicious |
| T-4-75 | Repudiation | a coverage number reached by exemption | mitigate | `grep -rn "pragma: no cover" src/taskmanager/` printing nothing and `grep -c "omit" pytest.ini` printing `0` are acceptance criteria |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package; `requirements*.txt` are unchanged across the whole phase |
</threat_model>

<verification>
- `make lint && make typecheck && make arch && make test` all exit 0 with the `db` container
  running, and `make docker-test` exits 0.
- `make arch` reports `Contracts: 4 kept, 0 broken.`
- No Phase 4 requirement remains unticked in `.planning/REQUIREMENTS.md`, and no Phase 4
  Traceability row still reads `Pending`.
- `.planning/ROADMAP.md` Phase 4 contains no `TBD`, lists all twelve plans and is marked complete.
- `DECISION_LOG.md` grew by at least nine ADRs and lost no line.
- `.planning/phases/04-task-lists-tasks/evidence/04-12-phase-gate.txt` contains the whole run.
</verification>

<success_criteria>
- Every Phase 4 decision that outlives the phase is an ADR with its rejected alternatives
  (DOC-03 groundwork).
- `AI_WORKFLOW.md` carries the phase's dated, traceable incidents.
- `CLAUDE.md` records the two new gates, transcribed from the gates themselves.
- All fifteen requirement IDs are ticked, each against a command that was run and passed.
- The four quality gates plus `make docker-test` are green in one recorded, clean-tree run.
</success_criteria>

<output>
Create `.planning/phases/04-task-lists-tasks/04-12-SUMMARY.md` when done
</output>
