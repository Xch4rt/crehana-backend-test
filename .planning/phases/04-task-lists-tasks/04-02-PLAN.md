---
phase: 04-task-lists-tasks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/taskmanager/application/ports/repositories.py
  - src/taskmanager/infrastructure/db/repositories/task_lists.py
  - tests/unit/application/fakes.py
  - tests/unit/application/test_ports.py
  - tests/unit/application/test_fakes.py
  - tests/unit/infrastructure/test_adapter_ports.py
  - tests/integration/test_repositories_task_lists.py
  - .importlinter
  - tests/architecture/test_layer_boundaries.py
  - .planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt
autonomous: true
requirements: [LIST-03]

must_haves:
  truths:
    - "TaskListRepository.list_for_owner_with_stats(owner_id) exists on the port, returns Sequence[tuple[TaskList, CompletionStats]] in domain types only, and both the in-memory fake and the SQLAlchemy adapter satisfy it under mypy strict (LIST-03)"
    - "The adapter answers LIST-03 in ONE SQL statement: a LEFT OUTER JOIN with count(tasks.id) FILTER, grouped by task_lists.id — a list with zero tasks reports total 0 and 0.0, never a phantom task"
    - "Both list-returning methods order by created_at then id, in the adapter and in the fake alike (D-13)"
    - "The D-15 import-linter contract forbids domain, application and infrastructure from importing fastapi or starlette, EXPECTED_CONTRACT_NAMES names it, and the gate is shown red with a planted violation captured under evidence/ (D-15)"
    - "No transaction-ending call exists anywhere under infrastructure/db/repositories/ after the new method lands"
  artifacts:
    - path: "src/taskmanager/infrastructure/db/repositories/task_lists.py"
      provides: "lists_with_stats_statement() and SqlAlchemyTaskListRepository.list_for_owner_with_stats"
      contains: "lists_with_stats_statement"
    - path: ".importlinter"
      provides: "the D-15 forbidden contract"
      contains: "no-http-below-presentation"
    - path: ".planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt"
      provides: "The planted-violation red capture and its green counterpart"
  key_links:
    - from: "src/taskmanager/infrastructure/db/repositories/task_lists.py"
      to: "src/taskmanager/domain/value_objects/completion.py"
      via: "each grouped row is mapped into CompletionStats(total=..., completed=...)"
      pattern: "CompletionStats"
    - from: ".importlinter"
      to: "tests/architecture/test_layer_boundaries.py"
      via: "EXPECTED_CONTRACT_NAMES asserts the exact contract set, so the two files move together"
      pattern: "No web framework below presentation"
---

<objective>
Add the one repository capability LIST-03 needs — every list of one owner with its two counters, in
a single statement — and land the first half of D-15's proof gate.

Purpose: LIST-03 says "no N+1" and plan 04-10 will assert it with a statement counter. That
assertion is only meaningful if the query it counts was written as one grouped statement in the
first place. The port change is the phase's one three-file atomic edit: adding the method breaks
the fake, `test_ports.py` and `test_adapter_ports.py` simultaneously under mypy strict, so all of
it belongs in one change. The import-linter contract rides along because it touches a disjoint set
of files and needs no code that does not exist yet.
Output: `list_for_owner_with_stats` on the port, the fake and the adapter; the grouped statement
with a compiled-SQL proof; and the D-15 contract with its red capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-lists-tasks/04-CONTEXT.md
@.planning/phases/04-task-lists-tasks/04-RESEARCH.md
@.planning/phases/04-task-lists-tasks/04-PATTERNS.md
@.planning/phases/04-task-lists-tasks/04-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- The new port method. Domain types only — never a Row (CLAUDE.md repositories rule). -->

```python
    async def list_for_owner_with_stats(
        self, owner_id: UUID
    ) -> Sequence[tuple[TaskList, CompletionStats]]: ...
```

<!-- The statement, executed against PostgreSQL 18.4: 1 statement for 2 lists and for 5 lists,
     empty list -> total=0 completed=0 pct=0.0, counters arrive as Python int.
     04-RESEARCH Pattern 3. -->

```python
def lists_with_stats_statement(owner_id: UUID) -> Select[tuple[TaskListRow, int, int]]:
    return (
        select(
            TaskListRow,
            func.count(TaskRow.id).label("total"),
            func.count(TaskRow.id)
            .filter(TaskRow.status == TaskStatus.COMPLETED.value)
            .label("completed"),
        )
        .outerjoin(TaskRow, TaskRow.task_list_id == TaskListRow.id)
        .where(TaskListRow.owner_id == owner_id)
        .group_by(TaskListRow.id)
        .order_by(TaskListRow.created_at, TaskListRow.id)
    )
```

Compiled against the psycopg dialect it renders
`count(tasks.id) FILTER (WHERE tasks.status = %(status_1)s) AS completed`
over `task_lists LEFT OUTER JOIN tasks ON tasks.task_list_id = task_lists.id`.

<!-- The D-15 contract, executed against the current graph and reported KEPT
     (`Contracts: 4 kept, 0 broken.`). 04-RESEARCH Pattern 9. -->

```ini
[importlinter:contract:no-http-below-presentation]
name = No web framework below presentation
type = forbidden
source_modules =
    taskmanager.domain
    taskmanager.application
    taskmanager.infrastructure
forbidden_modules =
    fastapi
    starlette
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The port method, the in-memory fake and the two conformance tests, in one edit</name>
  <files>src/taskmanager/application/ports/repositories.py, tests/unit/application/fakes.py, tests/unit/application/test_ports.py, tests/unit/application/test_fakes.py</files>
  <read_first>
    - `src/taskmanager/application/ports/repositories.py` — the whole file:
      `TaskRepository.list_for_task_list` L49-L55 (the keyword-only, comment-above-the-method
      shape), `completion_stats` L57, the `TaskListRepository` block L60-L72 where the new method
      goes beside `list_for_owner`, and the existing `CompletionStats` import at L30
    - `tests/unit/application/fakes.py` — the whole file:
      `FakeTaskRepository.completion_stats` L82-L90 (counted from what is stored, never canned),
      `FakeTaskListRepository.list_for_owner` L117-L122 (which has **no ordering**), and
      `FakeUnitOfWork.__init__` L184-L197, which already constructs both repositories and is the
      natural place to hand one to the other
    - `tests/unit/application/test_ports.py` L47-L64 — the conformance-test shape
    - `tests/unit/application/test_fakes.py` — the behavioural tests of the fakes themselves
    - `src/taskmanager/domain/value_objects/completion.py` — `CompletionStats`; `percentage` is a
      **property**, already rounded to two decimals
    - `.planning/phases/04-task-lists-tasks/04-PATTERNS.md` §4 and §11 — the three things this
      change breaks simultaneously and the two scheduling notes
  </read_first>
  <action>
    Add `list_for_owner_with_stats(self, owner_id: UUID) -> Sequence[tuple[TaskList, CompletionStats]]`
    to the `TaskListRepository` Protocol, directly beneath `list_for_owner`, with a comment above
    it in the style of `list_for_task_list`'s: LIST-03 puts the counters on every entry of the
    collection, so computing them one list at a time would be the N+1 that PITFALLS Pitfall 11
    names; the method returns domain types because CLAUDE.md's repositories rule forbids a `Row`
    crossing into `application`; and a bare `tuple` rather than a new domain value object because
    the naming belongs on the application result DTO, not on a concept only one query needs
    (04-RESEARCH Open Question 2).

    Implement the method on `FakeTaskListRepository`. The fake list repository has no access to
    tasks, so wire it explicitly: give `FakeTaskListRepository.__init__` an optional
    `tasks: FakeTaskRepository | None = None` parameter and have `FakeUnitOfWork.__init__` pass the
    task repository it already constructs. Compute the counters from what the task fake has stored,
    exactly as `FakeTaskRepository.completion_stats` does, so the fake can *disagree* with a wrong
    use case. State the wiring choice in a comment.

    Also add the D-13 ordering to both list-returning fake methods:
    `sorted(..., key=lambda entry: (entry.created_at, entry.id))` on `list_for_owner` and on the
    new method. The adapter orders by `(created_at, id)` and the fake did not, which is exactly the
    divergence that would let a D-13 assertion pass in a unit test and fail over HTTP
    (04-PATTERNS §11).

    Add the port-conformance assertion for the new method in `tests/unit/application/test_ports.py`
    following the existing `test_fake_task_list_repository_satisfies_the_task_list_repository_port`
    shape, and extend `tests/unit/application/test_fakes.py` with: the fake returns one entry per
    stored list of that owner and none belonging to another owner; a list with no tasks yields
    `CompletionStats(total=0, completed=0)` whose `percentage` is `0.0`; a list with a known mix
    yields the matching counters; and the entries come back ordered by `(created_at, id)` with two
    lists sharing a `created_at` instant, so the `id` tie-break is exercised rather than assumed.

    Do not touch the SQLAlchemy adapter in this task — `tests/unit/infrastructure/test_adapter_ports.py`
    is expected to be red between Task 1 and Task 2, and that is the guard working.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/application -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/application -q --no-cov` exits 0
    - `.venv/bin/python -c "import inspect; from taskmanager.application.ports.repositories import TaskListRepository; assert 'list_for_owner_with_stats' in TaskListRepository.__dict__"` exits 0
    - `grep -c "list_for_owner_with_stats" src/taskmanager/application/ports/repositories.py tests/unit/application/fakes.py tests/unit/application/test_ports.py` prints a non-zero count for each of the three files
    - `grep -c "Row" src/taskmanager/application/ports/repositories.py` prints `0`
    - `grep -c "sorted(" tests/unit/application/fakes.py` prints at least `2`
    - `.venv/bin/pytest tests/unit/application/test_fakes.py -q --no-cov` exits 0 with a test whose name mentions ordering
  </acceptance_criteria>
  <done>The port speaks the LIST-03 capability in domain types, the in-memory fake implements it against its sibling's stored tasks, and both fakes now order the way the adapter does.</done>
</task>

<task type="auto">
  <name>Task 2: The grouped statement, the adapter method and its live proof</name>
  <files>src/taskmanager/infrastructure/db/repositories/task_lists.py, tests/unit/infrastructure/test_adapter_ports.py, tests/integration/test_repositories_task_lists.py</files>
  <read_first>
    - `src/taskmanager/infrastructure/db/repositories/task_lists.py` — the whole file:
      `list_for_owner` L118-L143 for the ordering and the "filter in SQL, not in Python" argument,
      and the module docstring's no-commit rule. Note it imports neither `TaskRow` nor `TaskStatus`
      today
    - `src/taskmanager/infrastructure/db/repositories/tasks.py` — `completion_statement` L54-L75
      (the module-level-function-so-the-SQL-can-be-compiled argument, verbatim) and
      `completion_stats` L178-L194 (the method that runs it and maps the row)
    - `src/taskmanager/infrastructure/db/mappers.py` — `task_list_to_entity`
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 3" — the verified body, the
      rendered SQL, the executed results, and the `count(tasks.id)`-not-`count(*)` trap
    - `tests/integration/test_repositories_task_lists.py` — the whole file: L50-L64 fixed
      identifiers, the `given_an_owner` helper L86-L118, and `pytestmark = pytest.mark.integration`
    - `tests/unit/infrastructure/test_adapter_ports.py` L67-L70 — the adapter-conformance test
      that is red until this task lands
    - `tests/architecture/test_no_commit_in_repositories.py` — the text scan this package is under
  </read_first>
  <action>
    Add a module-level `lists_with_stats_statement(owner_id: UUID) -> Select[tuple[TaskListRow, int, int]]`
    to `infrastructure/db/repositories/task_lists.py` with the body given in `<interfaces>`. Import
    `TaskRow` from `.models` and `TaskStatus` from `taskmanager.domain.value_objects.task_status`;
    neither is imported in this module yet.

    Its docstring carries the same two arguments `completion_statement` makes, plus the one that is
    specific to this shape: it is a module-level function so the SQL can be compiled and asserted on
    with no server at all, which is what turns "one statement" from an intention into a gate; and
    `count(tasks.id)` rather than `count(*)`, because on the null-extended row a LEFT OUTER JOIN
    produces for a list with no tasks, `count(*)` returns 1 and the list reports a percentage over a
    phantom task. `GROUP BY task_lists.id` alone is sufficient because `id` is the primary key, so
    PostgreSQL's functional-dependency rule makes every other selected column legal without being
    listed — say so, because a reader will otherwise expect the full column list.

    Add `list_for_owner_with_stats` to `SqlAlchemyTaskListRepository`: execute the statement with
    `self._session.execute(...)` and build
    `[(task_list_to_entity(row), CompletionStats(total=total, completed=completed)) for row, total, completed in result]`.
    No `.commit()`, no `flush()`, no `selectinload` — the `TaskListRow.tasks` relationship is
    `lazy="raise"` and touching it raises.

    Extend `tests/integration/test_repositories_task_lists.py` with:
      - `test_listing_with_stats_returns_one_entry_per_list_of_the_owner` — two lists with different
        task mixes, asserting the entities and both counters.
      - `test_listing_with_stats_reports_zero_for_a_list_with_no_tasks` — `total == 0`,
        `completed == 0` and `percentage == 0.0`, which is the `count(*)` trap made into a test.
      - `test_listing_with_stats_never_returns_another_owners_list`.
      - `test_listing_with_stats_orders_by_created_at_then_id` — two lists sharing a `created_at`,
        so the `id` tie-break is exercised (D-13).
      - `test_listing_with_stats_is_a_single_statement` — compile
        `lists_with_stats_statement(OWNER_ID)` with `.compile(dialect=postgresql.dialect())` and
        assert the rendered text contains `FILTER (WHERE`, `LEFT OUTER JOIN`, `count(tasks.id)`
        and exactly one `FROM task_lists`, mirroring
        `test_the_aggregate_is_a_single_statement` in the sibling module. No server needed.
      - `test_listing_with_stats_returns_counters_as_python_ints` — `type(total) is int`, because
        psycopg hands `bigint` back and a later change of driver or dialect would be invisible
        otherwise.

    Flush the owner row before adding lists: `TaskListRow` declares a `ForeignKey` but no
    `relationship`, so SQLAlchemy has no dependency edge to order the INSERTs by (04-RESEARCH
    Pitfall 4). The module's `given_an_owner` helper already does this — reuse it.

    `tests/unit/infrastructure/test_adapter_ports.py` should need no edit once the adapter method
    exists; confirm it is green rather than assuming it, and if the existing test enumerates method
    names, add the new one there.
  </action>
  <verify>
    <automated>docker compose up -d db && sleep 5 && .venv/bin/pytest tests/integration/test_repositories_task_lists.py tests/unit/infrastructure/test_adapter_ports.py -x --no-cov -q && .venv/bin/mypy src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration/test_repositories_task_lists.py -x --no-cov -q` exits 0 with at least 6 new tests passing
    - `.venv/bin/pytest tests/unit/infrastructure/test_adapter_ports.py -q --no-cov` exits 0
    - `.venv/bin/python -c "import uuid; from sqlalchemy.dialects import postgresql; from taskmanager.infrastructure.db.repositories.task_lists import lists_with_stats_statement; s=str(lists_with_stats_statement(uuid.uuid4()).compile(dialect=postgresql.dialect())); assert 'FILTER (WHERE' in s and 'LEFT OUTER JOIN' in s and 'count(tasks.id)' in s and s.count('FROM task_lists') == 1, s"` exits 0
    - `.venv/bin/python -c "import uuid; from sqlalchemy.dialects import postgresql; from taskmanager.infrastructure.db.repositories.task_lists import lists_with_stats_statement; s=str(lists_with_stats_statement(uuid.uuid4()).compile(dialect=postgresql.dialect())); assert 'count(*)' not in s, s"` exits 0
    - `grep -rn "\.commit()" src/taskmanager/infrastructure/db/repositories/` prints nothing
    - `.venv/bin/mypy src tests` exits 0 with no `type: ignore` in the changed files
    - `.venv/bin/pytest tests/integration/test_repositories_task_lists.py -q --cov=taskmanager.infrastructure.db.repositories.task_lists --cov-report=term-missing --no-header` shows no missing lines
  </acceptance_criteria>
  <done>LIST-03's data comes from one grouped statement whose shape is asserted by compiled SQL, an empty list reports 0.0 rather than a phantom task, and the adapter still never ends a transaction.</done>
</task>

<task type="auto">
  <name>Task 3: D-15's import-linter contract, its name registration and the red capture</name>
  <files>.importlinter, tests/architecture/test_layer_boundaries.py, .planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt</files>
  <read_first>
    - `.importlinter` — the whole file: `include_external_packages = True` at L14 (required for a
      `forbidden` contract against an external package) and the `domain-framework-free` contract
      L29-L50, whose comment-block-then-contract shape is the one to copy
    - `tests/architecture/test_layer_boundaries.py` — the whole file: `EXPECTED_CONTRACT_NAMES`
      L19-L23 is asserted exactly, and L13 names the Phase 1 evidence file whose format this
      task's capture follows
    - `.planning/phases/01-foundation-quality-gates/evidence/import-linter-red-green.txt` — the
      capture format: the planted violation, the red output, the removal, the green output
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 9" — the exact contract text,
      already executed and reported KEPT, and the note that `infrastructure`'s only occurrence of
      `starlette` today is a prose mention inside a docstring, which import-linter does not see
    - `CLAUDE.md` §"Quality gates" — a new gate goes in both `.pre-commit-config.yaml` and
      `.github/workflows/ci.yml` **unless** it rides inside something both already run
  </read_first>
  <action>
    Add the `no-http-below-presentation` contract to `.importlinter` exactly as given in
    `<interfaces>`, preceded by a comment block in the style of `domain-framework-free`'s. The
    comment states what is deliberately in and out: `presentation` is absent from
    `source_modules` because it is the layer that is *supposed* to import FastAPI; `main` is absent
    for the same reason; and the forbidden list names the two packages rather than the specific
    `HTTPException` symbol, because an import-linter `forbidden` contract works on modules and the
    symbol-level question is what the AST gate in plan 04-08 answers.

    Add `"No web framework below presentation"` to `EXPECTED_CONTRACT_NAMES` in
    `tests/architecture/test_layer_boundaries.py` in the same change. That set is asserted exactly,
    so adding the contract without the name fails the suite — expected, and the guard working
    (04-RESEARCH Pitfall 9).

    Add **no** `.pre-commit-config.yaml` hook and **no** `.github/workflows/ci.yml` step: both
    already run `lint-imports` (the hook id `import-linter`, the CI step "Architecture contracts").
    Record that argument in the `.importlinter` comment, referencing ADR-015 the way 02-06 and
    03-06 did.

    Then falsify the gate rather than asserting it. Create
    `.planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt` capturing, in order:
    the planted violation (a `from fastapi import HTTPException` line temporarily added to a module
    under `src/taskmanager/application/`), the full verbatim output of
    `.venv/bin/lint-imports --no-cache` showing the contract BROKEN with the offending import
    named, the full verbatim output of `.venv/bin/pytest tests/architecture/test_layer_boundaries.py -q --no-cov`
    showing it red, then the same two commands after removing the planted line, both green with
    `Contracts: 4 kept, 0 broken.`. Head the file with the command lines and the date, as the
    Phase 1 capture does. Remove the planted import before finishing — `git status --porcelain`
    must show only the intended files.
  </action>
  <verify>
    <automated>.venv/bin/lint-imports --no-cache && .venv/bin/pytest tests/architecture -q --no-cov && make lint && make typecheck && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/lint-imports --no-cache` exits 0 and its output contains `Contracts: 4 kept, 0 broken.`
    - `.venv/bin/lint-imports --no-cache` output contains the line `No web framework below presentation KEPT`
    - `grep -c "no-http-below-presentation" .importlinter` prints `1`
    - `grep -c "No web framework below presentation" tests/architecture/test_layer_boundaries.py` prints `1`
    - `.venv/bin/pytest tests/architecture -q --no-cov` exits 0
    - `grep -c "import-linter\|lint-imports" .pre-commit-config.yaml` prints at least `1` and the file is unchanged (`git diff --stat .pre-commit-config.yaml .github/workflows/ci.yml` prints nothing)
    - `.planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt` exists and contains both `BROKEN` and `4 kept, 0 broken`
    - `git status --porcelain` lists no file outside this plan's `files_modified`
  </acceptance_criteria>
  <done>A business layer that imports the web framework fails a gate that was proven able to fail, and the proof is on disk rather than claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database rows → domain entities | The adapter is the only place a `Row` may exist; anything it returns crosses into `application` |
| owner_id → SQL predicate | `owner_id` originates from the caller's identity and is the sole scope of the LIST-03 query |
| layer graph → build outcome | An import that crosses a layer boundary must fail a command, not a review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Elevation of privilege | `list_for_owner_with_stats` cross-tenant leak | mitigate | `WHERE task_lists.owner_id = :owner_id` is the only scope, bound as a parameter, and `test_listing_with_stats_never_returns_another_owners_list` asserts it (ASVS V4, ADR-008) |
| T-4-07 | Information disclosure | a `Row` or an ORM exception crossing into `application` | mitigate | The port's return type names domain types only; `grep -c "Row" ports/repositories.py` is an acceptance criterion, and the `application-framework-free` contract forbids naming a row type there at all |
| T-4-08 | Denial of service | N+1 amplification on the index endpoint | mitigate | One grouped statement, its shape asserted from compiled SQL with no server; plan 04-10 adds the runtime statement counter on top |
| T-4-09 | Tampering | a business layer reaching for `fastapi.HTTPException` | mitigate | The `no-http-below-presentation` forbidden contract, registered in `EXPECTED_CONTRACT_NAMES` and proven able to fail with a planted violation |
| T-4-10 | Repudiation | a gate that passes vacuously | mitigate | The contract set is asserted exactly by `test_every_contract_is_configured`, so a silently dropped contract fails the suite; the red capture proves the new one is not decorative |
| T-4-11 | Integrity | an adapter ending its own transaction | mitigate | `tests/architecture/test_no_commit_in_repositories.py` already scans this package by text; the new method is a pure read and does not flush |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package; `requirements*.txt` unchanged |
</threat_model>

<verification>
- With `docker compose up -d db`: `make lint && make typecheck && make arch && make test` exits 0.
- `.venv/bin/lint-imports --no-cache` reports `Contracts: 4 kept, 0 broken.`
- The compiled `lists_with_stats_statement` contains `FILTER (WHERE`, `LEFT OUTER JOIN` and
  `count(tasks.id)`, and does not contain `count(*)`.
- `grep -rn "\.commit()" src/taskmanager/infrastructure/db/repositories/` prints nothing.
- `.planning/phases/04-task-lists-tasks/evidence/04-02-importlinter-red.txt` shows the contract
  BROKEN and then KEPT.
</verification>

<success_criteria>
- `list_for_owner_with_stats` exists on the port, the fake and the adapter, and all three
  conformance tests are green under mypy strict (LIST-03).
- LIST-03's counters come from one grouped statement, correct for an empty list, ordered by
  `(created_at, id)` in both the adapter and the fake (D-13).
- The D-15 import-linter contract is configured, registered by name, proven able to fail, and adds
  no pre-commit hook and no CI step.
</success_criteria>

<output>
Create `.planning/phases/04-task-lists-tasks/04-02-SUMMARY.md` when done
</output>
