---
phase: 04-task-lists-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_settings.py
  - AI_WORKFLOW.md
  - src/taskmanager/domain/entities/task.py
  - src/taskmanager/domain/entities/task_list.py
  - src/taskmanager/application/dto/unset.py
  - tests/unit/domain/test_task.py
  - tests/unit/domain/test_task_list.py
  - tests/unit/application/test_unset.py
autonomous: true
requirements: [LIST-04, TASK-01, TASK-03, TASK-08]

must_haves:
  truths:
    - "make test is green on the developer host: tests/unit/test_settings.py::test_get_settings_is_cached no longer depends on whether a .env file exists beside the repository root"
    - "TaskList.describe(description, *, now) and Task.describe(description, *, now) exist, validate before assigning, and stamp updated_at from the validated moment (LIST-04, TASK-03)"
    - "Task.reprioritise(priority, *, now) exists with the same guard-before-assign shape (TASK-03)"
    - "Task.DEFAULT_PRIORITY is the single copy of the `medium` default and Task.create reads it (TASK-01)"
    - "application/dto/unset.py exports a single-member Unset enum and UNSET, narrowing `str | Unset` to `str` under mypy strict — the typed marker Phase 4's PATCH commands use for `field not provided`"
  artifacts:
    - path: "src/taskmanager/application/dto/unset.py"
      provides: "The typed PATCH sentinel"
      contains: "class Unset"
    - path: "src/taskmanager/domain/entities/task.py"
      provides: "describe, reprioritise, DEFAULT_PRIORITY"
      contains: "DEFAULT_PRIORITY"
    - path: "src/taskmanager/domain/entities/task_list.py"
      provides: "describe"
      contains: "def describe"
  key_links:
    - from: "src/taskmanager/domain/entities/task.py"
      to: "src/taskmanager/domain/validation.py"
      via: "describe delegates to optional_text with the entity's own DESCRIPTION_MAX_LENGTH"
      pattern: "optional_text"
    - from: "src/taskmanager/domain/entities/task.py"
      to: "src/taskmanager/domain/value_objects/task_priority.py"
      via: "DEFAULT_PRIORITY is a ClassVar holding TaskPriority.MEDIUM and Task.create's default reads it"
      pattern: "DEFAULT_PRIORITY"
---

<objective>
Clear the three blockers that every other Phase 4 plan stands on: the pre-existing red test that
would make `make test` a dishonest commit gate, the four domain mutators the PATCH endpoints call,
and the typed `Unset` sentinel the PATCH commands are built from.

Purpose: `make test` is a gate before every commit in this project (CLAUDE.md). A test that is red
for an environmental reason makes the gate meaningless from the first Phase 4 commit onward. The
entity mutators and the sentinel are the two things a plan that "assumes they exist" would
discover at its first mypy run (04-RESEARCH Pitfall 2).
Output: a hermetic settings test, `Task.describe` / `Task.reprioritise` / `Task.DEFAULT_PRIORITY`,
`TaskList.describe`, and `application/dto/unset.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-lists-tasks/04-CONTEXT.md
@.planning/phases/04-task-lists-tasks/04-RESEARCH.md
@.planning/phases/04-task-lists-tasks/04-PATTERNS.md
@.planning/phases/04-task-lists-tasks/04-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- The existing mutator shape, verbatim from src/taskmanager/domain/entities/task.py L133-L144. -->

```python
    def rename(self, title: str, *, now: datetime) -> None:
        """Replace the title, applying the same guard construction applied."""
        moment = require_utc(now, field="now")
        self.title = require_text(
            title, field="title", max_length=self.TITLE_MAX_LENGTH
        )
        self.updated_at = moment
```

Entity ClassVars that already exist: `Task.TITLE_MAX_LENGTH = 200`,
`Task.DESCRIPTION_MAX_LENGTH = 2000`, `TaskList.NAME_MAX_LENGTH = 120`,
`TaskList.DESCRIPTION_MAX_LENGTH = 2000`.

The sentinel shape verified under this repository's mypy 2.3.1 --strict
(04-RESEARCH Pattern 1):

```python
class Unset(Enum):
    TOKEN = "unset"


UNSET: Final = Unset.TOKEN
```

Revealed types inside `if value is not UNSET:` — `str | Unset` becomes `str`,
`str | None | Unset` becomes `str | None`; omitting the guard is an `arg-type` error.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Make the settings cache test hermetic and record the incident</name>
  <files>tests/unit/test_settings.py, AI_WORKFLOW.md</files>
  <read_first>
    - `tests/unit/test_settings.py` — the whole file; `test_get_settings_is_cached` is the only
      failing test and the other seven show the `Settings(_env_file=None)` convention in use
    - `src/taskmanager/infrastructure/config/settings.py` — `model_config` sets
      `env_file=".env"`, which pydantic-settings resolves relative to the process working
      directory, and `get_settings()` is `lru_cache(maxsize=1)` over a bare `Settings()`
    - `AI_WORKFLOW.md` — the dated incident-log format used since Phase 1
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Environment Availability" — the
      confirmed diagnosis and the measured baseline (`1 failed, 292 passed`)
  </read_first>
  <action>
    `get_settings()` constructs `Settings()` with the declared `env_file=".env"`, so on a developer
    host it reads the repository's real `.env` (which exports `TEST_DATABASE_URL`), while the test's
    comparison object `Settings(_env_file=None)` does not. The two differ on `test_database_url`
    and the equality assertion fails. CI and the Docker `test` stage have no `.env`, so it passes
    there.

    Fix the test's isolation, not the assertion. Add `tmp_path: Path` to
    `test_get_settings_is_cached`'s signature and call `monkeypatch.chdir(tmp_path)` before
    `get_settings.cache_clear()`, so the relative `.env` name resolves to a directory that has no
    such file and both constructions read exactly the monkeypatched environment. Keep all three
    existing assertions unchanged — `first is second`, `first == Settings(_env_file=None)` and
    `first.database_url == ENV["DATABASE_URL"]` — and keep the `try/finally` that clears the cache
    on the way out. Do not add `_env_file=None` to the `get_settings()` call path, do not add
    `pytest.mark.skipif`, and do not delete the test: each of those weakens the claim the test
    exists to make, which is that the cached instance carries the *current* environment.

    Extend the test's existing comment block with one sentence naming why the working directory is
    changed: the declared `env_file` is a relative name, so a developer host with a real `.env`
    beside the repository root would otherwise make this test's outcome depend on an untracked
    file.

    Append one dated entry to `AI_WORKFLOW.md`'s incident log in the established format: the
    failure was pre-existing and host-specific, it was found by the Phase 4 researcher running the
    baseline suite (`1 failed, 292 passed`) rather than by CI, the cause was a relative `env_file`
    meeting an untracked `.env`, and the fix changed the test's isolation rather than its
    assertion. Name the file and the test.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_settings.py -q --no-cov && .venv/bin/pytest tests/unit tests/architecture -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_settings.py -q --no-cov` exits 0 with 8 tests passing
    - `.venv/bin/pytest tests/unit/test_settings.py::test_get_settings_is_cached -q --no-cov` exits 0 while a real `.env` exists at the repository root (confirm with `ls .env`)
    - `grep -c "skipif\|xfail" tests/unit/test_settings.py` prints `0`
    - `grep -c "first == Settings(_env_file=None)" tests/unit/test_settings.py` prints `1`
    - `grep -c "monkeypatch.chdir" tests/unit/test_settings.py` prints `1`
    - `grep -c "test_get_settings_is_cached" AI_WORKFLOW.md` prints at least `1`
    - `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0 with 0 failures
  </acceptance_criteria>
  <done>`make test`'s unit half is green on a host that has a `.env`, and the reason it was not is recorded rather than silently fixed.</done>
</task>

<task type="auto">
  <name>Task 2: The four domain mutators the PATCH endpoints require</name>
  <files>src/taskmanager/domain/entities/task.py, src/taskmanager/domain/entities/task_list.py, tests/unit/domain/test_task.py, tests/unit/domain/test_task_list.py</files>
  <read_first>
    - `src/taskmanager/domain/entities/task.py` — the whole file: the ClassVar block L38-L41, the
      `__post_init__` `optional_text` call L63-L67, `create` L90+ (its
      `priority: TaskPriority = TaskPriority.MEDIUM` default), and `rename` L133-L144 /
      `reschedule` L146-L159, which are the shape to copy
    - `src/taskmanager/domain/entities/task_list.py` — the whole file: ClassVars L39/L42,
      `__post_init__` L54-L60, `rename` L89-L96
    - `src/taskmanager/domain/validation.py` — `require_text`, `optional_text`, `require_utc` and
      exactly which `field=` and `max_length=` keywords they take
    - `tests/unit/domain/test_task.py` and `tests/unit/domain/test_task_list.py` — the existing
      mutator tests, for naming, fixed instants and the negative-case style
    - `.planning/phases/04-task-lists-tasks/04-PATTERNS.md` §17 — the three rules, and the note
      that both entity docstrings already announce this change
  </read_first>
  <action>
    Add to `TaskList`:
      - `describe(self, description: str | None, *, now: datetime) -> None` — validate `now` with
        `require_utc(now, field="now")` first, then assign
        `optional_text(description, field="description", max_length=self.DESCRIPTION_MAX_LENGTH)`,
        then stamp `self.updated_at` from the validated moment. `optional_text` already folds `""`
        to `None`, so an explicit empty string clears the field exactly as an explicit JSON null
        does (D-05).

    Add to `Task`:
      - `describe(self, description: str | None, *, now: datetime) -> None` — identical shape,
        using `Task.DESCRIPTION_MAX_LENGTH`.
      - `reprioritise(self, priority: TaskPriority, *, now: datetime) -> None` — validate `now`,
        assign `self.priority = priority`, stamp `updated_at`. No value guard: `TaskPriority` is a
        `StrEnum` and the boundary already refuses anything that is not a member, so a guard here
        would be an unreachable branch the 100% coverage norm could not cover.
      - `DEFAULT_PRIORITY: ClassVar[TaskPriority] = TaskPriority.MEDIUM` in the existing ClassVar
        block, with a comment naming TASK-01 as the rule and Phase 2 D-04 as the reason it lives in
        one place. Change `Task.create`'s signature so its `priority` default reads
        `DEFAULT_PRIORITY` instead of spelling `TaskPriority.MEDIUM` a second time — the schema
        default in plan 04-07 reads the same ClassVar, so `medium` exists exactly once
        (04-RESEARCH Pitfall 6).

    Every mutator is keyword-only on `now`, validates everything before assigning anything, and
    stamps `updated_at` from the validated moment — the three rules `rename`'s comment already
    argues for. Update the "Phase 4 adds the ones its PATCH endpoints require" sentence in both
    entity docstrings now that the mutators have landed.

    Extend `tests/unit/domain/test_task_list.py` with: describe sets a description and moves
    `updated_at`; describe with `None` clears it; describe with `""` clears it; describe with
    an over-length string raises `ValidationError` carrying `field: description` and leaves the
    entity byte-identical (assert both the old description and the old `updated_at`); describe
    with a naive datetime raises `ValidationError` carrying `field: now` and changes nothing.

    Extend `tests/unit/domain/test_task.py` with the same five describe cases plus: reprioritise
    moves `priority` and `updated_at`; reprioritise with a naive `now` raises and changes nothing;
    and a test asserting `Task.DEFAULT_PRIORITY is TaskPriority.MEDIUM` and that a `Task.create`
    call which omits `priority` produces `Task.DEFAULT_PRIORITY`, so the two cannot drift apart.

    Use the modules' existing fixed UUID and datetime constants. No `uuid4()`, no `datetime.now()`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/domain/test_task.py tests/unit/domain/test_task_list.py -q --no-cov && .venv/bin/mypy src tests && .venv/bin/pytest tests/unit tests/architecture -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/domain/test_task.py tests/unit/domain/test_task_list.py -q --no-cov` exits 0
    - `.venv/bin/python -c "from taskmanager.domain.entities.task import Task; from taskmanager.domain.value_objects.task_priority import TaskPriority; assert Task.DEFAULT_PRIORITY is TaskPriority.MEDIUM; assert hasattr(Task, 'describe') and hasattr(Task, 'reprioritise')"` exits 0
    - `.venv/bin/python -c "import inspect; from taskmanager.domain.entities.task import Task; assert inspect.signature(Task.create).parameters['priority'].default is Task.DEFAULT_PRIORITY"` exits 0
    - `.venv/bin/python -c "from taskmanager.domain.entities.task_list import TaskList; assert hasattr(TaskList, 'describe')"` exits 0
    - `grep -c "TaskPriority.MEDIUM" src/taskmanager/domain/entities/task.py` prints `1`
    - `.venv/bin/mypy src tests` exits 0
    - `.venv/bin/pytest tests/unit/domain --cov=taskmanager.domain.entities --cov-report=term-missing --no-header -q` shows `task.py` and `task_list.py` with no missing lines
  </acceptance_criteria>
  <done>The PATCH endpoints have every mutator they call, each following the validate-then-assign convention, and `medium` is spelled once in the whole project.</done>
</task>

<task type="auto">
  <name>Task 3: The typed Unset sentinel for PATCH commands</name>
  <files>src/taskmanager/application/dto/unset.py, tests/unit/application/test_unset.py</files>
  <read_first>
    - `src/taskmanager/application/dto/commands.py` — the module docstring, which states the DTO
      convention the sentinel serves, and the import style of the package
    - `src/taskmanager/domain/value_objects/task_status.py` — the nearest analog for a
      module-level enum plus a module constant, and its docstring register
    - `.planning/phases/04-task-lists-tasks/04-RESEARCH.md` §"Pattern 1" — the verified body, the
      mypy narrowing output, and §"Alternatives Considered" for the two rejected shapes
    - `CLAUDE.md` §"Project Rules" — `application` imports no web framework and no ORM, so the
      sentinel must be stdlib
  </read_first>
  <action>
    Create `src/taskmanager/application/dto/unset.py` with a single-member `enum.Enum` named
    `Unset` holding one member `TOKEN = "unset"`, and a module constant `UNSET: Final = Unset.TOKEN`.
    Import only `enum` and `typing.Final`; nothing else, and nothing third-party.

    The module docstring must follow the project's register — state the rejected alternatives and
    why, not what the code does. Name all three: `None` cannot serve because it is a legal *value*
    for every nullable PATCH field, so it cannot also mean absent; a bare `object()` sentinel gives
    mypy nothing to narrow on, so a forgotten guard becomes a runtime surprise instead of an
    `arg-type` error; and a per-field `Patch[T]` wrapper narrows correctly but makes every read
    `command.field.value` behind `command.field.provided`, adding a second DTO vocabulary for no
    gain. State that the sentinel deliberately stops at the application boundary: putting it in the
    Pydantic request schemas was executed and rejected because it leaks a `_Unset` component into
    `/openapi.json` and splits the explicit-null error into two unreadable entries at
    `body.title.str` and `body.title.enum[_Unset]` (04-RESEARCH Pattern 2).

    Create `tests/unit/application/test_unset.py` with: `UNSET is Unset.TOKEN`; `Unset` has exactly
    one member (`len(list(Unset)) == 1`); `UNSET is not None` and `UNSET != None`, so it cannot be
    confused with the value it exists to be distinguishable from; and a narrowing test that calls a
    module-level helper typed `str | Unset -> str` which returns the value only inside
    `if value is not UNSET`, proving at runtime the branch mypy proves statically.

    Do not add the sentinel to any command in this plan — plan 04-04 owns the commands.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/application/test_unset.py -q --no-cov && .venv/bin/mypy src tests && make lint && make arch</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/application/test_unset.py -q --no-cov` exits 0 with at least 4 tests passing
    - `.venv/bin/python -c "from taskmanager.application.dto.unset import UNSET, Unset; assert UNSET is Unset.TOKEN; assert len(list(Unset)) == 1"` exits 0
    - `grep -cE "^(import|from) " src/taskmanager/application/dto/unset.py` prints `2` and neither line names a third-party package
    - `.venv/bin/mypy src tests` exits 0 with no `type: ignore` in the new file
    - `make lint` exits 0
    - `make arch` exits 0 and reports `Contracts: 3 kept, 0 broken.`
    - `.venv/bin/pytest tests/unit/application/test_unset.py -q --cov=taskmanager.application.dto.unset --cov-report=term-missing --no-header` shows no missing lines
  </acceptance_criteria>
  <done>The application layer has a stdlib-only, mypy-narrowable marker for "this PATCH field was not provided", and the reason it is not a Pydantic construct is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untracked host file (`.env`) → the commit gate | A gate whose outcome depends on a file outside version control is not a gate |
| caller-supplied PATCH field → entity mutator | The mutators added here are the first code an HTTP body reaches once plan 04-08 lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Repudiation | `make test` as a commit gate | mitigate | Task 1 makes the failing test hermetic by changing the working directory, so the gate's verdict no longer depends on an untracked file; the incident is recorded in `AI_WORKFLOW.md` rather than the test being skipped |
| T-4-02 | Tampering | `Task.describe` / `TaskList.describe` over-length input | mitigate | Both delegate to `optional_text(..., max_length=self.DESCRIPTION_MAX_LENGTH)`, the one copy of the limit; the guard runs before any assignment, so a refused call leaves the aggregate byte-identical and a test asserts that |
| T-4-03 | Tampering | half-applied mutation on a refused call | mitigate | Validate-before-assign is copied from `rename`, whose comment records the defect it fixes; each new negative test asserts the old value *and* the old `updated_at` |
| T-4-04 | Elevation of privilege | `Task.reprioritise` accepting an out-of-range priority | mitigate | Typed `TaskPriority`; the enum-typed query/body parameter at the boundary (plan 04-07) refuses anything else before the entity is reached, so no free text can arrive |
| T-4-05 | Information disclosure | the `Unset` sentinel leaking into the public API | mitigate | The sentinel is confined to `application/dto/`; the measured OpenAPI leak is named in the module docstring so a later plan cannot reintroduce it by accident |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package; `requirements.txt` and `requirements-dev.txt` are unchanged (04-RESEARCH §"Package Legitimacy Audit" — not applicable) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit tests/architecture -q --no-cov` exits 0 with zero failures.
- `.venv/bin/pytest tests/unit/test_settings.py -q --no-cov` exits 0 while a real `.env` exists.
- `make lint && make typecheck && make arch` all exit 0.
- `make test` exits 0 with the `db` container running (`docker compose up -d db`) and reports
  `Required test coverage of 75% reached`.
- `grep -rn "pragma: no cover" src/taskmanager/` prints nothing.
</verification>

<success_criteria>
- The developer-host baseline is green: no pre-existing red remains to mask a Phase 4 regression.
- `TaskList.describe`, `Task.describe` and `Task.reprioritise` exist with the project's
  validate-then-assign shape and full negative coverage.
- `Task.DEFAULT_PRIORITY` is the only occurrence of `TaskPriority.MEDIUM` in the entity module and
  `Task.create` reads it.
- `application/dto/unset.py` narrows under `mypy --strict`, imports only stdlib, and records why the
  sentinel never crosses into a Pydantic schema.
</success_criteria>

<output>
Create `.planning/phases/04-task-lists-tasks/04-01-SUMMARY.md` when done
</output>
