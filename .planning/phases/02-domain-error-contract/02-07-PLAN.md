---
phase: 02-domain-error-contract
plan: 07
type: execute
wave: 5
depends_on: ["02-05", "02-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .importlinter
  - CLAUDE.md
  - AI_WORKFLOW.md
autonomous: true
requirements: [ARC-04, ARC-02]
must_haves:
  truths:
    - "User decision C-02: ARC-05 in REQUIREMENTS.md and ROADMAP Phase 4 success criterion 5 both read 'HTTP request/response schemas and settings' and name frozen dataclasses for application command/result DTOs, so Phase 4 verification cannot fail against its own text"
    - "User decision C-02: the stale .importlinter comment claiming 'DTOs are pydantic models' is corrected, while the three contracts and their names are byte-identical, so tests/architecture/test_layer_boundaries.py::test_every_contract_is_configured still passes with an unchanged three-element EXPECTED_CONTRACT_NAMES"
    - "User decision C-02: the CLAUDE.md Project Rules line now says Pydantic is permitted in the application layer while command and result DTOs are frozen dataclasses per ADR-020"
    - "pydantic is deliberately NOT added to the application-framework-free forbidden_modules list - enforcement stays permissive by decision, not by omission"
    - "AI_WORKFLOW.md carries a Phase 2 section and dated incident entries written from captured output, appended at the end of the phase rather than reconstructed in Phase 7"
    - "The whole phase gate has been observed green in one uninterrupted run: make lint && make typecheck && make arch && make test, coverage 100% over taskmanager with no pragma and no omit, zero warnings"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ARC-05 reworded to match ADR-020"
      contains: "frozen dataclasses"
    - path: ".importlinter"
      provides: "corrected application-layer comment, contracts untouched"
      contains: "ADR-020"
    - path: "CLAUDE.md"
      provides: "corrected application-layer DTO rule"
      contains: "frozen dataclasses"
    - path: "AI_WORKFLOW.md"
      provides: "Phase 2 human-versus-AI split and dated incident entries quoting real captures"
      contains: "Phase 2"
  key_links:
    - from: ".planning/REQUIREMENTS.md ARC-05"
      to: "DECISION_LOG.md ADR-020"
      via: "explicit ADR reference in the requirement text"
      pattern: "ADR-020"
    - from: ".importlinter"
      to: "DECISION_LOG.md ADR-020"
      via: "comment naming the decision that makes the permissive contract deliberate"
      pattern: "ADR-020"
    - from: "AI_WORKFLOW.md"
      to: ".planning/phases/02-domain-error-contract/evidence/domain-stdlib-red-green.txt"
      via: "incident entry quoting the captured red/green output verbatim"
      pattern: "domain-stdlib-red-green"
---

<objective>
Finish the phase by making the documentation agree with the shipped code: reconcile the four
artifacts that still claim application DTOs are Pydantic models, append the Phase 2 section
and incident entries to `AI_WORKFLOW.md`, and capture the whole gate green in one
uninterrupted run.

Purpose: the project's thesis is that its documentation is verified rather than asserted. A
requirement whose text contradicts the code is the exact failure this deliverable is
supposed to demonstrate immunity to - and ARC-05's current wording would make Phase 4's own
acceptance criterion unsatisfiable. `AI_WORKFLOW.md` is appended to at the end of every
phase for the same reason: reconstructing it in Phase 7 would undermine the claim it makes.
Output: four corrected artifacts, a Phase 2 `AI_WORKFLOW.md` section, and a captured
full-gate run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-error-contract/02-CONTEXT.md
@.planning/phases/02-domain-error-contract/02-RESEARCH.md
@.planning/phases/02-domain-error-contract/02-PATTERNS.md
@.planning/phases/02-domain-error-contract/02-05-SUMMARY.md
@.planning/phases/02-domain-error-contract/02-06-SUMMARY.md

<interfaces>
The exact target text of the four amendments.

  # .planning/REQUIREMENTS.md, the ARC-05 bullet (currently line 32)
  BEFORE: "Pydantic v2 models type every boundary: HTTP request/response schemas,
           application command/result DTOs, settings [PDF 2.b]"
  AFTER:  "Pydantic v2 models type every HTTP boundary: request/response schemas and
           settings; application command/result DTOs are frozen dataclasses (ADR-020)
           [PDF 2.b]"
  The `- [ ] **ARC-05**:` prefix and the `[PDF 2.b]` citation must survive unchanged.

  # .planning/ROADMAP.md, Phase 4 success criterion 5 (currently line 127)
  BEFORE: "Pydantic v2 models type every boundary crossed in this slice (HTTP
           request/response schemas and application command/result DTOs), and no router
           imports SQLAlchemy or raises `HTTPException` for a business failure."
  AFTER:  "Pydantic v2 models type every HTTP boundary crossed in this slice (request and
           response schemas), application command/result DTOs are frozen dataclasses per
           ADR-020, and no router imports SQLAlchemy or raises `HTTPException` for a
           business failure."
  The "  5. " numbering and indentation must survive unchanged.

  # .importlinter, the comment above [importlinter:contract:application-framework-free]
  BEFORE: "# The application layer orchestrates use cases through ports. It may use pydantic
           # (DTOs are pydantic models), but it must never see the web framework or the ORM."
  AFTER:  a comment that keeps the first sentence, states that pydantic remains permitted
          here deliberately - the contract is not tightened, because a later phase may
          legitimately want a Pydantic DTO - and that ADR-020 chooses frozen dataclasses
          for command and result DTOs, so nothing in the layer imports it today; closing
          with the unchanged point that the layer must never see the web framework or the
          ORM. Comment lines only. The `[importlinter...]` sections, the contract `name`
          values and every `forbidden_modules` entry stay byte-identical.

  # CLAUDE.md, Project Rules -> Layers, the application bullet (currently lines 280-282)
  BEFORE: "Pydantic *is* allowed there - application DTOs are Pydantic models."
  AFTER:  "Pydantic *is* permitted there and the contract is deliberately not tightened,
           but application command and result DTOs are `@dataclass(frozen=True,
           slots=True)` per ADR-020, so nothing in the layer imports it today."

Out of scope for this plan: the `## Conventions` and `## Architecture` sections of
`CLAUDE.md`. They sit inside `<!-- GSD:* -->` delimiter blocks and are regenerated by
tooling; hand-edits there are lost. Only the hand-maintained `## Project Rules` section is
touched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile ARC-05, ROADMAP SC-5, the .importlinter comment and the CLAUDE.md rule</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .importlinter, CLAUDE.md</files>
  <read_first>
    - `.planning/phases/02-domain-error-contract/02-RESEARCH.md` section "Conflicts -> C-02" - the five-source contradiction table and the three recommended amendments, including the explicit instruction not to add `pydantic` to `forbidden_modules`
    - `DECISION_LOG.md` ADR-020 (appended by plan 02-06) - the decision these four edits propagate; quote its id, never restate its reasoning at length
    - `.planning/REQUIREMENTS.md` around the ARC-05 bullet - the exact prefix, bold-id and citation format of its neighbours
    - `.planning/ROADMAP.md` Phase 4 "Success Criteria" block - the numbering and indentation of its neighbours
    - `.importlinter` in full - the header comment voice ("comments state the failure mode being prevented") and the three contracts that must not move
    - `CLAUDE.md` section "Project Rules -> Layers" - the bullet to amend and the surrounding voice
    - `tests/architecture/test_layer_boundaries.py` lines 19-23 - `EXPECTED_CONTRACT_NAMES`, which this task must leave valid without editing the test
  </read_first>
  <action>
Apply the four amendments exactly as specified in `<interfaces>`. Each is a wording change
that propagates ADR-020; none of them changes behaviour, and none of them may change an
identifier a test or a tool reads.

Order of work and the traps in each:

1. `.planning/REQUIREMENTS.md` - edit the ARC-05 bullet in place. Keep the
   `- [ ] **ARC-05**:` prefix and the trailing `[PDF 2.b]` citation. Do not tick the
   checkbox: ARC-05 is a Phase 4 requirement and is still unmet.
2. `.planning/ROADMAP.md` - edit Phase 4 success criterion 5. Touch nothing else in the
   file; the Phase 2 `**Plans**:` list was already filled in during planning and the phase
   status lines are owned by the execution tooling.
3. `.importlinter` - replace only the two stale comment lines above
   `[importlinter:contract:application-framework-free]`. This is the highest-risk edit in
   the plan: the file is read by `lint-imports`, by `make arch`, by pre-commit, by CI and by
   `tests/architecture/test_layer_boundaries.py`, which asserts an exact three-element set
   of contract names. Do not add a contract, do not rename one, and do not add `pydantic`
   to the application contract's `forbidden_modules` - CONTEXT lists the current contract
   shape as locked, and enforcing D-16 mechanically is explicitly out of scope. After the
   edit, `.importlinter` must still hold exactly three `[importlinter:contract:` headers and
   `lint-imports` must still report all three KEPT.
4. `CLAUDE.md` - edit the application-layer bullet inside the hand-maintained
   `## Project Rules` section. Leave every `<!-- GSD:* -->` block alone; edits inside them
   are destroyed on the next regeneration. Em dashes are the house style in this file, so
   keep the surrounding punctuation consistent with its neighbours.

Then confirm nothing downstream broke: `.venv/bin/pytest tests/architecture -q --no-cov`,
`.venv/bin/lint-imports`, and `git diff --stat` showing exactly these four files.
  </action>
  <verify>
    <automated>grep -q 'ARC-05' .planning/REQUIREMENTS.md && grep -A0 'ARC-05' .planning/REQUIREMENTS.md | grep -q 'frozen dataclasses' && grep -q 'ADR-020' .planning/REQUIREMENTS.md && ! grep -q 'application command/result DTOs, settings' .planning/REQUIREMENTS.md && grep -q 'ADR-020' .planning/ROADMAP.md && ! grep -q 'HTTP request/response schemas and application command/result DTOs' .planning/ROADMAP.md && ! grep -q 'DTOs are pydantic models' .importlinter && grep -q 'ADR-020' .importlinter && test "$(grep -c '^\[importlinter:contract:' .importlinter)" -eq 3 && test "$(grep -c '^name = ' .importlinter)" -eq 3 && ! grep -A12 'application-framework-free' .importlinter | grep -qx '    pydantic' && grep -q 'frozen dataclasses' CLAUDE.md && ! grep -q 'application DTOs are$' CLAUDE.md && .venv/bin/pytest tests/architecture -q --no-cov && .venv/bin/lint-imports && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` ARC-05 reads "Pydantic v2 models type every HTTP boundary: request/response schemas and settings; application command/result DTOs are frozen dataclasses (ADR-020) [PDF 2.b]", still prefixed `- [ ] **ARC-05**:` and still unticked
    - The literal string `application command/result DTOs, settings` no longer appears in `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md` Phase 4 success criterion 5 names frozen dataclasses and ADR-020; the literal string `HTTP request/response schemas and application command/result DTOs` no longer appears
    - `.importlinter` contains no `DTOs are pydantic models`, contains `ADR-020`, and still has exactly three `[importlinter:contract:` headers and three `name = ` lines
    - The `application-framework-free` contract's `forbidden_modules` list still does not contain `pydantic`
    - `CLAUDE.md` Project Rules states Pydantic is permitted in `application` while command and result DTOs are frozen dataclasses per ADR-020; no `<!-- GSD:` block was modified
    - `tests/architecture/test_layer_boundaries.py` passes unmodified, with `EXPECTED_CONTRACT_NAMES` still a three-element set
    - `.venv/bin/lint-imports` exits 0 reporting all three contracts KEPT, and `.venv/bin/pytest -q` exits 0
    - `git diff --stat` lists exactly `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.importlinter` and `CLAUDE.md`
  </acceptance_criteria>
  <done>All four artifacts now say what the code does, the permissive contract is documented as deliberate, and no tool-read identifier changed.</done>
</task>

<task type="auto">
  <name>Task 2: append the Phase 2 record to AI_WORKFLOW.md and capture the full gate green</name>
  <files>AI_WORKFLOW.md</files>
  <read_first>
    - `AI_WORKFLOW.md` in full - the five-section skeleton, the "What is already true of Phase 1:" / "**Decided by the human**" / "**Delegated to AI**" structure at lines 33-64, the "Verification Practices" bullet voice at lines 68-92, and the Incident Log preamble at lines 96-101 plus the four existing `### YYYY-MM-DD - <title>` entries
    - `.planning/STATE.md` "Blockers/Concerns" - the standing rule that `AI_WORKFLOW.md` is appended to at the end of every phase and that reconstructing it in Phase 7 would undermine the project's own thesis
    - `.planning/phases/02-domain-error-contract/evidence/domain-stdlib-red-green.txt` (plan 02-06) - the capture to quote verbatim
    - `.planning/phases/02-domain-error-contract/02-0{1,2,3,4,5,6}-SUMMARY.md` - what actually happened, including any gate that fired unexpectedly during execution
    - `DECISION_LOG.md` ADR-020, ADR-021, ADR-022 - the decisions to cite by id rather than re-argue
    - `.planning/phases/01-foundation-quality-gates/01-07-SUMMARY.md` - how Phase 1 wrote this file, including the rule that quoted output is verbatim and that only real incidents are listed
  </read_first>
  <action>
Append the Phase 2 record to `AI_WORKFLOW.md`, matching the existing structure rather than
inventing a new one.

1. In "Human-Decided vs AI-Delegated", after the Phase 1 block, add a
   "What is already true of Phase 2:" block with the same two bold sub-headings.
   **Decided by the human**: the task transition matrix and that a same-state request is an
   idempotent no-op rather than a 409 (D-01, D-02); that `completed_at` is set on entering
   and cleared on leaving `completed` (D-03); the RFC 9457 member set and the URN `type`
   scheme (D-05, D-06); that an unexpected error returns one fixed body in every
   environment (D-08); that application DTOs are frozen dataclasses rather than Pydantic
   models, resolving a contradiction across five artifacts (ADR-020); that a domain
   `ValidationError` maps to 422 (ADR-021); and that `CompletionStats` ships now so the
   repository port has a real return type. Each claim points at a decision id or a file, per
   the section's own rule.
   **Delegated to AI**: executing candidate `DomainError` base shapes against the real
   `.flake8` before one was adopted; discovering that mypy strict rejects the natural
   exception-handler signature; discovering that `ServerErrorMiddleware` always re-raises;
   writing the entities, ports, handlers and the ~60 tests; and the AST import scanner.
2. In "Verification Practices", add one bullet for the new gate: an AST test asserting that
   every import in `taskmanager.domain` resolves to `sys.stdlib_module_names` or to
   `taskmanager`, which closes the gap an enumerated `forbidden` contract cannot - and note
   it was observed red on a planted import that `lint-imports` reported as KEPT.
3. In "Incident Log", append **at least three** dated `### YYYY-MM-DD - <title>` entries,
   inserted before the `## What I Did Not Do` section, each following the existing entries'
   shape: what happened, the evidence, and what changed as a result. The three that are
   owed, because each is a documented case of this project's own artifacts being wrong:
   - The project's own `ARCHITECTURE.md` Pattern 6 prescribed an exception base class that
     this repository's flake8 rejects with B042 - and the B042-clean leaf subclass that
     research then tried *passes* the linter while genuinely breaking `pickle` and
     `copy.copy`. Quote the B042 message and the `AttributeError` verbatim.
   - A roadmap success criterion claimed something no gate could prove: an enumerated
     `forbidden` contract cannot show that the domain imports *no* third-party library, and
     the `forbidden_modules = *` escape hatch also forbids `dataclasses`, `datetime` and
     `enum`. Quote the captured contrast from
     `.planning/phases/02-domain-error-contract/evidence/domain-stdlib-red-green.txt`:
     the planted `import greenlet` turning the new test red while `lint-imports` exited 0
     with `Domain is framework-free` KEPT.
   - Five artifacts asserted that application DTOs are Pydantic models while the newest
     user decision said frozen dataclasses. It was resolved by a human decision plus an
     append-only ADR and four wording amendments (Task 1) - not by quietly editing ADR-004,
     and not by leaving one requirement unsatisfiable.
   Add a fourth entry only if a further real incident occurred during Phase 2 execution -
   for example `E301` firing on black's own formatting of a wrapping Protocol stub, or the
   500 test seeing the probe's `RuntimeError` instead of a response. Do **not** pad: the
   file's own preamble says a log padded with plausible-sounding mistakes is worse than a
   short one. Every number and every quoted line must come from a real capture; if a
   sentence cannot be traced to a file, a commit or a captured run, delete it.
   Leave the `_To be completed in Phase 7._` markers and the "How This Project Was Built"
   placeholder exactly as they are - Phase 7 owns them.

Then run the whole phase gate in one uninterrupted sequence, `make format` first and proven
to be a no-op, and record the observed numbers (test count, statement count, coverage
percentage, warning count) in the summary:

  make format && git diff --quiet && make lint && make typecheck && make arch && make test

and confirm coverage over `taskmanager` is 100% with no `pragma: no cover` anywhere and no
`omit` entry in `pyproject.toml`.
  </action>
  <verify>
    <automated>grep -q 'Phase 2' AI_WORKFLOW.md && grep -q 'B042' AI_WORKFLOW.md && grep -q 'greenlet' AI_WORKFLOW.md && grep -q 'domain-stdlib-red-green' AI_WORKFLOW.md && grep -q 'ADR-020' AI_WORKFLOW.md && grep -q 'stdlib_module_names' AI_WORKFLOW.md && test "$(grep -c '^### 20' AI_WORKFLOW.md)" -ge 7 && grep -q '_To be completed in Phase 7._' AI_WORKFLOW.md && grep -q '^## What I Did Not Do' AI_WORKFLOW.md && test "$(git diff -- AI_WORKFLOW.md | grep -c '^-[^-]')" -eq 0 && ! grep -rq 'pragma: no cover' src tests && ! grep -q '^omit' pyproject.toml && make format && git diff --quiet -- src tests && make lint && make typecheck && make arch && make test && .venv/bin/coverage report --include='*/taskmanager/*' --fail-under=100</automated>
  </verify>
  <acceptance_criteria>
    - `AI_WORKFLOW.md` contains a "What is already true of Phase 2:" block with both `**Decided by the human**` and `**Delegated to AI**` sub-headings
    - "Verification Practices" gains one bullet naming the AST stdlib-only test and `sys.stdlib_module_names`
    - `grep -c '^### 20' AI_WORKFLOW.md` returns at least 7 (the four Phase 1 entries plus at least three Phase 2 entries)
    - The new entries contain the literal strings `B042`, `greenlet` and `domain-stdlib-red-green`, and quote the captured `lint-imports` KEPT line for the planted `greenlet` import
    - One entry states that ADR-004 was refined by an appended ADR-020 rather than edited
    - `git diff -- AI_WORKFLOW.md | grep -c '^-[^-]'` returns 0 - the file was appended to and its existing entries were not rewritten
    - `_To be completed in Phase 7._` and the `## What I Did Not Do` heading are still present
    - `grep -rc 'pragma: no cover' src tests` returns 0 for every file and `pyproject.toml` contains no `omit` entry
    - `make format` produces no diff under `src` and `tests`, then `make lint && make typecheck && make arch && make test` all exit 0 in the same run
    - `.venv/bin/coverage report --include='*/taskmanager/*' --fail-under=100` exits 0, and `make test` reports zero warnings
  </acceptance_criteria>
  <done>The phase's own record is written while the evidence is fresh, and the whole gate has been observed green in one uninterrupted run at 100% coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> enforcement config | `.importlinter` is read by four gates; a careless edit to it can disable a boundary check while the prose still claims it |
| Requirement text -> future verification | Phase 4 is verified against ARC-05's literal wording; text that contradicts the code makes a later phase unverifiable |
| Project claims -> evidence | `AI_WORKFLOW.md` is the artifact that asserts this project's process is honest; an unverifiable sentence in it costs more than it adds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-30 | Tampering | `.importlinter` comment edit | mitigate | Only comment lines change. Grep gates assert exactly three `[importlinter:contract:` headers and three `name = ` lines, that `pydantic` is still absent from the application contract's `forbidden_modules`, and that `tests/architecture/test_layer_boundaries.py` passes unmodified with its three-element `EXPECTED_CONTRACT_NAMES` |
| T-02-32 | Repudiation | documentation drifting from shipped code | mitigate | The four artifacts that contradicted ADR-020 are amended in one task, each edit asserted by a positive grep for the new text and a negative grep for the old, so a partial reconciliation fails the task |
| T-02-33 | Repudiation | an unverifiable claim in `AI_WORKFLOW.md` | mitigate | Every incident entry must quote a real capture; the plan forbids padding and names the three owed entries explicitly. `git diff` is asserted to contain no removed lines, so existing entries cannot be quietly rewritten |
| T-02-34 | Tampering | coverage reached by escape hatch | mitigate | Grep gates assert no `pragma: no cover` under `src`/`tests` and no `omit` in `pyproject.toml`, and the final run asserts 100% over `taskmanager` rather than merely clearing the 75% floor |
| T-02-35 | Tampering | hand edits inside a regenerated `CLAUDE.md` block | mitigate | Only the hand-maintained `## Project Rules` section is touched; the plan names the `<!-- GSD:* -->` blocks as out of scope because edits there are destroyed on regeneration |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan; the phase's "Package Legitimacy Audit" records it as not-applicable |
</threat_model>

<verification>
1. `grep -c '^\[importlinter:contract:' .importlinter` - 3, and `.venv/bin/lint-imports` exits 0 with all three KEPT
2. `.venv/bin/pytest tests/architecture -q --no-cov` - passes with `test_layer_boundaries.py` unmodified
3. `grep -q 'ADR-020' .planning/REQUIREMENTS.md .planning/ROADMAP.md .importlinter CLAUDE.md` - all four reference the decision
4. `git diff -- AI_WORKFLOW.md | grep -c '^-[^-]'` - 0
5. `grep -rc 'pragma: no cover' src tests` - 0 for every file
6. `make format && git diff --quiet -- src tests` - exit 0 (formatting is already canonical)
7. `make lint && make typecheck && make arch && make test` - exit 0 in one uninterrupted run, zero warnings
8. `.venv/bin/coverage report --include='*/taskmanager/*' --fail-under=100` - exit 0
</verification>

<success_criteria>
- ARC-05 and ROADMAP Phase 4 SC-5 name frozen dataclasses for application DTOs and cite ADR-020, so Phase 4 can be verified against its own text (user decision C-02)
- The stale `.importlinter` comment is corrected and the permissive application contract is documented as deliberate, with the three contracts and their names byte-identical (user decision C-02)
- The `CLAUDE.md` Project Rules application bullet matches the shipped code (user decision C-02)
- `pydantic` is still absent from the `application-framework-free` `forbidden_modules` list - a decision, recorded as such
- `AI_WORKFLOW.md` carries the Phase 2 human-versus-AI split, one new Verification Practices bullet, and at least three dated incident entries quoting real captures, appended without rewriting a single existing entry
- The full gate `make lint && make typecheck && make arch && make test` has been observed green in one uninterrupted run with 100% coverage over `taskmanager`, no `pragma`, no `omit` and zero warnings
</success_criteria>

<output>
Create `.planning/phases/02-domain-error-contract/02-07-SUMMARY.md` when done.
</output>
