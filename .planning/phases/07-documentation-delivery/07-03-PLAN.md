---
phase: 07-documentation-delivery
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - AI_WORKFLOW.md
autonomous: true
requirements: [AIW-01, AIW-02]

must_haves:
  truths:
    - "AI_WORKFLOW.md shows the real workflow in at least three Mermaid diagrams: the pipeline from brief to delivery, the gate set, and one verification round trip that caught a real defect"
    - "The human-decided vs AI-delegated section names every phase from 1 to 7"
    - "Every claim in that section points at an ADR id, a file, a test name or a commit hash, and at least one claim per phase cites a commit hash"
    - "The 'What I Did Not Do' section has a body listing the scope left out and the shortcuts rejected"
    - "No 'to be completed in Phase 7' marker survives anywhere in the file"
    - "Nothing in the incident log is rewritten: the 51 existing dated entries are byte-identical"
  artifacts:
    - path: "AI_WORKFLOW.md"
      provides: "Three Mermaid diagrams, the Phase 5/6/7 human-AI blocks with commit references, and the What I Did Not Do body"
      contains: "```mermaid"
  key_links:
    - from: "AI_WORKFLOW.md"
      to: "DECISION_LOG.md"
      via: "ADR ids cited inline, each resolving to a heading (gated by 07-04)"
      pattern: "ADR-0[0-9][0-9]"
    - from: "AI_WORKFLOW.md"
      to: "git history"
      via: "short commit hashes cited beside the claims they support"
      pattern: "[0-9a-f]{7}"
---

<objective>
Finish the document the project's differentiator rests on. `AI_WORKFLOW.md` already holds 51 dated
incident entries and a human/AI split for Phases 1-4 — written incrementally, as its own thesis
demands. What it lacks is every Mermaid diagram, the Phase 5, 6 and 7 blocks, commit references for
its claims, and the body of its own "What I Did Not Do" section.

Purpose: AIW-01, AIW-02, roadmap SC-5, and the open half of AIW-03 (whose tick is currently ahead of
the file).

Output: three diagrams, three more human/AI blocks, per-claim references, the missing section body,
and zero remaining Phase 7 placeholders. Nothing already in the file is reconstructed or rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-delivery/07-RESEARCH.md
@CLAUDE.md

<interfaces>
Measured facts (07-RESEARCH.md §Gap 4, read in session). Take them as given.

- `AI_WORKFLOW.md` is ~1,914 lines. Structure: `## How This Project Was Built` (line 22, a
  two-paragraph stub closing `_To be completed in Phase 7._` at line 29); `## Human-Decided vs
  AI-Delegated` (line 33) with **Phase 1, 2, 3, 4** blocks and closing
  `_Phase 7 completes this section with the per-claim commit/file/test references._` (line 169);
  `## Verification Practices` (line 173, **done**, no marker); `## Incident Log` (line 209, **51
  dated entries**, 2026-09-17 → 2026-09-19); `## What I Did Not Do` (line 1909, three-line preamble
  then `_To be completed in Phase 7._`).
- **Zero** fenced `mermaid` blocks today.
- Existing claims already cite ADR ids, `NN-CONTEXT.md` D-numbers, test names, evidence files and
  source paths. What none of them cites is a **commit hash** — that is the gap AIW-02's "traceable to
  a commit, file or test" leaves open.
- Repository facts for the diagrams and the claims: 332 commits, 100% conventional-commits, zero AI
  attribution trailers of any kind; 60 executed plans across six phases (8, 7, 11, 12, 17, 5); the
  suite is 1101 tests at 100.00% over 1659 statements; four import-linter contracts and eight
  architecture gates; `make break-check` sits outside every gate path by D-09; pre-commit is
  host-only while CI and the Docker `test` stage run the same tools as named steps (ADR-015).
- The three diagrams, each mapping to something that really happened:
  1. `flowchart TD` — brief → research (four agents) → REQUIREMENTS → ROADMAP (7 phases) → the
     per-phase discuss/research/plan/execute/verify/review loop → delivery.
  2. `flowchart LR` or `stateDiagram-v2` — the gate set: edit → pre-commit (black, isort, flake8,
     mypy, lint-imports) → `make test` (1101 tests, the 75% gate, 4 contracts, 8 architecture gates)
     → `make docker-test` (Python 3.13) → CI. Two honest annotations: `make break-check` is outside
     the loop by D-09, and pre-commit is host-only.
  3. `sequenceDiagram` — one real verification round trip: Phase 5's verification found the forged
     token gap, plan 05-17 closed it (ADR-084), re-verification passed 6/6. This is the diagram that
     proves the process caught something.
- **Mermaid rules for GitHub** (07-RESEARCH.md §Mermaid Constraints): only `flowchart TD`,
  `flowchart LR`, `sequenceDiagram`, `stateDiagram-v2`; **double-quote every label** containing
  `(`, `)`, `[`, `]`, `:` or `,`; no Font-Awesome icons, no click handlers, no emoji, no inline
  styling; `end` is never a bare node id. There is **no local renderer** and adding a Node toolchain
  to a Python deliverable is the wrong trade — rendering is verified by a human on GitHub after the
  push (plan 07-05's checkpoint).
- Two incident entries 06-04 explicitly asked Phase 7 to write: `make docker-test` having been broken
  for two phases unnoticed, and the CI leg of D-11's three-way coverage agreement. **Those belong to
  the dated Phase 7 entry, which plan 07-05 appends at the end of the phase** (the Standing Rule).
  This plan writes no dated incident entry.
- Plan 07-04 adds the gate that reads this file (≥ 3 mermaid blocks, phases 1..7 named, no surviving
  marker, every cited `ADR-NNN` resolving) and owns the `Dockerfile` `COPY` line — **do not add a
  Dockerfile change here**, and do not cite an ADR id that does not exist (07-02 took the log up to
  its new maximum; read the last `## ADR-` heading to know it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: How This Project Was Built — the three Mermaid diagrams (AIW-01)</name>
  <files>AI_WORKFLOW.md</files>
  <read_first>
    - AI_WORKFLOW.md lines 1-40 (the document's opening, its thesis about disagreeing with the
      repository, and the stub this task replaces)
    - AI_WORKFLOW.md §"Verification Practices" (lines 173-208 — the gate list the second diagram
      must agree with; do not contradict it)
    - .planning/ROADMAP.md §Overview and §Standing Rules (the seven phases and the per-phase loop)
    - .planning/phases/05-auth-assignment-notifications/05-VERIFICATION.md and 05-17-SUMMARY.md (the
      forged-token gap, its closure and the re-verification result — the third diagram's content)
    - 07-RESEARCH.md §Mermaid Constraints (the syntax rules this task is bound by)
  </read_first>
  <action>
    Replace the `_To be completed in Phase 7._` line under `## How This Project Was Built` with the
    real narrative plus the three diagrams described in the interfaces block, in that order, each
    with two or three sentences of prose above it saying what it shows and one honest caveat below it
    where there is one to make.

    Content rules:
    - The pipeline diagram shows what happened, not an idealised process: the four research agents
      and the fact that they disagreed, the discuss step that exists for some phases and not others,
      the verify step that sent Phase 5 and Phase 6 back for gap closure, and the code review after
      Phase 6 that found two criticals.
    - The gate diagram must agree with the `Makefile` and `.github/workflows/ci.yml` as they are:
      pre-commit on the host only, CI and the Docker `test` stage running the same tools as named
      steps because a runner has no `.venv` (ADR-015), and `make break-check` deliberately outside
      every gate path (D-09).
    - The sequence diagram walks the Phase 5 round trip end to end and names the artifact at each
      step, so a reader can open each one.
    - Syntax: only the four permitted diagram types; every label containing a bracket, parenthesis,
      colon or comma double-quoted; no emoji, no icons, no click handlers, no styling directives.
    - Numbers cited in the prose must be the current ones (1101 tests, 100.00% over 1659 statements,
      332 commits, 60 executed plans) — never a figure predicted by an earlier document.

    Leave every other section of the file untouched.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' AI_WORKFLOW.md` is at least 3, and the fences balance
      (`grep -c '^```' AI_WORKFLOW.md` is even)
    - `grep -c "To be completed in Phase 7" AI_WORKFLOW.md` decreased by 1
    - No diagram block contains an emoji, `fa:`, `click `, `style ` or `classDef`
    - Every diagram type used is one of `flowchart TD`, `flowchart LR`, `sequenceDiagram`,
      `stateDiagram-v2`
    - `git diff AI_WORKFLOW.md` touches no line inside `## Incident Log`
    - `make test` exits 0
  </acceptance_criteria>
  <done>The workflow is drawn from what happened, in three diagrams that obey GitHub's Mermaid limits, with the rendering check owed to plan 07-05.</done>
</task>

<task type="auto">
  <name>Task 2: Phases 5, 6 and 7 in the human/AI split, with commit references (AIW-02)</name>
  <files>AI_WORKFLOW.md</files>
  <read_first>
    - AI_WORKFLOW.md lines 33-172 (all four existing phase blocks — the exact shape: a short framing
      line, **Decided by the human**, **Delegated to AI**, each bullet carrying an ADR id, a D-number,
      a file or a test name)
    - .planning/phases/05-auth-assignment-notifications/05-CONTEXT.md and the 05-1*-SUMMARY files
      (what the human decided in Phase 5: the bearer scheme that raises no HTTPException, the
      404/403 matrix, the notification seam, ADR-084's placeholder refusal)
    - .planning/phases/06-test-hardening-coverage/06-CONTEXT.md, the five 06-0*-SUMMARY files and
      06-REVIEW.md (Phase 6's decisions, the two code-review criticals CR-01/CR-02, and the seven
      warnings recorded as open in ADR-096)
    - .planning/phases/07-documentation-delivery/07-RESEARCH.md §Delivery and §Open Questions (what
      the human decided for Phase 7, including keying the evidence map on the `[PDF x.y]` citations
      rather than redistributing the brief)
    - DECISION_LOG.md — the entries 07-02 added, for the ids the Phase 7 block cites
  </read_first>
  <action>
    Append Phase 5, Phase 6 and Phase 7 blocks in the existing shape, then replace the line-169
    promissory note (`_Phase 7 completes this section with the per-claim commit/file/test
    references._`) with the references themselves.

    For each of the three new blocks: what the human decided (with the ADR id or the `NN-CONTEXT.md`
    D-number for each) and what was delegated (with the file, the test name or the count it produced).
    Phase 7's block records the decisions that shaped this phase — the requirement-to-evidence map
    keyed on the `[PDF x.y]` citations already in REQUIREMENTS.md rather than redistributing the
    brief, the curated ambiguity block instead of a 96-row generated index, the rehearsal that
    executes the README's own commands instead of a pasted transcript, and the rehearsal leaving the
    developer stack down — and states plainly that the dated Phase 7 incident entry is appended at
    the end of the phase, per the project's own standing rule.

    Then add commit references. Find them with `git log --oneline --grep=<scope>` (for example
    `--grep='(05-17)'`) rather than guessing, and attach a short hash to at least one claim per
    phase — preferring the claims that are hardest to believe without one: the placeholder secret
    refusal, the coverage configuration pin, the two code-review criticals, the marker partition. Keep
    each reference inline and tiny (`ADR-084, commit abc1234`), never a separate table that has to be
    maintained.

    Do not touch the incident log, the Verification Practices section, or the four existing phase
    blocks except to extend a claim with a reference.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - The human/AI section names every phase 1..7: `grep -c "Phase 5\|Phase 6\|Phase 7" AI_WORKFLOW.md`
      matches inside that section's line range, and each of the seven has a **Decided by the human**
      and a **Delegated to AI** list
    - `grep -n "Phase 7 completes this section" AI_WORKFLOW.md` returns nothing
    - At least seven short commit hashes appear in the section, and **every one resolves**:
      `git cat-file -e <hash>^{commit}` succeeds for each
    - Every `ADR-NNN` cited in the new text resolves to a `^## ADR-NNN:` heading in `DECISION_LOG.md`
    - `git diff AI_WORKFLOW.md` touches no line inside `## Incident Log` or `## Verification Practices`
    - `make test` exits 0
  </acceptance_criteria>
  <done>Every phase has its split, and the claims a reader would most want to check carry the hash that lets them.</done>
</task>

<task type="auto">
  <name>Task 3: What I Did Not Do</name>
  <files>AI_WORKFLOW.md</files>
  <read_first>
    - AI_WORKFLOW.md lines 1905-1914 (the section's preamble, which sets what the body owes)
    - .planning/REQUIREMENTS.md §"Out of Scope" and §"v2 Requirements" (the scope deliberately left
      out: pagination and sorting, multi-value filters, an explicit invitation endpoint, refresh
      tokens, password reset, integer priorities)
    - .planning/phases/06-test-hardening-coverage/06-REVIEW.md and DECISION_LOG.md ADR-096 (the seven
      open gate-robustness warnings WR-03, WR-05, WR-06, WR-07, WR-08, WR-10, WR-11 and the IN-01..06
      informational items)
    - DECISION_LOG.md ADR-066, ADR-067, ADR-068 (the three conceded security properties)
    - 07-RESEARCH.md §Open Follow-Ups (the same inventory, already assembled)
  </read_first>
  <action>
    Replace the `_To be completed in Phase 7._` line under `## What I Did Not Do` with the body, in
    four short itemized groups. No prose padding, no apology, and nothing invented — every item must
    already exist in a requirement, a review or an ADR.

    1. **Scope deliberately out of v1** — pagination and sorting (API-01, ADR-043), multi-value
       filters (API-02), an explicit invitation endpoint (API-03), refresh tokens (AUTH-07), password
       reset (AUTH-08), integer priorities. One line each with the id.
    2. **Security properties conceded on purpose, not overlooked** — register's 409 is an
       account-enumeration oracle (ADR-066); no rate limiting on login (ADR-067); `GET /users` is an
       email directory any authenticated caller can read (ADR-068). Say why each was accepted for a
       take-home deliverable, and what the production answer would be.
    3. **Gates that are knowingly weaker than they look** — the seven open warnings from the Phase 6
       review (WR-03, WR-05, WR-06, WR-07, WR-08, WR-10, WR-11), each in one line, citing ADR-096 as
       the entry that records them as open. This group is the section's whole point: a project that
       lists its own gate weaknesses is making a different claim than one that does not.
    4. **Shortcuts considered and rejected** — `# pragma: no cover` and a coverage `omit` entry
       (forbidden by CLAUDE.md, and the 75% threshold never lowered); `Base.metadata.create_all()`
       instead of Alembic; SQLModel, which would have collapsed the domain/schema boundary the brief
       asks for; ruff instead of the brief-mandated flake8; a docker-compose override for the host
       fast loop (D-16); testcontainers instead of the compose database; `--no-verify` to commit
       through a red gate.

    One closing sentence: the only signing key this repository has ever published is the placeholder,
    and the application refuses to boot with it (ADR-084) — stated as a property, not a promise.
  </action>
  <verify>
    <automated>make lint && make typecheck && make arch && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "To be completed in Phase 7" AI_WORKFLOW.md` is 0
    - The section body names all seven of `WR-03`, `WR-05`, `WR-06`, `WR-07`, `WR-08`, `WR-10`,
      `WR-11` and all three of `ADR-066`, `ADR-067`, `ADR-068`
    - Every `ADR-NNN` and `WR-NN` id cited resolves — the ADRs to a `^## ADR-NNN:` heading in
      `DECISION_LOG.md`, the warnings to `.planning/phases/06-test-hardening-coverage/06-REVIEW.md`
    - `git diff AI_WORKFLOW.md` touches no line inside `## Incident Log`
    - `make lint && make typecheck && make arch && make test` exits 0
    - `git log -1 --format=%B` contains no `Co-Authored-By` and no `Generated with` line
  </acceptance_criteria>
  <done>The document says what was left out and where its own gates are weak, in the reviewer's own vocabulary, with an id per item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → evaluator | `AI_WORKFLOW.md` is the project's honesty claim; an unverifiable sentence in it costs more than a missing one |
| markdown → GitHub renderer | Mermaid source is executed by GitHub's renderer at view time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Repudiation | Claims about who decided what | mitigate | Every new claim carries an ADR id, a file, a test name or a commit hash, and the hashes are resolved with `git cat-file -e` in the acceptance criteria rather than asserted by eye |
| T-07-08 | Tampering | The 51 existing incident entries | mitigate | Every task's criteria include `git diff` touching no line inside `## Incident Log`; the log is appended to, never rewritten, and plan 07-05 appends the dated Phase 7 entry |
| T-07-09 | Denial of service | Mermaid blocks that do not render | accept | No local renderer exists and adding a Node toolchain to a Python deliverable is a cost an evaluator notices; the syntax is constrained to four diagram types with quoted labels, and rendering is a human checkpoint in 07-05 |
| T-07-10 | Information disclosure | Incident prose quoting past defects | accept | The defects are already public in `.planning/` and in the commit history; no credential, no live secret and no personal data is quoted |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Documentation only; no dependency is added |
</threat_model>

<verification>
- `make lint && make typecheck && make arch && make test` green.
- At least three `mermaid` blocks, all four-type-conformant, no emoji and no click handlers.
- Phases 1..7 each have a human/AI block; no Phase 7 placeholder marker survives anywhere.
- Every cited commit hash resolves and every cited ADR id resolves to a heading.
- `## Incident Log` is byte-identical to its pre-plan state.
</verification>

<success_criteria>
`AI_WORKFLOW.md` shows the real workflow in diagrams, states who decided what with something a reader
can open for every claim, and admits what was not done — including where its own gates are weak.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-delivery/07-03-SUMMARY.md` when done
</output>
