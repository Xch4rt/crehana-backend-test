---
phase: 07-documentation-delivery
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - DECISION_LOG.md
  - CLAUDE.md
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "Each of the brief's five named ambiguities resolves to a numbered ADR heading, not to a source docstring"
    - "The three allowed statuses, the transition matrix and the single forbidden move are decided in the log, with the rejected alternatives named"
    - "The three priority values, their ascending order and where the medium default is applied are decided in the log"
    - "ADR-019's stale 'never run on a real runner' claim and ADR-072's stale test path are corrected by a superseding entry, with both old entries left byte-identical"
    - "The decisions 07-01 shipped (the published error media type, the component registration, the tag table, the completeness gate) each have an ADR and a CLAUDE.md Project Rules bullet"
    - "A five-minute reader finds the five ambiguities and their ADR ids in the first screen of a 4,200-line log"
    - "The log is append-only mechanically: git diff shows no deleted entry line"
  artifacts:
    - path: "DECISION_LOG.md"
      provides: "The two ambiguity ADRs, the corrections ADR, 07-01's ADRs and the curated start-here block"
      contains: "## ADR-097"
    - path: "CLAUDE.md"
      provides: "A Project Rules bullet per new gate and per new published-document rule"
      contains: "problem+json"
  key_links:
    - from: "DECISION_LOG.md"
      to: "src/taskmanager/domain/value_objects/task_status.py"
      via: "the transition-matrix ADR naming the module that owns ALLOWED_TRANSITIONS"
      pattern: "ALLOWED_TRANSITIONS"
    - from: "DECISION_LOG.md"
      to: "tests/architecture/test_openapi_completeness.py"
      via: "the gate ADR naming the test that enforces it"
      pattern: "test_openapi_completeness"
    - from: "CLAUDE.md"
      to: "tests/architecture/test_openapi_completeness.py"
      via: "a Project Rules bullet naming the gate and what makes it red"
      pattern: "test_openapi_completeness"
---

<objective>
Close DOC-03. The log's *format* half is already satisfied — 96 ADRs, all four-part, spot-checked
against ADR-001/002/069/070/096. What is missing is two of the brief's five named ambiguities: the
allowed statuses and their transitions, and the priority values. Both are decided today only in a
domain docstring, which is the wrong place for a decision an evaluator is told to look for in
`DECISION_LOG.md`.

Purpose: DOC-03 and roadmap SC-2, plus the two stale claims the log itself handed to Phase 7 and the
ADRs 07-01's `src/` changes owe.

Output: two ambiguity ADRs, one superseding corrections ADR, the ADRs for 07-01, matching CLAUDE.md
Project Rules bullets, and a curated start-here block so the five ambiguities are findable in the
log's first screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-delivery/07-RESEARCH.md
@.planning/phases/07-documentation-delivery/07-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Measured facts (07-RESEARCH.md §Gap 2, read in session). Take them as given.

- `DECISION_LOG.md` is ~4,227 lines, `ADR-001`..`ADR-096`. **The next free number is ADR-097.**
  Heading form: `## ADR-0NN: <lowercase statement of the decision> (date, amending/extending ADR-0MM)`,
  then `**Context**`, `**Options**` (bulleted, each rejected option named with its reason inline),
  `**Decision**`, `**Consequences**` (bulleted, bold opening clause).
- The brief's five ambiguities and who answers them **after this plan**:
  | # | Ambiguity | ADR |
  |---|-----------|-----|
  | 1 | Completion-percentage scope (whole list vs filtered subset) | ADR-009 (refined by ADR-049, ADR-054) — already covered |
  | 2 | Allowed task status values and transitions | **this plan** |
  | 3 | Priority values | **this plan** |
  | 4 | Who may be assigned to a task | ADR-069 (+ ADR-068) — already covered |
  | 5 | What triggers the fake invitation | ADR-070 (+ ADR-069 consequences) — already covered |
  Plan 07-04's gate pins exactly this mapping, so the ids chosen here are the ids that test reads.
- The truth ambiguity 2 must state, read from
  `src/taskmanager/domain/value_objects/task_status.py`: `pending`, `in_progress`, `completed`;
  `ALLOWED_TRANSITIONS` = pending→{in_progress, completed}, in_progress→{pending, completed},
  completed→{in_progress}; **completed→pending is the single forbidden move**; `StrEnum` rather than
  `(str, Enum)` because since Python 3.11 the latter formats as `TaskStatus.PENDING`; the matrix is
  exhaustive on purpose so a fourth status raises `KeyError` instead of falling through a permissive
  default.
- The truth ambiguity 3 must state, from `task_priority.py`: `low|medium|high`, ascending, and the
  `medium` default is applied by the `Task` entity (`Task.DEFAULT_PRIORITY`, bound to the schema
  default by a test) and **not** by the enum — deliberately, so the vocabulary every layer shares
  carries no policy. Integer priorities are in REQUIREMENTS.md "Out of Scope".
  ADR-048 decides *where the status door is* (a dedicated endpoint) and mentions the matrix only in
  passing; it is the entry the new status ADR should relate itself to.
- Both docstrings already contain the reasoning. These two ADRs are transcription plus the rejected
  options, not new thinking.
- Two stale claims the log hands to Phase 7:
  (a) ADR-019 (~line 587) says "the workflow has never run on a real runner" — false since plan
  01-08, CI run `35301518310` concluded success on the first attempt;
  (b) `DECISION_LOG.md:3025`, inside ADR-072, names `tests/api/test_error_contract.py`, a path
  06-04 moved to `tests/unit/presentation/test_error_contract.py`.
- The append-only rule governs **entries**, not the header region: 06-04 recorded a header edit for
  which `git diff DECISION_LOG.md | grep -c '^-'` returned 1 (the diff's own `--- a/…` line).
  Precedents for correcting by id: ADR-020→ADR-004, ADR-041→ADR-017/018, ADR-028→ADR-021.
- Nothing in `tests/`, `scripts/`, `.pre-commit-config.yaml` or `.github/` reads `DECISION_LOG.md`
  today, so this plan breaks no gate. Plan 07-04 adds the gate that reads it, and 07-04 owns the
  `Dockerfile` `COPY` line that gate needs — **do not add a Dockerfile change here.**
- The full 96-row table of contents is deliberately **not** being written (orchestrator decision on
  Q7): a hand-maintained 96-row list is a second home for the truth and drifts on ADR-097, and a
  generated one needs a generator, a sync gate and another `COPY`. The curated block plus 07-04's
  README evidence map is the chosen form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two missing ambiguity ADRs (DOC-03)</name>
  <files>DECISION_LOG.md</files>
  <read_first>
    - DECISION_LOG.md lines 1-17 (the header's declared four-part shape) and ADR-096 in full (the
      current house style, including the `(date, amending ADR-0NN)` heading suffix)
    - DECISION_LOG.md ADR-009 and ADR-048 (the neighbours these two entries relate to: the
      whole-list percentage, and where the status door is)
    - src/taskmanager/domain/value_objects/task_status.py (in full — the matrix and the docstring's
      reasoning, which is what gets transcribed)
    - src/taskmanager/domain/value_objects/task_priority.py (in full)
    - src/taskmanager/domain/entities/task.py (`DEFAULT_PRIORITY` and where the default is applied)
    - .planning/REQUIREMENTS.md §"Out of Scope" (integer priorities)
  </read_first>
  <action>
    Append two ADRs, starting at the next free number (ADR-097 today — confirm by reading the last
    `## ADR-` heading rather than trusting this line).

    **ADR-097 — the three task statuses and the one forbidden transition.** Context: the brief names
    the status vocabulary and the legal moves as an ambiguity to resolve, and the resolution has
    lived in a domain docstring since Phase 2 (D-01), which is not where a reader is told to look.
    Options, each with its rejection reason: a free-text status string; a two-state
    `done`/`not done` flag; the three states with a *permissive* default (any move allowed unless
    listed); the three states with an **exhaustive** matrix. Decision: `pending`, `in_progress`,
    `completed` as a `StrEnum`, with `ALLOWED_TRANSITIONS` exhaustive and `completed → pending` the
    single forbidden move, refused with 409 `invalid_status_transition`. Consequences: a fourth
    status raises `KeyError` rather than being silently permitted; the same-state move is an
    idempotent no-op that returns the entity byte-identical (D-02); `StrEnum` rather than
    `(str, Enum)` because the latter serialises as `TaskStatus.PENDING` on Python 3.11+; the move is
    refused by the entity, so no route can bypass it; the matrix's complement is gated by a test and
    by break 2 of `scripts/break-check.sh`. Relate it to ADR-048 by id (which decided the dedicated
    status endpoint and mentioned the matrix only in passing).

    **ADR-098 — three priority values, and the default lives on the entity.** Context: the brief
    leaves the priority vocabulary open. Options with rejection reasons: integers (`1..5`) — no
    shared meaning across clients, and recorded as v2 "Out of Scope"; an open string; a
    `low|medium|high` enum with the default applied by the **enum**; the same enum with the default
    applied by the **entity**. Decision: `low|medium|high` ascending as a `StrEnum`, with
    `Task.DEFAULT_PRIORITY = medium` applied by `Task.create`. Consequences: the vocabulary every
    layer shares carries no policy, so the default can change in one place; the schema default is
    bound to the entity ClassVar by a test rather than restated; an enum-typed boundary field refuses
    a non-member, which is why `reprioritise` carries no value guard (an unreachable branch the
    no-pragma coverage rule could not excuse); a numeric ordering — sorting by priority — is not
    supported today and is named as such.

    Write both in the ADR-096 format exactly. Do not edit, reflow or renumber a single existing
    entry.
  </action>
  <verify>
    <automated>.venv/bin/pytest -m unit --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## ADR-0" DECISION_LOG.md` increased by exactly 2 and the numbering is contiguous
      with no gap or duplicate (`grep -o "^## ADR-[0-9]*" DECISION_LOG.md | sort | uniq -d` is empty)
    - `git diff DECISION_LOG.md | grep -c '^-'` is 1 (the diff's own header line — no existing line
      was deleted or reflowed)
    - The five ambiguities each resolve: `grep -c "^## ADR-009:\|^## ADR-069:\|^## ADR-070:\|^## ADR-097:\|^## ADR-098:" DECISION_LOG.md` is 5
    - Both new entries contain all four of `**Context**`, `**Options**`, `**Decision**`,
      `**Consequences**`
    - The transition ADR names every one of `pending`, `in_progress`, `completed` and states
      `completed` → `pending` as the forbidden move; the priority ADR names `low`, `medium`, `high`
      and `Task.DEFAULT_PRIORITY`
    - `.venv/bin/pytest -m unit --no-cov` exits 0 (nothing reads the log yet; this is the regression check)
  </acceptance_criteria>
  <done>All five brief ambiguities are answered by a numbered ADR, and the two new ones say what the code says.</done>
</task>

<task type="auto">
  <name>Task 2: The corrections ADR, 07-01's ADRs, and the CLAUDE.md bullets</name>
  <files>DECISION_LOG.md, CLAUDE.md</files>
  <read_first>
    - DECISION_LOG.md ADR-019 (the stale runner claim, in full) and ADR-072 around line 3025 (the
      stale test path) — read them to quote them, never to edit them
    - DECISION_LOG.md ADR-041 (the "follow-up to ADR-017 and ADR-018" precedent for correcting by id)
    - .planning/phases/07-documentation-delivery/07-01-SUMMARY.md (what 07-01 actually shipped and
      what it measured — the ADRs must describe the shipped form, not the planned one)
    - CLAUDE.md §"Project Rules" (the bullet style: the rule, the file that enforces it, what fails)
    - .planning/STATE.md §Blockers/Concerns (where the ADR-019 handover is recorded)
  </read_first>
  <action>
    Append, continuing the numbering from task 1.

    **The corrections ADR** — one entry carrying both stale claims, in the ADR-041 shape
    ("follow-up to ADR-019 and ADR-072"). Context: the log is append-only, and two of its sentences
    are now false — ADR-019's "the workflow has never run on a real runner" (CI run `35301518310`
    concluded success on the first attempt in plan 01-08) and ADR-072's reference to
    `tests/api/test_error_contract.py` (moved to `tests/unit/presentation/test_error_contract.py` by
    plan 06-04). Options with rejection reasons: edit both in place (destroys the property that makes
    the log worth reading, and shows as deletions in the diff); leave them and mention the drift in
    the README (puts the correction where the reader of the wrong sentence will not be); a
    superseding entry per claim (two entries for two one-line factual corrections is ceremony); one
    superseding entry naming both by id. Decision: the last. Consequences: the two old entries stay
    byte-identical and are read *with* this one; the mechanism is stated once for the next phase that
    needs it; and note that Phase 7's push finally runs Phases 2-6 on a runner for the first time, so
    ADR-019's underlying worry gets its real answer in plan 07-05.

    **One ADR per decision 07-01 shipped**, each stating what was measured rather than what was
    assumed:
    - The published error media type and the component registration: the no-`model` +
      explicit-`$ref` form with a `FastAPI` subclass, because `{"model": M}` publishes
      `application/json` — a media type this API never emits — and two of the other three spellings
      publish both. Name the three rejected spellings and that each was executed. Consequences: the
      component is registered in one override; `mypy --strict` rejects the documented
      `app.openapi = fn` alternative as `method-assign`; `/health` 503 is exempt by D-08.
    - The completeness gate: `tests/architecture/test_openapi_completeness.py` as a totality gate
      with set equality on tags, an exemption that must keep earning itself, and a non-vacuity guard
      that **fails** rather than skips (the WR-06 lesson). State that it adds no pre-commit hook and
      no CI step because it rides inside pytest — CLAUDE.md's two-places rule applies to a gate that
      introduces a new command.
    - The `openapi_tags` table, if 07-01's SUMMARY records a decision worth its own entry; otherwise
      fold it into the media-type entry as a consequence rather than inventing a third ADR.

    Then add the matching `## Project Rules` bullets to `CLAUDE.md` (hand-maintained section, outside
    every `<!-- GSD:*-start -->` block — do not disturb the delimiters):
    - Under a new or existing subsection on the published document: every non-2xx response leg is
      declared through `problem_response()` and never with a `model=` key; `/health` 503 is the one
      exemption; enforced by `tests/architecture/test_openapi_completeness.py`, which fails naming
      the offending `method path code`.
    - A module added under `src/taskmanager/presentation/api/` joins `REQUIRED_SCANNED_MODULES` in
      the commit that creates it — extend the existing bullet's module list rather than adding a
      second bullet, if that reads better.

    Nothing in this task edits an existing ADR.
  </action>
  <verify>
    <automated>make lint && make typecheck && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make lint && make typecheck && make arch && make test` exits 0
    - `git diff DECISION_LOG.md | grep -c '^-'` is still 1 across both tasks
    - `grep -n "35301518310" DECISION_LOG.md` matches in the new entry, and
      `grep -c "tests/unit/presentation/test_error_contract.py" DECISION_LOG.md` is at least 1
    - ADR-019's and ADR-072's own text is unchanged: `git diff DECISION_LOG.md` shows no hunk
      touching either entry
    - `grep -n "test_openapi_completeness" CLAUDE.md` matches, and the new bullets sit inside
      `## Project Rules` with every `<!-- GSD:` delimiter untouched (`git diff CLAUDE.md` shows no
      deleted delimiter line)
    - ADR numbering is still contiguous with no duplicate
  </acceptance_criteria>
  <done>The log's two false sentences are corrected without being edited, 07-01's measured decisions are recorded, and each new rule exists in both of its homes.</done>
</task>

<task type="auto">
  <name>Task 3: The curated start-here block in the log header</name>
  <files>DECISION_LOG.md</files>
  <read_first>
    - DECISION_LOG.md lines 1-19 (the header region and the `---` that closes it, which is where the
      block goes — before the first `## ADR-001` heading)
    - 07-RESEARCH.md §Gap 2 "On an index for a 4,227-line log" (why the 96-row TOC is rejected)
  </read_first>
  <action>
    Insert a short start-here block into the header region — after the existing paragraphs, before
    the closing `---` and the first entry — by **adding lines only**, so the append-only diff
    property survives (`git diff | grep -c '^-'` stays 1).

    Two small tables and nothing else, about twenty lines total:
    - **The brief's five ambiguities → the ADR that decides each**, using the ids fixed in task 1:
      completion-percentage scope → ADR-009 (with ADR-049, ADR-054); statuses and transitions →
      the task-1 status ADR; priority values → the task-1 priority ADR; who may be assigned →
      ADR-069 (with ADR-068); what triggers the invitation → ADR-070. This is the table plan 07-04's
      gate reads, so the ids must be exactly these.
    - **The five decisions an evaluator is most likely to question**, one line each with its ADR id:
      PostgreSQL rather than SQLite; 404 for invisible resources and 403 for visible-but-forbidden;
      the completion percentage over the whole list rather than the filtered view; PyJWT and pwdlib
      rather than python-jose and passlib; flake8 rather than ruff (the brief mandates the linter).

    One sentence above them saying what the block is for and that the entries below are in
    chronological order, never rewritten. Do **not** write a 96-row table of contents, and do not add
    a generator.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0
    - `git diff DECISION_LOG.md | grep -c '^-'` is 1 across all three tasks of this plan
    - The block sits above the first `## ADR-` heading: the line number of the block's first table
      row is less than the line number of `^## ADR-001:`
    - Every ADR id the block cites resolves to a `## ADR-NNN:` heading (check each with grep; 07-04's
      gate will assert this automatically once it exists)
    - The block is under 30 lines and names no ADR that does not exist
    - `git log -1 --format=%B` contains no `Co-Authored-By` and no `Generated with` line
  </acceptance_criteria>
  <done>A reader who opens a 4,200-line log lands on the five ambiguities and the five contested decisions with the ADR id for each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → evaluator | `DECISION_LOG.md` is read as a record of what was decided *when*; an edited entry is an unfalsifiable claim |
| documentation → code | An ADR that describes a rule the code does not implement is worse than no ADR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Repudiation | `DECISION_LOG.md` history | mitigate | Corrections land as a superseding entry naming ADR-019 and ADR-072 by id; the `git diff … grep -c '^-'` criterion is the mechanical proof no line was deleted, and the header block is inserted rather than rewritten |
| T-07-05 | Tampering | The ambiguity-to-ADR mapping | mitigate | The five ids are fixed here and pinned by 07-04's gate, so a renumbered or deleted ADR becomes a red test rather than a broken cross-reference |
| T-07-06 | Information disclosure | New ADR prose | accept | The entries quote the transition matrix, the priority vocabulary and a public CI run id — all already public in the tracked source. No secret, no credential, no live `JWT_SECRET` is quoted; the only key this repository has ever published is the placeholder the app refuses at boot (ADR-084) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Documentation only; no dependency is added |
</threat_model>

<verification>
- `make lint && make typecheck && make arch && make test` green.
- `git diff DECISION_LOG.md | grep -c '^-'` is 1 for the whole plan.
- All five ambiguity ADR ids resolve to headings, and every id the header block cites exists.
- ADR-019 and ADR-072 are byte-identical to their pre-plan state.
</verification>

<success_criteria>
Every brief ambiguity, and every decision this phase's `src/` change made, is recorded in the one
file the brief names for it — as context, options, decision and consequences, appended and never
rewritten.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-delivery/07-02-SUMMARY.md` when done
</output>
