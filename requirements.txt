# Runtime dependencies - exact pins only (single source of truth for versions).
# Some packages below are not imported until later phases; they are pinned now
# so the Docker layer and the CI cache key never change shape.

# Web framework and typed boundaries
fastapi==0.141.1
pydantic==2.13.5
pydantic-settings==2.15.0
uvicorn[standard]==0.53.0

# Database access and migrations
SQLAlchemy[asyncio]==2.0.54
psycopg[binary]==3.3.5
alembic==1.20.0

# Authentication and hashing
PyJWT==2.14.0
pwdlib[argon2]==0.3.1
# anyio is imported directly by infrastructure/security/passwords.py, which runs
# Argon2 off the event loop, and is deliberately NOT given its own exact pin: it
# is a hard transitive dependency of Starlette, which the FastAPI pin above
# already fixes, so a second pin here could only ever contradict it. Same
# convention requirements-dev.txt records for coverage and grimp.

# Form parsing and email validation used by the API boundary
python-multipart==0.0.32
email-validator==2.3.0
